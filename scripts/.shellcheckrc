# shellcheck の設定 (CI の check ジョブが `shellcheck scripts/*.sh scripts/mx` で読む。T17.13)
#
# SC2016 (単一引用符の中の $ や ` が展開されない) は切る: この道具たちは Markdown の要約を
# printf の書式に書くので `…` を文字どおり出したいし、bash -c '…"$1"…' や正規表現の $ も
# わざと単一引用符に入れている。展開したい所は無く、22 か所に 1 つずつ書くと本文が読みにくくなる
disable=SC2016
