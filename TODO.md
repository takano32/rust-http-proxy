# TODO.md — 認証なし・手軽・最速の HTTP Proxy にするための作業一覧

> このファイルは 2026-09-19 まで `TASKS.md` という名前だった (T15.14 で改名。`git log --follow TODO.md` で履歴を追える)。
> 済んだタスクの本文と `結果:`、過去のコミットメッセージに出てくる `TASKS.md` はこのファイルのことで、歴史としてそのまま残してある。
>
> **雪像の置き場は 2026-09-26 に `~/rust-http-proxy-status/` からリポジトリの `status/` (`.gitignore` 済み) へ移した** (利用者の決定。`$HOME` には書かない)。
> `scripts/collect-deployed.sh` の既定の `DIR` も `status/`。
> **機械のロック `mx` は `scripts/mx`** (同じ日にリポジトリへ入れた。本文の `mx …` は `scripts/mx …` のこと)。済んだタスクの本文に出てくる `~/rust-http-proxy-status/…` は、いまは `status/…` にある。

**この文書の形式**: 済んだ作業もこれからの作業も、同じ 1 つの形で書く。

```
- [x] **Tn.n 題**
  - 目的: なぜやるか
  - 変更箇所: どのファイル (済んだものは、いまコードがある場所)
  - やること: 何をするか
  - 受け入れ基準: どうなったら終わりか
  - 結果: 実測値と結論 (`コミット`)   ← 終わったものだけ
```

1 タスク = 1 コミット。性能に関わるものは **変更前後を必ず測って** コミットメッセージ本文に貼る。
測って「やらない」と決めたものは消さずに §4 に残す (同じ道を二度調べないため)。
番号は付けた順で、並び替えても振り直さない (コミットメッセージから引けるように)。**各 Phase の `Tn.99` は「デプロイ後の様子見と修正、
締めの文書」に予約する** (Phase 14 から。デプロイして数字を見てから書くタスクを、その Phase の最後に置くため。`.9` ではなく `.99` なのは、
タスクが 9 つを超えても番号がぶつからないように)。**`Tn.99` の最後の手順は「次のターンの準備」** (2026-09-24、利用者の決定): 判定のあと、
(1) 次の再デプロイで何を判定するか (次の Phase の完了の定義の下書きと、今回「判定できず」になった項目) を並べ、(2) それぞれに要る計器がデプロイ先で**いま**出ているかを
雪像で確かめ、(3) 足りない計器は `T(n+1).0` として**次の再デプロイの前に**入れる (Phase 15 の T15.0 と同じ「計器が先」)。(4) 利用者に再デプロイを頼むのは、(3) が済んでから。
計器の無いまま出すと、その 24 時間は判定に使えない (2026-09-24 の T15.99 は、再デプロイを頼んだあとで計器の不足に気づいた)。

| 節 | 何が書いてあるか |
|---|---|
| §0 | ゴールと、守ること |
| §1 | 計測の作法 (道具と手順) |
| §2 | 現在地 (数字の一覧) |
| §3 | やったこと (Phase 0〜7) |
| §4 | 測って採らなかったもの |
| §5 | これから (Phase 8〜16。Phase 12 は完了、Phase 13 は個票まで完了・名前解決は届かず、Phase 14 は T14.0 で T14.1〜T14.3 を決めた、Phase 15 は T15.12 / T15.15 が残り、Phase 16 は T16.0 (計器) と T16.99 (判定: 3 つとも満たした、`codegen-units = 4` はそのまま) まで済み。版 `2e57626` は 2026-09-24 12:36 UTC から動いている、Phase 17 は 2026-09-26 に候補 22 を全部タスクにした (未着手。実装は Opus (medium))) |
| 付録 A | 計測の記録 (時系列) |

## 0. ゴールと前提

**ゴール**: 「バイナリを 1 つ置いて起動するだけで使える、認証なしの HTTP/HTTPS(CONNECT) プロキシ」を、
同じ条件でこれ以上速くならないところまで速くする。**速さは必ず計測して示す** (印象で判断しない)。

**到達**: loopback は Phase 11 まで (§2 の表)。**デプロイ先** (`nagoya.sorahost.net:50697`、Pterodactyl コンテナ) は
**2026-09-26、Phase 16 (T16.0 + `codegen-units = 4`) をデプロイして 47.2 時間の実測** (§2 の「デプロイ先の現在地 (2026-09-26)」。出どころは
`status/2026-09-26T114602Z-snapshot.json`。Phase 15 の 126.6 時間の 1 枚と Phase 14 の 36.6 時間の 1 枚は付録 A の「デプロイ先の現在地 (2026-09-24)」「(2026-09-18)」):

- **AAAA のあるホストの CONNECT 確立 257 → 9 ms** (Phase 12 / T12.1。Happy Eyeballs の `STAGGER` 250 ms を払わなくなった)
- **名前解決のミス 0.55 → 0.144 回/接続** (Phase 14 / T14.1 の keep-warm。接続 1 本あたり 6.4 → 2.8 ms)
- **平常時の CONNECT 確立 p50 は 8.2 → 8.4 ms で動かず、完了の定義の 6 ms には届かなかった**。理由は測って分かった:
  確立の中央値は**すでにオリジンまでの 1 RTT そのもの** (確立 − カーネルが測った RTT の中央値が −0.21 ms) で、窓に出る宛先で
  最良 RTT が 6 ms 未満のものは 1 本も無い (個票 614 本の `rtt_ms.origin` は最小 6.72 ms)。**6 ms はコードでは届かない数字**で、
  基準の立て方の方が誤っていた (§4 の最終行、Phase 14 の「判定 (2026-09-18)」)。
- **2026-09-24 (Phase 15、126.6 時間)**: **`discord.com` の名前解決のミス率 0.441 → 0.01** (T15.4 で keep-warm の窓を 900 → 3,600 秒。
  0.441 は T15.0 の版の 9 時間、0.01 は 10 / 1,600。`/hosts` の差分)、全体 0.40 → **0.05 回/接続** (平常時。`/history?res=3600`)。
  **CPU の絞り 0 / 889,600 周期** (T15.5。`kernel.cgroup_cpu.since_start`。Phase 14 は 0.5 コアを 24 時間張り付かせていた)、
  **connect の `timeout` 0 件** (T15.6 の既定 10 秒のまま。`/history` の `errors_by_cause`)。
  **次の基準線は平常時の `wait` p50 約 8.2 / p95 約 189 ms** (`/history?res=3600`。p50 は 1 RTT のまま、p95 の尾は遠い相手の connect
  で、相手までの距離のせいなのでコードでは縮まない。§4 の最終行、T15.99 の「判定 (2026-09-24)」)

**守ること (非目標も含む)**:

- **認証は入れない**。`Proxy-Authorization` を要求するコードは書かない (履歴上 `dad8984 refactor: remove authentication` で意図的に外した)。
- **外部クレートは追加しない**。依存してよいのはこのリポジトリのコードだけ (`crates/` のワークスペースメンバー)。
  crates.io のクレートは 1 つも入れない (`libc` も入れない)。
  システムコールが必要なら `crates/sys/src/sys.rs` (`poll` / `epoll` / `splice` / `recv`)、`crates/sys/src/signal.rs`、
  `crates/sysinfo/src/sysinfo/inotify.rs`、`crates/tls/src/tls.rs` (`dlopen`) と同じく `unsafe extern "C"` で直接宣言し、
  `#[cfg(target_os = "linux")]` で囲んで他 OS には従来コードへのフォールバックを残す。
- **既存機能を壊さない**。`cargo test --workspace` は常に全通過。キャッシュ・ダッシュボード等は
  「使わないときに一切コストがかからない」ようにするのが方針で、削除はしない。
- Rust は `edition = "2024"`、stable の `rustc` で動くこと (版は固定しない)。`cargo clippy --workspace --all-targets` の警告を増やさない。
- **`cargo build --release` はメモリ 120 MB で通ること** (動作環境のコンテナが小さい。2026-09-16 に 200 → 180、2026-09-26 に T17.11 (T15.12 段 8) で 180 → 120 に下げた。
  手元の通る最小は 90 MB)。`scripts/build-memory.sh` が CI で見張っている (実際にその cgroup に入れてビルドする)。
  効くのは 2 つだけ: **クレートを小さく割ること** (`rustc` はクレート単位で全部を抱える。
  **ファイルを割っても下がらない**) と、**`jobs = 1`** (`.cargo/config.toml`。並列に走る rustc の
  合計が上限を超えるため)。
- **1 ファイルは 2,000 行を目安**にする。超えたら責務で割る。
- コメントとログの文体は既存に合わせる (コメントは日本語、ログ文字列は英語)。
- 動作環境は Linux (Pterodactyl コンテナ) が主。`SERVER_PORT` / `SERVER_MEMORY` / `$HOME/.env` の扱いは変えない。
- **作業で `$HOME` に書かない** (2026-09-26、利用者の決定。**例外は rustup / cargo の `~/.rustup` と `~/.cargo`** — ほかの作業でも使うので許す)。雪像は `status/`、一時ファイルはスクラッチパッドか `/tmp`、手元で起動するプロキシは
  `HOME` をスクラッチに向ける (プロキシは `$HOME/.rust-http-proxy.*` に状態を書くため)。**作業に要る自作の道具は `scripts/` に入れてコミットする** (`scripts/mx` など)。
- **権限を上げる手段 (`sudo`・`sysctl` で締め付けを緩める等) は、よほどのものでなければ使わない** (同日、利用者の決定)。`unshare` の要る
  `scripts/deployed-like.sh` は CI (`deployed-like-snapshot`) で回す。

**作業の進め方 (毎タスク共通)**:

1. 着手前に対象ファイルを読み、README の該当箇所を確認する。**着手前に「今どうなっているか」を測る。**
2. 実装 → `cargo fmt` → `cargo clippy --workspace --all-targets -- -D warnings` → `cargo test --workspace --no-fail-fast` (CI と同じ。1 本落ちても残りを走らせる) → `cargo build --release`。
3. 性能に関わるタスクは **変更前後の計測値** を取り、コミットメッセージ本文に貼る (§1)。
4. 環境変数や挙動を足したら README の表と説明を更新する。
5. コミットメッセージは既存に合わせて `feat:` / `fix:` / `perf:` / `docs:` / `chore:` / `refactor:` / `test:` の接頭辞。
6. 判断に迷ったら「速い方・単純な方・既定で安全な方」を選び、理由をコメントに残す。
7. 終わったらこの文書の該当タスクを `[x]` にし、`結果:` に実測値とコミットを書く。§2 の表に効く数字が変わったら更新する。
8. 実装を Opus (サブエージェント) に渡すときは §5 冒頭の「実装を Opus に渡すときの決まり」を指示文に貼る。
9. **再デプロイの直前と直後に `scripts/collect-deployed.sh` を 1 枚ずつ撮る** (直前 = 判定の「前」、直後 = 0 時間の雪像)。利用者が再起動したら親が撮り、その Phase の `Tn.99` の前提に**版 (`/status` の `version`) と起動時刻 (UTC)** を書く (T17.19。T16 の再デプロイは記録が無く、2026-09-26 に `uptime_secs` から起こした)。README の「運用」の 1 と 2 も同じ内容。

## 1. 計測の作法

**この機械は big.LITTLE** (cpu0-3 = Cortex-A55 / cpu4-7 = Cortex-A78、システムコールのコストが 2.2 倍違う)。
**必ず `taskset` でプロキシを 4-7、ベンチを 0-3 に固定する。** 固定しないとぶれが ±5-10% になり、
数 % の差は見えない。

**`--only connect` は tunnels/s (ops) を読まない**: TIME_WAIT 律速で同じバイナリでも ±24% 振れ、交互の組の中の位置だけで中央値が
10% 動く (T14.1 の対照実験)。主指標は CPU/本 だけ、**前後交互は 6 組** (実例: T13.4 の個票を足した変更は 3 組で +5.6% と出て、
6 組に増やしたら +1.4% に収まった)。他の経路 (forward / HIT / tunnel / 1 接続 1 要求) は 3 組のままでよい。この決まりは
`scripts/cpu-per-request.sh` の先頭の注記にも書いてある (T14.2 (6))。

**ただし `--only tunnel` だけは例外で、プロキシ cpu4-5 / ベンチ cpu6-7 (両方 big)。**
ベンチの blaster と reader はどちらも本気で回るスレッドで、LITTLE に置くとベンチが先に頭打ちになる
(同じバイナリで 1,534 → 3,956 MiB/s)。スクリプトが `--only tunnel` のときだけ自動で切り替える (T10.8)。

主指標は **CPU/要求** (`/proc/<pid>/stat` の utime+stime ÷ 要求数)。スループットは律速がベンチ側に
移るので補助でしかない。負荷に依らない指標として **1 要求あたりの確保回数** (数えるアロケータ) と
**システムコール回数** (`strace -f -c`) も使う。

メモリは 2 つを見る: **暇な接続 1 本あたりの RSS** (実行時) と、**ビルドが通る最小の cgroup 上限** (ビルド時、上限 200 MB)。

### 速さの測り方

`scripts/cpu-per-request.sh` が「固定して起動 → ベンチ → utime+stime の差 ÷ 操作数 → ピーク RSS とスレッド数」までやる。
ベンチ (`crates/bench`) は既定のビルド対象から外してあるので `cargo build --release -p proxy-bench` で作る。

**どの経路も同じ条件で測る** (T10.11)。プロファイルは `--lite`、ログ水準は `warn`、キャッシュの ballast は off。
§2 は「経路どうしを比べる表」なので、1 行だけ条件が違うと比べられない数字が並ぶ。
**条件はスクリプトが引き受ける**ので、環境変数を並べる必要はない
(以前は HIT の行だけ `PROXY_ARGS=""` = 既定プロファイル = ログ **info** で回す手順になっていて、
そこにだけアクセスログ 1 行 7.2 us/要求 が乗っていた。T10.2 が見つけ、T10.11 で直した)。

```bash
cargo build --release && cargo build --release -p proxy-bench   # 道具を作る

scripts/cpu-per-request.sh                                   # forward 8 並列 keep-alive
scripts/cpu-per-request.sh --conc 64                         # forward 64 並列
scripts/cpu-per-request.sh --cacheable                       # キャッシュ HIT
scripts/cpu-per-request.sh --no-keepalive                    # 1 接続 1 要求 (接続あたりの固定費)
scripts/cpu-per-request.sh --only connect                    # CONNECT の確立
scripts/cpu-per-request.sh --only tunnel --conc 1            # トンネル 1 本 (主指標は CPU/MiB)
PROXY_MAX_CONNS=8192 scripts/cpu-per-request.sh --only idle-tunnels --conc 5000  # 暇なトンネル 5,000 本
PROXY_MAX_CONNS=8192 scripts/cpu-per-request.sh --only idle-conns --conc 2000    # 暇な keep-alive 接続 2,000 本

BIN=target/dist/rust-http-proxy scripts/cpu-per-request.sh   # dist で測るときは BIN= を差し替えるだけ
```

**`--only idle-tunnels` / `--only idle-conns` で見るのはスレッド数と RSS** (CPU/op ではない)。
どちらも「確立 + 保持 + 終わりの一斉 close」の合計を本数で割った値になるので CPU は弱い指標
(T8.1、T11.4 でも 285〜425 us と揺れた)。読むのは `threads N -> M (max X, median Y)` と `proxy peak RSS` と `parked max`。
**`--only idle-conns` は 1 要求ずつ通してから握る** (T11.4)。要求を 1 本も通していない接続は `src/lib.rs` の
預ける判定 (`*served > 0`) を満たさないので**そもそも預けられず**、ワーカーを握ったまま
`PROXY_MAX_THREADS` (auto = 256) に当たって止まる。
**握る秒数は `PROXY_KEEPALIVE_SECS` (既定 15 秒) より短くする** (越えると預かり所が期限切れで閉じる。
ベンチが最後に「まだ生きている本数」を出すので、減っていればその行で分かる)。
**§2 の「着手前」の列 (預けない場合) を測るには `PROXY_PARK_IDLE=off PROXY_MAX_THREADS=0` を足す**
(上限を外さないと 257 本目から待ち行列に入って進まない)。

**出力の 1 行目に条件が出る**ので、数字を残すときはこの行ごと写す。
2 行目の `proxy NN% of one core` で**どちらが律速しているか**を毎回見る (100% = 1 コアを丸ごと。
プロキシに割り当ててあるのは 4 コア = 400%)。

手で回すなら、中身はこれと同じ:

```bash
taskset -c 4-7 ./target/release/rust-http-proxy --lite -p 18080 --bind 127.0.0.1 &
taskset -c 0-3 ./target/release/bench --proxy 127.0.0.1:18080 --conc 8 --seconds 10
```

**ぶれの扱い**: 同じ設定でも 5 秒の計測で ±8% ぶれることがある (2026-09-07 深夜の実測: 50.9 / 55.3 / 58.9 us)。
数 % の差を見るときは **変更前と変更後のバイナリを交互に 3 回ずつ** 10 秒で回し、中央値で比べる。
片方を 3 回続けて測ると、機械の状態の変化 (熱・他プロセス) が差に化ける。

**ビルドの直後 1〜2 本は捨てる** (T10.11)。ビルドの直後に始めた交互 3 回は 1〜2 本目が 48.96 / 49.58 us、
3 本目が 41.25 us で、そのあと続けた 5 回は 40.45〜42.56 us に収まった (forward `release`)。
**ビルドとベンチの間に 1 本捨て走りを入れる。**

**ログ水準を info にすると比べられなくなる** (T10.11)。アクセスログ 1 行は **7.2 us/要求**
(T10.10 の実測。うち kernel 4.9 us は `write(2)` そのもので、stdout に出す限り消えない)。
既定プロファイルはログ info なので、`PROXY_ARGS=""` だけで測ると全経路がそのぶん重くなる。
**比べる表に載せる数字は必ず warn。** 「既定プロファイルの値」を知りたいときは
`PROXY_LOG_LEVEL=info` を明示して、warn の表とは分けて書く。

**`--only connect` のスループットは TIME_WAIT 律速** (T10.11)。この機械は
`tcp_max_tw_buckets = 32768`、`ip_local_port_range` = 32768-60999 (28,231 個)。
CONNECT の計測は 10 秒で 7〜10 万本張るので**毎回バケットを使い切る**
(計測直後の TIME_WAIT は 11 回とも 30,100〜30,900)。プロキシは 4 コアのうち 1.0〜1.3 コア、
ベンチも 1.8〜2.4 コアしか使っておらず、**tunnels/s を決めているのはカーネルの TIME_WAIT の回転**。
11 回で 5,844〜10,203 /s (±25%) と暴れる。**この経路で比べてよいのは CPU/本**
(同じ 11 回で 135.3〜150.4 us、中央値 140.0 からのぶれ ±4%)。

**システムコールの数え方**: `strace -p` はこの環境では `ptrace` が拒まれるので、プロキシを **strace の下で起動する**
(`strace -f -c -o out.txt taskset -c 4-7 target/release/rust-http-proxy --lite ...`)。止めるときは strace ではなく
プロキシ (strace の子プロセス) に SIGINT を送る。回数 ÷ 操作数 が 1 要求 (または 1 接続) あたりの値。
**`strace` の時間表示は使えない** (T10.3: 同じ 10 秒で `-c -w` が 80.6 秒、実 CPU は 8.0 秒)。
**`strace` は回数を数える道具で、時間は `stime` から出す。**

**プロファイル**: `CARGO_PROFILE_RELEASE_STRIP=none CARGO_PROFILE_RELEASE_DEBUG=1 cargo build --release` でシンボルを残し、
`perf record -e cpu-clock -F 4999 -g -p <pid>` → `perf report`。この環境の `perf` は**ユーザー空間しか数えない**
(`perf_event_paranoid = 2`。既定の `cycles` はサンプルが取れないので必ず `-e cpu-clock`)。カーネル側は utime/stime の差で見る。

### デプロイ先に似せた条件 (手元で 250 ms を再現する)

**§1 のここまでの数字はすべて loopback** で、デプロイ先の一番の癖 = **IPv6 が黙って落ちる**が入っていない。
`scripts/deployed-like.sh` が root 無しでその条件を作る (T14.16)。

```bash
scripts/deployed-like.sh -- scripts/cpu-per-request.sh --only connect-multi --seconds 5
scripts/cpu-per-request.sh --deployed-like --only connect-multi --seconds 5   # 同じもの
scripts/deployed-like.sh --memory off --nofile 4096 -- <任意のコマンド>       # 上限は変えられる
```

作るのは `unshare -rmnC` の中の 5 つ: (1) lo だけのネット名前空間 (外へ出られないのでオリジンはベンチの内蔵のものだけ)、
(2) **IPv6 の既定経路を `dev lo`** (出た SYN は戻って捨てられ、`connect` は**約 1.02 秒ハングしてから `ENETUNREACH`** =
デプロイ先の「IPv6 リテラル宛ては約 1.0 秒で 502」と同じ姿)、(3) `/etc/hosts` を「元の内容 + `multi.test` の 2 行」に bind mount
(`2001:db8::1` = 黒穴 / `127.0.0.1` = 生きている → `getaddrinfo` が AAAA と A の 2 候補を返す)、(4) `ulimit -n 1024` と
`systemd-run --user --scope -p MemoryMax=256M`、(5) cgroup 名前空間 + `mount -t cgroup2` (プロキシ自身に 256 MiB の上限を見せる)。

**`blackhole` の経路では再現しない**: この機械では `connect` がその場で `EINVAL` を返す (実測 0.000 秒) ので、プロキシは待たずに
IPv4 へ移り 250 ms が出ない。**「黙って落ちる」ことが要る。**

**`--only connect` は Happy Eyeballs を通らない** (宛先が IP リテラル = 候補 1 つで `crates/net/src/net.rs` の `addrs.len() == 1` の
短絡に入る。T10.1 が「無罪」と結論した理由)。名前宛ての **`--only connect-multi`** は候補が 2 つになるので通り、**各スレッドの 1 本目の
確立時間**とそのあとの p50 / p95 / max を出す。**`--conc` は 3 以上**にすること (T12.1 の「3 連敗で IPv4 を先頭」は同時に走り出した
本数ぶんしか「まだ誰も負けていない」賭けを作らない。2 本目からはホストごとの記憶 `last_win_v6` が効いて IPv6 をそもそも試さない)。
終わりに `/status` の `"ipv6"` を 1 回引いて印字する。cgroup の上限はベンチにも掛かる (太るものを測るときは `--memory off`)。

**この条件の数字は §2 の表には載せない** (§2 は経路どうしを比べる表で、条件が 1 行だけ違う数字を混ぜられない)。名前空間が作れない
機械ではスクリプトが「使えない」と印字して**終了コード 2** (名前空間の外で `--only connect-multi` を回したときも 2)。

### デプロイ先の測り方

**§0〜§2 の数字はすべて手元の loopback**で、デプロイ先 (`nagoya.sorahost.net:50697`、Pterodactyl
コンテナ) で利用者が待っている時間は一度も測っていなかった (2026-09-10 の合議で分かった)。
デプロイ先で測るときの道具は 2 つで、**見ている場所が違う** (T12.0):

| 道具 | 見えるもの | 使うとき |
|---|---|---|
| `scripts/status-diff.py` | **プロキシから見た**ホスト別の待ち (`/status` の `hosts[]`) | 効きを面で見る。24 時間ぶんの差分 |
| `scripts/probe-deployed.sh` | **手元から見た**待ち (`curl -w`) | 1 経路を今すぐ確かめる。1 回 15 本・4 秒 |

**ホスト別統計は差分で見る** (`avg_ms` をそのまま読まない)。`hosts[]` は `.rrd` に永続化されて
**再起動をまたいで通算される**ので、直す前の 257 ms が何日も混ざり続ける。`avg_ms × timed` が
合計 ms なので、2 回取って `(avg2·timed2 − avg1·timed1) / (timed2 − timed1)` がその間の平均になる。

```bash
curl -s http://nagoya.sorahost.net:50697/status > a.json     # 直す前
# ... 24 時間 ...
curl -s http://nagoya.sorahost.net:50697/status > b.json     # 直した後
scripts/status-diff.py a.json b.json --aaaa aaaa.json        # 差分
scripts/status-diff.py b.json --aaaa aaaa.json               # 通算 (1 枚ならそのまま)
```

CONNECT (`connect://`) と forward を別の表にし、**AAAA の有無で 2 群に分けて中央値**を出す
(Happy Eyeballs の 250 ms を払うのは AAAA のあるホストだけなので、この 2 群の差が Phase 12 の主指標)。
**AAAA の判定はリゾルバ次第**なので、数字を残すときは引いた表を `--aaaa`
(`{"host": true/false}` の JSON) で固定する。既定は `getaddrinfo`、`--no-dns` で省ける。
合議のときはこの機械のリゾルバが www.google.com の AAAA を落として「無し」と出た
(同じ日に引き直したら 50 ホスト全部が dns.google の DoH の表と一致した)。
2026-09-10 05:37 UTC の 1 枚からは
**AAAA あり 25 ホスト・要求 6,022 (CONNECT の 74.5%)・avg の中央値 257.4 ms、
なし 22 ホスト・2,063 件・p50 の中央値 5.10 ms (avg 7.10)** が出る (付録 A の「デプロイ先の現在地 (2026-09-10)」の表と同じ)。

**差分の精度は `Δtimed` で決まる。** `/status` の `avg_ms` は小数第 1 位までなので、合計 ms の丸め誤差は
最悪 `0.05 × timed`。これが `Δtimed` で割られる。8 分あけた 2 枚では `Δtimed = 2` に対して
通算 `timed` が 3,930 あり、**誤差は ±196.6 ms** で桁が読めない。`Δavg_ms` の右にこの ± を出すので、
**幅が読みたい桁より小さくなるまで間隔をあける** (デプロイ先は 0.012 req/s = 80 秒に 1 件なので、
主要ホストで数十件貯めるのに数時間〜1 日)。**Phase 12 の受け入れ基準を 24 時間の差分で書くのはこのため。**

**手元からは `probe-deployed.sh`。** `time_appconnect − time_connect` が「CONNECT 確立 + TLS 握手」
(手元からプロキシまでの TCP は `time_connect` に入るので引き算で落ちる)。5 経路 × 3 回 = 15 本を
28 秒の予算つきで回し、予算を超えた行は `skip` と出す。**デプロイ先は利用者の本番なので、
確認に使う要求はこの 1 回分に留める。**

**この環境で 5% 未満の差は見えない** (回線のぶれが ±20 ms、`GET /status` の `time_connect` も
0.032〜0.190 秒と揺れる)。**Phase 12 の受け入れ基準は 5% ではなく桁で書く** (250 ms → 30 ms 未満、のように)。

**落とし穴 2 つ**:

- **forward の行はキャッシュ HIT のことがある。** `http://example.com/` は 3 回とも 0.070〜0.087 秒で、
  AAAA のあるホストなのに 250 ms を払っていない。`curl -D -` で見ると
  `X-Cache: HIT from rust-http-proxy (memory)` / `Age: 6239` で、**そもそも出て行っていない**。
  オリジンまでの待ちを見たいときは `-H 'Cache-Control: no-cache'` を足すか `X-Cache` を確かめる。
- **`/status` は窓が混在している。** `total_requests` は起動から、`hosts[]` は `.rrd` の通算。
  `status-diff.py` の見出しは両方を印字し、`uptime_secs` が減っていたら
  「**再起動をまたいでいる**」と出す (T12.4 (4) で `/status` 側に窓を書く)。

### ビルドのメモリの測り方

```bash
cargo clean && ./scripts/build-memory.sh 120                 # 120 MB の cgroup で通るか (2026-09-16 に 200 → 180、2026-09-26 に 180 → 120)
./scripts/build-memory.sh --find 80 85 90 95 100 110 120     # 通る最小の上限を探す (T14.55 で 135、T15.12 段 6' の後は 120〜125、T17.11 の後は 90。境目は揺れるので 2〜3 回回す)
gh workflow run ci.yml --ref main                             # CI の機械で測る (build-memory-find。T11.7)
```

`.cargo/config.toml` で `jobs = 1` にしてある。既定の並列数だと複数の `rustc` が同時に走り、その合計が上限を超えて
OOM killer に落とされる (実測: 200 MB の cgroup で、並列だと落ち、`-j 1` なら通る)。代償はビルド時間で 8 コアの機械で 14.9 → 32.3 秒。

**RSS の絶対値を上限と比べてはいけない** (T10.9 で理由がはっきりした)。rustc の VmRSS 154〜215 MB のうち
**110〜122 MB は `librustc_driver-*.so` (111 MB) をマップしたファイル由来のページ**で、ページキャッシュに載っていれば
その cgroup には課金されない。実測: **上限 110 MB の cgroup の中で通ったビルドでも `/usr/bin/time -v` の最大 RSS は
215 MB** と出る (メジャーフォールトは全クレート 0 = 1 ページも読みに行っていない)。cgroup が必ず抱えるのは
**RssAnon** の方で、こちらは 36〜86 MB。だからこのスクリプトは **実際にその上限の cgroup の中でビルドして通るか** を見る。
CI は毎回 120 MB で回す (T14.55 まで 200、T17.11 まで 180。ジョブ `build-memory`、ubuntu-24.04)。`scripts/build-memory.sh` が出す参考値も RssAnon 順にしてある (cgroup を作れない機械のときだけ出る)。

**上限を決めているのはどのクレートか** (T10.9)。`RUSTC_WRAPPER` に `/usr/bin/time -v` を噛ませ、
`/proc/<pid>/status` を 20 ms ごとに読んで rustc ごとに測った (3 回とも同じ順、±1 MB):

| クレート | RssAnon | VmRSS | 行 |
|---|---|---|---|
| **proxy-http** | **85.5 MB** | 205.3 MB | 1,992 |
| **proxy-blocklist** | **84.0 MB** | 205.8 MB | 812 |
| rust-http-proxy (本体) | 79.2 MB | 200.5 MB | 1,956 |
| proxy-metrics | 76.1 MB | 196.5 MB | 1,243 |
| proxy-net | 73.6 MB | 193.3 MB | 837 |
| proxy-cache | 67.0 MB | 186.0 MB | 2,257 |
| proxy-diskprobe (最小) | 35.7 MB | 138.7 MB | 514 |

上限を割ったときに rustc が落ちるのもこの上位で、**90 MB では `proxy-blocklist`、95 / 98 MB では `proxy-http`** の
ところで落ちた。**T9.2 が推定した「本体クレートが最大を決めている」は外れ** (本体は 3 番目)。**行数の順でもない**
(いちばん長い `proxy-cache` 2,257 行は 6 番目、`proxy-blocklist` は 812 行で 2 番目)。効くのは
「自分の行数 + 依存から単相化されてくる量」。**通る最小の上限 ≒ いちばん大きいクレートの RssAnon + 14〜20 MB** (T10.9: 85.5 + 14 ≒ 99。T14.55: 115.6 + 20 ≒ 135。T15.12 段 1〜3 の後: 121.7 + 18 ≒ 140。クレートごとの表は T14.55 と T15.12 の `結果:`)。
上限 200 MB に当たるにはいちばん大きいクレートの RssAnon が **2.2 倍 (85.5 → 186 MB)** にならないといけない。
下げたくなったら割るのは `proxy-http` と `proxy-blocklist`。

**手元でも判定できる** (T9.0)。この機械は**システムの systemd が動いていない** (`/run/systemd/system` が無く、
`systemd-run --scope` は "System has not been booted with systemd as init system" と言う。`sudo -n` 自体は通るので、
使えない理由は sudo ではない。T11.7 で確かめた) ためシステムの scope は作れないが、**ユーザーの systemd**
(`systemd-run --user --scope -p MemoryMax=…`) は動いていて上限も効く。スクリプトは
「システムの systemd → ユーザーの systemd → 参考の RSS だけ」の順に落ちる。
**いま通る最小は手元で 99〜100 MB** (T10.9。98 MB は 3 回とも落ち、99 MB と 100 MB は 5 回とも通った)。
**上限 200 MB に対して 100 MB (2.0 倍) の余裕がある。**

**CI で通る最小は 100 MB** (T11.7、run `34187654627`)。**手元の 99 MB とほぼ同じ**で、
**「手元は CI より 10 MB 低い」という長年の記述は実測で否定された**。CI の cgroup は
システムの systemd (`sudo systemd-run --scope`) で作られる (手元はユーザーの systemd)。
測り直したいときは `build-memory-find` ジョブを回す:`.github/workflows/ci.yml` の `build-memory-find` ジョブを
**手で回せば取れる**:

```bash
gh workflow run ci.yml --ref main                                  # 既定の段で測る
gh workflow run ci.yml --ref main -f ladder="95 100 105 110 115"   # 段を指定する
```

月曜 03:17 UTC の週 1 でも回る。結果は run の Summary に出て、同じ Summary に**測った runner の素性**
(image・arch・CPU・メモリ・どの systemd で cgroup を作れたか) も残る。push / PR では回らない
(毎回の関門は今までどおり 200 MB の `Build memory` だけ)。

**手元 (aarch64、ユーザーの systemd、8 コア / 6.6 GiB) と CI (`ubuntu-24.04`、x86_64、4 CPU / 15.6 GiB) は
機械もアーキテクチャも違うのに、通る最小は 99 と 100 MB でほぼ同じ**だった (T11.7)。
上限を決めているのが「いちばん大きいクレート 1 つの RssAnon + 14 MB」(T10.9) で、これは機械に依らない量だから。
古い「CI で 110 MB」は T7.4 (26 クレートに割った直後) の値で、Phase 8〜10 のコードが入っていなかった。

**手元の数字をどう読むか**: 手元の 99 MB は「**手元の機械で** 200 MB の関門にどれだけ余裕があるか」であって、
CI や動作環境 (Pterodactyl コンテナ) の値を代表しない。動作環境で通るかどうかの判定は **CI の 200 MB の関門**が
毎回やっている。手元の値が意味を持つのは「クレートを割る前と後」のような**同じ機械での前後比較**のとき。

**境目は薄い** (98 MB は落ち、99 MB は通る)。**必ず機械を占有して測ること** — 他のビルドが同時に走ると同じ上限でも落ちる
(T10.0 が 110 MB と出したのはこれが原因の可能性が高い)。
**落ちるクレートも回ごとに動く** (T10.9 は 95/98 MB で `proxy-http`、T11.7 の再確認では 98 MB で `proxy-blocklist`。
2 つの RssAnon は 85.5 と 84.0 で 1.5 MB しか違わない)。

## 2. 現在地

**この節の数字は 2026-09-08 に T10.11 が一度に測り直したもの** (rustc 1.96.0、8 コア / 6.6 GiB、loopback)。
道具は `scripts/cpu-per-request.sh` で、**§1 のレシピをそのまま実行すればこの表が出る** (T10.11 で 1 回確かめた)。
**全行が同じ条件**: `--lite` + `PROXY_LOG_LEVEL=warn`、プロキシ cpu4-7 / ベンチ cpu0-3、本文 1 KiB、
10 秒 × 3 回以上の中央値 (`--only tunnel` だけプロキシ cpu4-5 / ベンチ cpu6-7)。
括弧の **(NN%)** は**プロキシが使った 1 コアぶんの割合** (割り当ては 4 コア = 400%)。
これが 100% に遠い行は**プロキシが律速していない** = そのスループットはプロキシの上限ではない。
**配布するバイナリは `dist` (fat LTO) なので両方を載せる**。
「着手前」は 2026-09-07 の Python ベンチ (付録 A)。出典が別のものは行に書いた。

| 項目 | 着手前 | `release` | `dist` (配布用) |
|---|---|---|---|
| forward, 8 並列 | 188 req/s, p50 42.0 ms | **38,619 req/s, p50 0.177 ms** (205 倍、157%) | **39,368 req/s, p50 0.175 ms** (154%) |
| forward, 8 並列 (**既定プロファイル**、warn) | — | **36,586 req/s** — CPU **45.9 us** (T11.9。`--lite` より +7.3%。T11.2 の時点では +25.2% だった) | — |
| forward, 8 並列 (キャッシュ HIT) | — | 76,964 req/s, p50 0.085 ms (169%) | **77,501 req/s, p50 0.085 ms** (165%) |
| forward, 64 並列 | 1,517 req/s, p50 41.9 ms | 31,819 req/s, p50 1.79 ms, p99 6.5 ms (184%) | 31,443 req/s, p50 1.75 ms, p99 7.3 ms (181%) |
| 1 接続 1 要求 (`--no-keepalive`) | — | 14,105 req/s, p50 0.308 ms (145%) | 13,995 req/s, p50 0.307 ms (141%) |
| CONNECT 確立, 8 並列 (**TIME_WAIT 律速**) | 3,506 tunnels/s | 7,951 /s (5,844〜10,203), p50 0.186 ms (111%) | 7,297 /s (6,516〜9,320), p50 0.186 ms (100%) |
| CONNECT トンネル 1 本 (**ベンチ律速**) | 1,044 MiB/s | **3,960 MiB/s** (73%) | 3,950 MiB/s (72%) |
| CPU/要求 (forward / HIT / 64 並列) | — | **41.4 / 22.0 / 58.1 us** | **39.6 / 21.4 / 58.0 us** |
| CPU (1 接続 1 要求 / CONNECT 確立 / トンネル) | — | 103.3 us/接続 / 140.0 us/本 / 185.5 us/MiB | 101.3 / 138.5 / 183.3 |
| トンネル 1 本あたりのスレッド | 3 | **1 (中継中) / 0 (暇なとき)** | 同左 |
| 暇な CONNECT トンネル 5,000 本 | 5,005 スレッド / RSS 93.9 MB | **68 スレッド (山 260) / 29.9 MB** | 68 (260) / 28.7 MB |
| 暇な keep-alive 接続 2,000 本 | 2,004 スレッド / RSS 68.0 MB | **25 スレッド / 25.2 MB** | 28 スレッド / 25.4 MB |
| 1 要求あたりのシステムコール | 10.06 | **5.02** (`recvfrom` 3 + `sendto` 2) | 5.02 |
| 1 接続あたりのシステムコール (`--no-keepalive`) | 33.16 (T4.4 前) | **11.02** | 11.02 |
| CONNECT 1 本あたりのシステムコール | — | **20.08** (T10.0 の 29.06 から -31%。T10.1) | 20.18 |
| 1 要求あたりの確保回数 | 98.7 | **10.0** (T9.5 時点。T9.5 で指標として無効と分かったので測り直していない) | — |
| ビルドが通る最小のメモリ | 347 MB でも通らない | **手元 90 MB** (2026-09-26、T17.11 = T15.12 段 8 の後。90 は 4 回とも通り、85 以下は毎回落ちる。いちばん重いのは `proxy-endpoints` 82.5 MB。関門 120 に対し 30 MB の余裕。CI は未測定。以下は前の記録 →) 手元 120〜125 MB (2026-09-19、T15.12 段 1〜3・5・6' の後。125 は毎回通り、120 は通ったり落ちたりする境目、115 以下は毎回落ちる。いちばん重いのは `proxy-metrics-window` 107.8 MB。関門 180 に対し 55 MB の余裕。段 1〜3 の直後は 140 MB。以下は T14.55 の時点の記録 →) 手元 135 MB (T14.55。130 MB は落ちる。決めているのは `proxy-endpoints` 115.6 MB / `proxy-metrics-core` 110.2 MB / `proxy-net` 108.5 MB。CI は未測定) — 上限 180 MB に対し **45 MB (1.33 倍) の余裕** (T11.7 の時点は 99 MB / 上限 200 で 2.0 倍) | fat LTO なので 350 MB 要る (T9.1、配布は潤沢な環境で作る) |
| バイナリ | 857 KB | 2,498,344 B (T15.0 の波のあと。T14.55 は 2,432,400 B、T11.5 の時点は 1,381,872 B) | **1,777,024 B** (T11.5 の時点は 1,054,184 B) |
| テスト | 150 単体 + 21 結合 | **793 本** (T16.0 のあと、全通過 / 1 ignored。T15.0 の波のあとは 569 単体 + 217 結合 = 786 本。T14.55 時点は 527 + 200 = 727 本、T11.5 時点は 206 + 54 = 260 本) | — |

「暇な keep-alive 接続 2,000 本」の「着手前」列は `PROXY_PARK_IDLE=off PROXY_MAX_THREADS=0` を足して
測ったもの (T11.4)。この行だけ T10.11 のあとに **T11.4 が `--only idle-conns` を足して測り直した**。
**これで §2 の全行が §1 のレシピから出せるようになった** (穴は無い)。

`dist` は `release` に対して forward +1.9% (CPU -4.4%)、HIT +0.7% (CPU -2.6%)、64 並列 -1.2%、
1 接続 1 要求 -0.8% (CPU -1.9%)、トンネル -0.3%、CONNECT 確立 -8% (この経路のぶれ ±25% の中)。
**T10.0 が測った差 (forward +2.3% / HIT +3.9% / CONNECT +7.5%) より小さく、CPU 以外はほぼぶれの中**
(T10.0 の HIT の差はログ水準込みで測っていたぶんが乗っている)。
それでも `dist` は **327 KB 小さい**ので配布は `dist` のまま。
`release` を残しているのは「メモリ 200 MB でビルドが通ること」のためだけ (§0)。

**T10.0 (この日の朝) から動いた行と理由**:

| 行 | T10.0 | いま | 理由 |
|---|---|---|---|
| キャッシュ HIT | 67,269 req/s, CPU 37.3 us | **76,964 req/s, 22.0 us** (-41%) | **T10.0 は HIT だけログ info で測っていた** (T10.11 で揃えた。7.2 us) + T10.2 (-10.4%) + T10.10 (-13.4%) |
| CONNECT 確立 | 165.3 us/本、29.06 syscall/本 | **140.0 us/本、20.08 syscall/本** | T10.1 (`close` 6→2、`pipe2` 2→0、`fcntl` 2→0、`getpeername` 1→0) |
| CONNECT トンネル | 351.6 us/MiB | **185.5 us/MiB** | T10.8 (256 MiB 固定 0.2 秒 → 10 秒、ベンチも big へ)。§2 が T10.8 の値に更新されていなかった |
| 暇なトンネル 5,000 本 | 72.1 MB | **29.9 MB** | T10.5 (生きているスレッドの上限。山が 4,417 → 260 になったぶんスタックが減った) |
| バイナリ (`dist`) | 988,648 B | 1,054,184 B | Phase 10 で足したコード (T10.5 の `Workers`、T10.7 の `/status`、T10.10 のログ) |
| forward / 64 並列 / 1 接続 1 要求 | 42.7 / 62.0 / 102.7 us | 41.4 / 58.1 / 103.3 us | ぶれの中 (この 3 つは Phase 10 で触っていない) |

### 数字の読み方 — 何が律速しているかを毎回見る (T10.8 / T10.11)

`scripts/cpu-per-request.sh` の 2 行目が `proxy NN% of one core | bench NN us/unit (NNN% of one core)`。
プロキシに割り当ててあるのは 4 コア (= 400%) なので、**この値が小さい行のスループットはプロキシの上限ではない**。

| 経路 | プロキシ | ベンチ | 何が律速か |
|---|---|---|---|
| forward 8 並列 | 157% | 349% | ベンチ |
| forward 64 並列 | 184% | 380% | ベンチ |
| キャッシュ HIT | 169% | 371% | ベンチ |
| 1 接続 1 要求 | 145% | 364% | ベンチ |
| CONNECT 確立 | 111% | 224% | **どちらでもない。カーネルの TIME_WAIT** (§1) |
| トンネル 1 本 | 73% | 174% | ベンチ (blaster が 100% で飽和。T10.8) |

**T10.8 の「CONNECT 確立 95% = ここはプロキシ律速」は取り下げる** (T10.11)。95% は
**4 コアのうち 1 コアぶん**という意味で、プロキシは飽和していない。実際に律速しているのは
`tcp_max_tw_buckets = 32768` の回転で、11 回測って tunnels/s は ±25% 暴れるのに CPU/本 は ±4% しか動かない。

### 数字の読み方 — 固定するコアで最大 2 倍変わる (T10.0)

**同じバイナリでも `taskset` の当て方で結果が 2 倍動く。** この機械は big.LITTLE で、
`scripts/cpu-per-request.sh` は**ベンチを LITTLE (cpu0-3)、プロキシを big (cpu4-7)** に固定する。
ベンチ自身の上限 (`--only direct`) は **固定しなければ 263,821 req/s、LITTLE に固定すると 55,112 req/s (1/4.8)**。
そのため **ベンチが律速する経路 (トンネル 1 本・64 並列) は固定すると低く出て**、
**プロキシが律速する経路 (CONNECT 確立・キャッシュ HIT) は固定しないと低く出る**
(固定しないとプロキシのスレッドが LITTLE に落ちるため)。どちらが「正しい」でもないので、
**比べるときは必ず同じ固定の仕方で**。以下はすべて同じ日・同じ `release` バイナリ。

| 経路 | 固定あり (現行の道具) | 固定なし (2026-09-07 までの測り方) |
|---|---|---|
| direct (ベンチの上限) | 55,112 req/s | 263,821 req/s |
| forward 8 並列 | 37,899 req/s | 36,390 req/s |
| forward 64 並列 | 30,916 req/s | 36,921 req/s |
| キャッシュ HIT | 67,269 req/s | 54,005 req/s |
| CONNECT 確立 | 8,883 /s | 4,938 /s |
| トンネル 1 本 | 1,534 (proxy 4-7 / bench 0-3) → **3,956 (proxy 4-5 / bench 6-7)** | 3,916 (スレッドが 3 本しかないので固定なしでも big に乗る) |

**旧 §2 の「トンネル 2,681 MiB/s」は退行ではない** (T10.0 の結論)。2,681 は T4.2 (2026-09-07 朝) に
`taskset` 無しで測った値で、当時のバイナリ (`8b207ef`、923,040 B) を**今日ビルドして今日のベンチで測り直す**と、
固定なしで **1,981 MiB/s**、固定ありで **1,296 MiB/s**。同じ条件の今の `release` は **1,790 / 1,241 MiB/s** で、
差はトンネルのぶれ (±15%) の中。つまり 2,681 → 1,252 の内訳は **固定の仕方 (約 1.5 倍) と機械の状態 (約 1.35 倍)** で、
コードは遅くなっていない。同じ交互 3 回で他の経路も当時と比べたが、いずれも今の方が速い
(forward 8 並列 CPU 52.24 → 42.18 us、CONNECT 確立 235.21 → 169.93 us、HIT 39.83 → 36.85 us)。

同じ理由で、旧 §2 の **64 並列 25,912 req/s (T1.5)・キャッシュ HIT 75,563 req/s (T1.2)・
CONNECT 確立 8,283 /s (T4.3)** も固定なしの値だった。上の表に置き換えてある。

### 完了の定義 (§0 のゴールに対して)

- [x] Phase 0〜1 と T2.1・T2.2・T3.1・T3.2・T3.4 が完了、README の性能節に Rust ベンチの値がある
- [x] forward 8 並列 p50 が 1 ms 台 (loopback) — 実際は **0.18 ms**
- [x] **トンネル 1 本の CPU が 200 us/MiB を切る** — **185.5 us/MiB** (user 11.5 / kernel 174.0。ほぼ全部 `splice`)。
  同じ条件のスループットは **3,960 MiB/s (3.87 GiB/s)** で旧条件「1.5 GiB/s 以上」も満たすが、
  **この MiB/s はベンチ律速**なので完了条件には使わない (プロキシは 1 コアの 73% しか使わず、飽和するのはベンチの blaster)。
  測り方は `scripts/cpu-per-request.sh --only tunnel --conc 1` (プロキシ cpu4-5 / ベンチ cpu6-7、10 秒 × 3 回の中央値、T10.8)。
  旧記述の「固定なしで 2.2 GiB/s / 固定ありで 1.24 GiB/s」は **256 MiB 固定 (0.2 秒) の測り方**の値で、
  立ち上がりと 10 ms 刻みの丸めを含んでいた
- [x] 既定設定で `cargo test --workspace` 全通過 (T9.6 時点で 189 単体 + 48 結合)
- [x] `rust-http-proxy --lite -p 8080` の 1 行で「認証なし・手軽・最速」

古い 5 枚 (2026-09-10 / 2026-09-12 / 2026-09-16 / 2026-09-18 / 2026-09-24) は**付録 A の「デプロイ先の現在地の古い版」**へ 1 文字も変えずに移した
(2026-09-18 の 1 枚は T15.99、2026-09-24 の 1 枚は T17.17 で移した)。本文中の古い版への参照は「付録 A の『デプロイ先の現在地 (2026-09-1X)』」と「(2026-09-24)」を指す (「§2 の」と書いてあっても付録 A)。

### デプロイ先の現在地 (2026-09-26、Phase 16 (T16.0 + `codegen-units = 4`) をデプロイして 47.2 時間)

2026-09-24 12:36:19 UTC に版 `0.1.0+2e57626` (T16.0 の計器、T15.12 段 5〜7 の `codegen-units = 4` とクレートの組み替え) で起動し直した
(利用者が再デプロイ。起動時刻は雪像の `uptime_secs` から起こした)。2026-09-26 11:46:02 UTC (起動から 47.2 時間、`uptime_secs` 169,783) に
`scripts/collect-deployed.sh nagoya.sorahost.net:50697` で雪像を 1 枚取った。出どころは `status/2026-09-26T114602Z-snapshot.json`
(同時刻に `2026-09-26T114602Z-{healthz,profile_res_60,slo,daily,config}.json` も取った。1 枚の要約は `2026-09-26T114602Z-collect.md`)。
**雪像はリポジトリに入れない** (匿名化した写しは `scripts/testdata/deployed-2026-09-26{,-daily,-profile_res_60}.anon.json`。T17.16)。
前 = 付録 A の「デプロイ先の現在地 (2026-09-24)」の「後」の列をそのまま (版 `9f07de4`、平常時 111 標本 5,452 本)、
後 = `$.history["3600"].samples` を起動時刻で切った**バーストの無い時間帯 (1 標本 300 本未満)** 37 標本 2,477 本 (バーストの窓 9 本は外した)。
同じ雪像の履歴で Phase 15 の期間 (2026-09-19 03:00Z → 09-24 12:36Z) を切り直すと 112 標本 5,618 本・確立 avg 22.0 / p50 8.1 / p95 187.7 ms で、前の列と 0.7 ms 以内で合う。
判定は T16.99 の「結果」と、`snapshot-diff.py --criteria phase17` (T17.0a) の 8 行。

| 項目 | 前 (Phase 15 の平常時、126.6 時間) | 後 (Phase 16 の平常時、47.2 時間) | 出どころ |
|---|---|---|---|
| CONNECT 確立 | avg 21.9 / **p50 8.1** / p95 187.0 ms | avg 19.0 / **p50 7.8** / p95 159.9 ms | `$.history["3600"].samples` の `connect_buckets` を段ごとに足して線形補間 |
| 利用者が待つ `wait` (基準線) | **p50 約 8.2 / p95 約 189 ms** | avg 25.0 / **p50 7.9 / p95 167.9 ms**。参考: 直近 1,024 本 (14.3 時間ぶん) は p50 7.8 / p90 16.0 / p99 269 ms | `$.history["3600"].samples` の `wait` / `$.status.recent_quantiles.wait` |
| 名前解決のミス | **0.05 回/接続**、ミス 1 回 33.2 ms、接続 1 本あたり 1.6 ms | **0.06 回/接続** (2,477 接続 / 141 ミス)、ミス 1 回 **106.8 ms**、接続 1 本あたり **6.1 ms**。日ごと (この版の 2 日) は 2 日とも 0.001 (どちらもバーストの日) | 同上 (`dns_misses` / `connects` / `dns_ms_sum`)、`/daily` の `dns_per_connect` |
| 主要 3 ホストのミス率 | `discord.com` 0.01 (1,600 / 10)、`http-intake.logs.us5.datadoghq.com` 0.00 (4,330 / 17)、`mtalk.google.com` 0.01 (381 / 3) | **`discord.com` 0.02** (355 要求 / 8 ミス)、`http-intake.logs.us5.datadoghq.com` 0.00 (2,250 / 1)、`mtalk.google.com` はこの間に要求 0 | 前後 2 枚の `$.hosts.hosts` の差分 (後の窓は再起動前の 3.0 時間を含む) |
| ミスの種類 (起動から) | `cold` 229 : `expired` 238 : `warm_stale` 6 : `negative` 0 | `cold` 101 : `expired` 119 : `warm_stale` 10 : `negative` 0 (= 0.44 : 0.52 : 0.04 : 0)。`refresh_late` 0 / `refresh_failures` 0 | `$.status.dns.misses_by_kind` |
| 裏の引き直し | 通算 **359 回/時**、名前ごとの最大 78.2 回/時、`warm` 9 件 | 通算 20,586 回 = **436 回/時**、名前ごとの最大 **80.0 回/時** (`http-intake.logs.us5.datadoghq.com` 3,771 回。閾 80 = TTL 60 秒の 3/4 おきの物理上の上限にちょうど)、`warm` 9 件 | `$.status.dns.refreshes` ÷ `uptime_secs`、`$.dns.entries` |
| warm の枠 (T16.0) | (欄が無い。60 秒の最大 9、1 時間平均の最大 21) | `dns_warm_max` **15** (5 秒 / 60 秒 / 1 時間のどの窓でも、上限 32)、`warm_evicted` **0**。1 時間 23,404 本のバーストでも warm の最大は 6 | `$.history["3600"]` の `dns_warm_max`、`$.status.dns.warm_evicted` |
| CPU (コンテナ) | 絞り 0 / 889,600 周期、プロセス全体 0.0014 コア | **絞り 0 / 412,474 周期** (起動から)、プロセス全体 **0.0013 コア** (`/profile?res=60` の 308 標本 = 5.1 時間)。cgroup の起動からの通算 374.1 秒 = **0.0022 コア** (ユーザー空間 115.3 秒 = **31%**、カーネル 258.8 秒。前の版には欄が無い)。`/healthz` の `checks.cpu` ok (`throttled_5m` 0) | `$.status.kernel.cgroup_cpu.since_start`、`/profile?res=60`、`/healthz` |
| conn 役の CPU/要求 | 738 us/要求 (`/profile?res=60` の 5.5 時間、要求 949 本) | 1,038 us/要求 (要求 366 本) = 1.41 倍。材料は 10 ms 刻みの数十回ぶんしか無く、MiB あたりでは向きが逆 (T16.99 の (i))。**桁の悪化は無い** | `snapshot-diff.py --criteria phase17 --profile … --profile-before …` |
| 確立の尾 | 平常時の p90: `wait` 約 120 / `connect` 約 117 ms。100〜250 ms の区間に 10.7% | 平常時の p90: `wait` **20.6** / `connect` **14.1 ms** (差 6.5 ms、平均の差 6.0 ms は名前解決の 6.1 ms/接続)。100〜250 ms の区間に 4.8%、250〜500 ms に 2.1%。`run_delay_us` の conn 役は 5.1 時間で合計 39 ms | `$.history["3600"]` の `wait` / `connect`、`/profile?res=60` の `run_delay_us` |
| `/events` の anomaly (起動から) | `dns_slow` 0.09 件/時、`connect_p95` 0.01 件/時 | **`dns_slow` 29 件 = 0.61 件/時**、`connect_p95` 7 件 = 0.15 件/時 (`cleared:` 36 件は数えない)。`cpu_throttled` / `tunnel_spin` 0 件 | `$.events` (`snapshot-summary.py` の件/時の表、T17.0c) |
| エラー | **0 件** (`timeout` 0.00 件/時) | **2 件** (平常時 1、どちらも原因 `dns` の 502 = 宛先の名前が引けない)。`timeout` **0 件**。connect の最大は平常時 1,000 ms・バースト込み 4,372 ms で、既定の 10 秒に一度も当たらず | `$.history["3600"]` の `errors_by_cause`、`$.errors.errors` |
| バースト | 窓 14 本、山 83 / 上限 240 | 窓 **9 本** (1 標本 300 本以上。バースト込みの 107,386 本が要求のほぼ全部)、山 **67** / 上限 240、`rejected_overload` 0 / `evicted_idle` 0 | `$.history["3600"]` の `active_max`、`$.status` |
| RSS | 47.0 MB | **54.2 MB** (54,231,040 B = 51.7 MiB)。増えたのは `heap_free` 11.9 → 21.2 MB (解放したあとも持っているヒープ) で、`heap_used` 9.9 → 10.1 MB・`mmap` 14.0 → 14.4 MB・`rings_used.total` 13.25 → 13.53 MB (≤ `rings.total` 15.02 MB) はほぼ同じ | `$.status.memory` |
| canary | 失敗 0 / 7,472 回 | 失敗 **0 / 2,780 回**。IPv6 側は `null` のまま | `$.status.canary` |
| 状態ファイル | `.rrd` 8,388,608 B・版 3・書出し 7,592 回・エラー 0。`.recent` 記録 91,016 件・`dropped` 9,598 | `.rrd` 8,388,608 B・版 3・書出し 2,829 回・エラー 0。`.recent` 記録 114,035 件・`dropped` 355 (枠が一巡して古い方から落としたぶん)・エラー 0 | `$.status.state_file` |
| 接続元 | ほぼ全部が 1 つ (100,596 要求、3.0 GiB)、ほかに 3 つ | ほぼ全部が 1 つ (差分 118,112 要求、3.1 GiB)。**起動より後に新しく現れた接続元は 0**、IP リテラル宛て 0 本・443 / 80 以外の CONNECT 0 本。ほかは再起動前の 3 時間に 9 要求の 1 つだけ。`/readers` にだけ居る接続元 (プロキシを使わず `GET /` だけを引いた走査とみられる) が 3 つ (2026-09-25) | `$.clients.clients` の差分、`snapshot-summary.py` の接続元の見張り (T17.0c) |
| ディスク | 3.1% | **3.9%** | `$.status.cache.system.disk_used_percent` |
| 版 | `0.1.0+9f07de4` | `0.1.0+2e57626` | `$.status.version` |

**読み方**:

- **Phase 16 の完了の定義 3 つは満たした** (T16.99 の「結果」)。(i) `codegen-units = 4` で桁の悪化は無い (プロセス全体 0.0014 → 0.0013 コア、絞り 0、
  `cpu_throttled` / `tunnel_spin` 0 件)。(ii) `MAX_WARM` 32 の取り合いは無い (`dns_warm_max` 15、`warm_evicted` 0)。(iii) 引き直しは名前ごとに 80.0 回/時で閾ちょうど
  (ずっと warm な名前は物理的に 80 回/時に張り付き、超えることは無い)。**`codegen-units = 4` はそのまま**。
- **phase17 の判定表は満たした 5 行・届かず 2 行・判定できず 1 行**。これは Phase 17 の完了の定義の物差しで、Phase 16 の判定ではない。届かずの 2 行は
  conn 役の CPU/要求 (1.41 倍。上の表のとおり材料が少なく読めない) と `dns_slow` 0.61 件/時 (閾 0.1 → T17.1)。判定できずは cgroup の起動からの CPU
  (前の版に `usage_usec` が無い。次の比較からは両側にある)。
- **名前解決はミスの率が 0.05 → 0.06 回/接続で変わらず、ミス 1 回が 33.2 → 106.8 ms に上がって、接続 1 本あたり 1.6 → 6.1 ms**。主要な相手のミスは
  warm でほぼ消え (`discord.com` 8 回・1 回 8.9 ms)、残ったのは**たまにしか引かない名前**の高いミス (T16.99 の気づき。T15.99 の「安いミスが消えて高い尻尾が残った」と同じ理由)。
  平常時の `wait` と `connect` の平均の差 6.0 ms はこのぶんで、`dns_slow` が 1 回のミスで立つようになったのも同じ理由 → T17.1。
- **p95 が 187 → 160 ms、p90 が 117 → 14 ms に下がったのはプロキシではなく行き先の構成**。平常時に遠い相手へ行く本数が減った (100〜250 ms の区間 10.7% → 4.8%) ためで、
  遠い相手への CONNECT はほとんどバーストの中にある。p50 は 8.1 → 7.8 ms で 1 RTT のまま (付録 A の 2026-09-18 の読み)。尾は相手までの距離のせいでコードでは縮まない (§4 の最終行)。
- **RSS 47.0 → 54.2 MB はリークではない**。増えたのは `heap_free` だけで、使っているヒープ・`mmap`・記録のリングはほぼ同じ (§4 の `malloc_trim` の項 → T17.8)。
- **物差しの但し書き**: `recent` / `hosts` / `profile` の部は 256 KiB で切れている (`hosts` 634 / 1,000 件、`profile` 408 / 720 件)。隣の `-profile_res_60.json` も
  この雪像では切れている (308 標本 = 5.1 時間。`offset=` で繋ぐ T17.0b の前に取ったため)。`/daily` のこの版の日は 2 日ともバーストの日で、日ごとのミスの幅は
  平常時の率としては読めない。

## 3. やったこと

Phase 0〜7 の「やったこと」は [`docs/HISTORY.md`](docs/HISTORY.md) に移した (2026-09-26、T17.18。本文は 1 文字も変えていない)。

## 4. 測って採らなかったもの

同じ道を二度調べないための記録。**すべて実装して測ったうえで戻した / 入れなかった**もの。

| 案 | 測った結果 | 判断 |
|---|---|---|
| `epoll` リアクターでトンネルを多重化 (T2.3) | 5,000 本のアイドルトンネルで RSS 198 MiB、新規 CONNECT p99 9.1 ms | 受け入れ基準 (200 MiB / 10 ms) を満たすので作らない |
| `SO_REUSEPORT` で待ち受けを複数に (T4.3) | accept スレッドの CPU は 1 コアの 45%、並列を上げるとむしろ下がる | accept は律速していない |
| `opt-level = 3` (T4.2、当時) | +2.2%、バイナリ +131 KB | 5% に届かないので見送り (T7.1 で LTO を切ったときに採用) |
| musl 静的リンク (T3.3) | ビルドがメモリ 200 MB に収まらない。`dlopen(libssl)` も使えず HTTPS オリジンのキャッシュが死ぬ | 採らない |
| 統計 `Mutex` の分割 | 統計を丸ごと消したビルドでも `futex` 0.010 vs 0.011 回/要求、p99・CPU とも差なし | 期待効果 0〜0.25% |
| 統計の鍵を 16 分割する (T14.45、`/profile` の `locks` で測り直し) | 既定プロファイルの forward 8 並列 10 秒 × 3 組で、ホスト表の鍵の取り合いは **0.869 / 0.645 / 0.784%** (基準 1%)。接続元の表の鍵 (計測用に 5 本目の counter を足した版) は **0.042 / 0.042 / 0.057%**。`dns` 0、`park` 1〜14、`workers` 0〜2 | **分割しない**。1% に届かず、待たされた 2,564 回に futex 1 us (T9.4) を掛けても **2.6 ms = CPU 16.5 秒の 0.016%**。鍵の内側は約 0.25 us と逆算でき、丸ごと消しても 0.54%。手元のベンチはホストも接続元も鍵が 1 つなので 16 に割っても同じ枠に入り判定できない。`hosts` の鍵は `total` / `interval` / `stages` / `quantiles` / `series` も守っていて分けられない |
| `recvfrom` を 3 → 2 回に (プールの生存確認の省略) | — | プールはクライアント間で共有なので、desync すると**他人の応答**が渡り、誤った URL のキャッシュとして固定化する。期待値 1% のために踏む橋ではない |
| クライアント書き込みバッファのプール化 | アイドル接続あたり 30.3 → 30.9 kB、CPU 43.3 → 43.9 us | 改善なし。アイドル接続の資源はスレッドスタックが主因だった |
| 猶予を `poll(2)` で待つ (T6.4) | `ppoll` が 1 回/要求 増えて CPU +5.5% | 読み取りタイムアウトに畳めば 0 回 (T6.5) |
| `PROXY_PARK_MAX_GRACE` (猶予待ちスレッドの上限) | — | T6.5 で猶予が「普通に次の要求を読んでいる状態」になり、数えて止める意味が消えた |
| 期限切れの close をワーカーに投げる | — | 受け渡し (Mutex + channel + スレッド起床) が隠したい `close` と同じ桁。空きスレッド上限 64 を越えると新規スレッドを起こす |
| 要求ごとに行の置き場を借りて返す (T6.2) | +1.5% | 接続あたり 1 回だけ借りる |
| クレート分割後に `#[inline]` を当てる (T7.1) | 41.7 us (当てる前 41.4 us) | 効かない。LTO を切った損は個々の小さい関数ではなかった。`opt-level = 3` の方が効いた (41.4 → 39.7 us) |
| クレート分割で `lto = "thin"` | ビルド最大 334 MB、CPU 39.4 us | 200 MB に届かない。`lto = false` (194 MB / 39.7 us) を採る (7 クレート・並列ビルドのときの値。26 クレート・`jobs = 1` で測り直したのが下の 2 行) |
| `lto = "thin"` (26 クレート、`jobs = 1`) | 通る最小 **170 MB** (`false` は 100 MB)、ビルド 38.7 → 55.4 秒、バイナリ 1,316,336 → 1,316,328 B (**8 バイト**)、CPU/要求 forward -0.2% / connect +0.7% / HIT -2.5% | **払うものだけあって得るものが無い**。速さはすべてぶれ (±8%) の中で、バイナリも実質同じ。CI では +10 MB (≈180 MB) になり上限 200 MB の余裕が 20 MB しか残らない (T9.1) |
| `lto = "fat"` (26 クレート、`jobs = 1`) | 通る最小 **350 MB** (300 MB では通らない)、ビルド 48.8 秒、バイナリ -128 KB、CPU/要求 forward -4.5% / connect -2.6% / HIT -9.3% | 効くが上限 200 MB の **1.75 倍**。LTO の重さは最終リンク 1 回にかかるので、クレートを 26 に割っても下がらなかった。配布用の `dist` プロファイルだけで使う (T9.1) |
| 冷たい 9 クレートを `opt-level = "s"` に (T9.2) | バイナリ **1,316,336 B のまま** (1 バイトも変わらない。`.text` は -17,952 B だが例外表が +6,660 B、セクション合計 -10,940 B は 64 KiB のセグメント境界の詰め物に吸われる)、`dist` も 988,648 B のまま、通る最小のメモリ 100 MB のまま、クリーンビルド 37.6 / 36.3 → 36.0 / 36.1 秒、CPU/要求 forward +2.3% / connect -4.4% / HIT -0.2% | **払うものは無いが得るものも無い**。冷たい層は合計しても `.text` の 2% しかなく、減った分はページ境界の詰め物に消える。9 行の override を Cargo.toml に置く (クレートが増えるたびにどちらの層か決める) 分だけ損 (T9.2) |
| accept したスレッドがそのまま接続を処理する (leader/follower、T9.4) | `futex` 2.02 → **0.02 回/接続**、システムコール 11.05 → 9.04 回/接続。だが 1 接続 1 要求の CPU/接続 114.99 → 113.04 us (**-1.7%**、目標 -5%)、p50 0.321 → 0.319 ms で**動かない**。keep-alive 48.56 → 50.14 us (**+3.3%**)、CONNECT 確立 176.25 → 187.53 us (**+6.4%**)、忙しいときのスレッド 12 → 18 / 68 → 131 | **受け渡しの `futex` は遅さの原因ではなかった**。この機械では 1 回 1 us なので 2 回消しても上限 -2%。しかも長生きする仕事 (keep-alive の接続・トンネル) を accept したスレッドが抱えるので群れが上限 64 まで育ち、そこから先は結局ワーカーへ渡す = 両方の費用を払う (実装 `f7eb35e`、差し戻し `42889d8`) |
| クライアント書き込みバッファ (64 KiB) を `CopyBuf` と同じくプールする (T9.5) | user CPU 12.15 → 11.65 us (**-4.1%**、交互 3 組とも同じ向き)、全体 43.57 → 42.72 us (-2.0%)、確保 10.0 → 9.0 回/要求 | **5% (user で 0.8 us) に届かない**。`BufWriter` を手書きの版で置き換える (flush・`Drop`・容量超えの直書きの分岐) 保守と誤りのリスクに見合わない。**tcache に載らない唯一の確保なので、小さい確保と違って効きはする** (段 3 と対照的)。やり方は「`crates/http` に `ClientOut` を足し、`let client = &mut BufWriter::with_capacity(...)` を差し替える」だけ。次のラウンドで採るなら判断材料はこれで足りる |
| forward のカーネル 30.6 us/要求 を削る (T10.3) | 5 回のシステムコールの実費を単独で測ると合計 25.8 us (寝起き 19.1 + システムコールの本体 6.5 + 生存確認 0.2)、残り 4.4 us は 8 接続を 4 コアで捌く取り合い。conc=1 の実測 27.1 us は模型と +5% で合う | **これが床**。出入りだけなら 1 回 0.23 us なので、システムコールを束ねる仕掛け (io_uring 等) の上限は **-1.1 us (-2.7%)**。63% は要求と応答を 1 回ずつ待って起きる代金で、寝ないようにすると CPU/要求 は増える。うち 9.8 us は cpu4-7/cpu0-3 をまたぐぶんで測り方の値段 |
| プールの生存確認 `recvfrom` を条件付きにする (T10.4) | 空振りの実費は **0.32 us (カーネル 0.20)** で、forward の CPU/要求 の **0.5%**。回数では 5 回中 1 回 (20%) だが時間では 0.7% | **回数と時間は別物だった**。受け入れ基準の -5% は上限 0.5% で原理的に不可能。§4 が警告する desync の危険 (他人の応答が渡り誤ったキャッシュとして固定化) を 0.5% のために踏む理由がない。着手せず取り下げ |
| 保存判定に応答ヘッダーの組を渡すのをやめる (T11.2) | `freshness::response_policy` を借用のヘッダー行のまま判断する形にし、`headers::response_pairs` の `Vec<(String,String)>` を作らない。確保 19.25 → **11.25 回/要求** (-8.00)、既定プロファイルの forward CPU/要求 は 8 並列 51.95 → 51.74 (**-0.4%**)、1 並列 42.31 → 41.58 (-1.7%) | **5% に届かない (ぶれの中)**。T9.5 の段 3 (確保 -7 回で CPU が 1 ミリも動かない) を既定プロファイルで再現した形。減らしたのは 4〜192 B の確保で、glibc の tcache から出て戻るだけ。**既定プロファイルが `--lite` より 10.3 us 重い理由は確保ではなく合流の待ち** (T11.2 → T11.9) |
| `connect_timeout` を自前実装して `ioctl` を減らす (T11.1) | `ioctl` 4.00 → **1.00 回/本**、システムコール 20.12 → 18.08 回/本 (-10.1%)。だが CONNECT 確立の CPU/本 は 138.83 → 140.20 us (**+1.0%**、前後交互 6 組の中央値。生の値は 142.26/146.16、139.06/136.11、137.30/139.95、139.52/140.45、135.16/144.32、138.59/139.77 で向きが揃わない)。forward +0.5%、1 接続 1 要求 +3.4% | **回数は減るが時間は減らない**。T10.1 の「1 回 ≒ 2.4 us」は消す種類に依る見積もりで、`pipe2`/`fcntl`/`close` (カーネル側の対象を作って壊す) には当てはまるが、`ioctl(FIONBIO)` は `struct file` のフラグを 1 つ立てるだけ。240 行の `unsafe extern "C"` (sockaddr のレイアウト・arch ごとの定数・先読みぶんを書き切る `poll` ループ) を抱える理由が無い。実装 `d466ace`、差し戻し `3c78ce3` |
| **確立 p50 を 6 ms 以下にする (T14.1 の目標、T14.99 で判定)** | デプロイ先の平常時の確立 p50 は 8.2 → **8.4 ms** で動かず。確立 − カーネル RTT の中央値は **−0.21 ms** (個票 614 本) で**プロキシの取り分は中央値で 0**。窓に出る宛先で最良 RTT が 6 ms 未満のものは **0 本** (`rtt_ms.origin` の最小 6.72 ms、要求で重み付けた最良 RTT の中央値 6.59 ms)。名前解決を 6.4 → 2.8 ms/接続 に減らしても p50 は +0.15 ms しか動かない (払う接続が 0.144 回/接続 = 少数派なので中央値は定義上動かない) | **物理なので追わない**。中央値の接続は既にオリジンまでの 1 RTT で、コードで削れる余地は無い。1 RTT を下回るには要求が来る前に繋いでおく (先張り) しかなく、それは「利用者が頼んでいない接続を張る」設計判断。**次の Phase の基準は「6 ms」のような固定値ではなく「そのときのホストの RTT + N ms」にする**。なお 6 ms の根拠 (T14.1 の「名前解決 6.3 ms/接続 = p50 の床のうち動かせる唯一の部分」) は平均の引き算を中央値に当てたもので、しかも判定が見ている確立の時計に名前解決は 1 ms も入っていなかった (T15.0 (1)) |
| Happy Eyeballs の負けた試行を 1 スレッドにまとめる (T12.2) | T12.1 のあと、黒穴 `[::1]` + 生きた `127.0.0.1` へ 100 本 (締め切り 2 秒) で残るのは **3 スレッド / 3 fd、1.7 秒で消える** (同じホストなら 0、600 秒ごとの探りは 1)。残る時間は締め切りそのもの (2 秒 → 1.8、4 秒 → 3.8) | **10 本未満なので採らない**。`d466ace` (240 行の `unsafe`、T11.1 で CPU +1.0% として戻したもの) を資源のために復活させる理由が無い。T12.1 が「先頭が勝つ」を作った時点で、負けた試行は起動から 3 本と 600 秒に 1 本しか生まれない |
| **確立の尾 (平常時 p95 約 187〜189 ms) を縮める (T15.99 で正体を測った)** | デプロイ先の 126.6 時間 (`2026-09-24T093400Z-snapshot.json`) の平常時で、p90 は `wait` 約 120 / `connect` 約 117 ms、差は約 3 ms (平均の差 1.6 ms は名前解決)。尾は**遠い相手の connect** (100〜250 ms の区間に 10.7%)。`/profile?res=60` の `run_delay_us` は conn 役で 5.45 時間に合計 72 ms、CPU の絞り 0 / 889,600 周期 | **相手までの距離のせいで、コードでは縮まない** (機械のせいでもコードのせいでもない)。次の基準線は平常時の `wait` p50 約 8.2 / p95 約 189 ms (T15.99 の 6) |
| `promote` から `idle < window` を外す (T17.5、`scripts/dns-replay.py` の模型で 09-26 の雪像 6.53 時間・到着 558 件を再生) | いまの規則の模型は実機に ±30% で合う (ミス 0.075 対 0.063 回/到着、引き直し 481 対 436 回/時)。外すとミス 42 → 37 (**−11.9%**)、引き直し 3,143 → 3,613 (**+15.0%**)。前から居た名前を置かない下限側は −2.4% / +11.3% | 目安「ミス −15% 以上かつ引き直し +10% 以内」に両方届かない。採らない (T15.4 の前の再生の −21% は窓 3,600 秒でほぼ食われていた) |

## 5. これから (未着手)

上と同じ形式。**着手する前に必ず「今どうなっているか」を測る**こと。

### 実装を Opus に渡すときの決まり

**2026-09-18 の注**: Phase 14 で固まった運用 (worktree を自分で作る / lint と自分のテストだけ / 全体テストは波の最後に 1 回 /
`mx` 経由 / `TODO.md` は親だけ / 自分の PID だけ止める) は **Phase 15 の「渡し方」にまとめた**。下の「直列に回す」と
「エージェントが守ること」の 2. (タスクごとに全部通す) は古く、食い違う所は Phase 15 の方が正。1.・3.・4.・5.・7. は今も生きている。

ここから下のタスクは、1 つずつ Opus (Claude Code のサブエージェント) に渡して実装する前提で書いてある。
渡すときの指示はこの節と各タスクの本文だけで足りるようにしてある (別のメモは要らない)。

**渡し方**: 1 タスク = 1 エージェント = 1 コミット。直列に回す (ベンチが重なると計測が汚れる。`TODO.md` の取り合いにもなる)。
指示文には「最初に読むもの」として §0・§1・§4 とそのタスクの本文、触るクレートのソース全体を挙げ、
下の「エージェントが守ること」を貼る。おすすめの順は **T9.3 → T9.4 → T8.1 → T9.5 → T8.5 → T8.6**
(効きの大きさと、後のタスクが前の結果を使う順。T9.4 は T9.3 の `SO_RCVTIMEO` を、T8.5 は T8.1 の結果を使う)。

**エージェントが守ること** (指示文にそのまま貼る):

1. 外部クレートは 1 つも足さない。システムコールは `crates/sys/src/sys.rs` と同じ `unsafe extern "C"` + `#[cfg(target_os = "linux")]`、
   他 OS には従来コードを残す。認証は入れない。既存機能を壊さない。
2. `cargo fmt` → `cargo clippy --workspace --all-targets -- -D warnings` → `cargo test --workspace` → `cargo build --release` を通してからコミット。
   `.cargo/config.toml` の `jobs = 1` はそのまま。
3. 性能に関わる変更は、**変更前のバイナリを退避して、変更後と交互に 3 回ずつ 10 秒** (`scripts/cpu-per-request.sh`、`BIN=` で切り替え) 測り、
   **ビルドとベンチの間には捨て走りを 1 本入れる** (T10.11。ビルド直後の 1〜2 本は 15〜20% 重く出る)。
   中央値で比べる。差が 5% 未満なら「ぶれの中」と正直に書く。システムコールは strace の下でプロキシを起動して数える (§1)。
   計測中は他のビルド・ベンチを走らせない。終わったらプロキシとベンチのプロセスを残さない (`pgrep -x rust-http-proxy` が空)。
   **A/B 用に退避するバイナリは `rust-http-proxy` という名前のまま別ディレクトリに置くこと** (T11.1)。
   `mx` の待ちループも `strace` の停止も `pgrep -x rust-http-proxy` を使うので、名前を変えると
   止められずにロックを占有し続ける (実際に 20 分止まった)。
4. 効かなかったら**戻す**。戻した案は §4 の表に数字つきで 1 行足す (これも 1 コミット)。
5. コミットメッセージは既存の形式 (`perf:` / `feat:` / `fix:` / `docs:` / `chore:` / `refactor:` / `test:` + 日本語 1 行 + 本文に前後の表)。
   `Co-Authored-By:` にモデル名を付ける。**`git push` はしない** (main にもブランチにも。CI で確かめたいときは人が push する)。
6. 終わったらそのタスクを `[x]` にして `結果:` に実測値とコミットを書く。§2 の表に効く数字が変わったら更新する。
   環境変数や挙動を足したら README の表と説明も更新する。
7. 最後の報告に、コミットハッシュ・前後の表・テストの本数・やり残しと気づいた問題を入れる。

**この機械で分かっている計測の癖** (エージェントに伝える): `strace -p` は `ptrace` が拒まれる (strace の下で起動する)。
`perf` は `perf_event_paranoid = 2` でユーザー空間しか数えず、既定の `cycles` はサンプルがほぼ取れないので `-e cpu-clock` を使う。
CPU/要求 は 5 秒の計測で ±8% ぶれる。ビルドの通る最小メモリは境目が 1 MB 刻みで薄いので、機械を占有して測る (T10.9)。手元 99 MB / CI 100 MB でほぼ同じ (T11.7)。

**Phase 8〜14 と Phase 15 の済んだタスクの本文は [`docs/HISTORY.md`](docs/HISTORY.md) に移した** (2026-09-26、T17.18。そのまま移したので、コメントの `TODO.md T14.xx` はそこで引ける)。

### 既知の小物 (Phase 12〜17 で見つけたもの。旧 Phase 14 の表)

| 既知の小物 | 出どころ | 何をするか |
|---|---|---|
| keep-alive の HTTP 接続が `/connections` でバイト数を出さない (宛先は T14.2 (5) / T14.48 で済。`set_bytes` を呼ぶのはトンネルだけ) | T13.4、T17.20 の気づき | 要求ごとに書かない方針とどう両立させるかを先に決める |
| `dashboard.html` が上限まで余り 247 B (81,673 / 81,920 B) | T15.0 単位 9 と全体チェック | 次にカードを足す前に、`scripts/check-dashboard.js` の上限を上げるか、古いカードの説明文を削る |
| `proxy-base` の `log::tests::the_ring_clips_long_lines_and_wraps_at_1000` が稀に落ちる (`crates/base/src/log.rs:725` の `total == MAX_LOG_LINES + 5`。2026-09-18 の全体テストで 1 回、同じ binary の回し直しは 5 / 5 通過) | T15.5 のマージ後の全体チェック | リングとログ水準を触るテスト 4 本は全部 `RING_TEST_LOCK` を取っているので、**鍵を取らずに warn 以上を書く (か水準を変える) 別のテストが同じ binary にいる**はず。落ちたときの `left` / `right` を控えて (多いのか少ないのか)、犯人のテストに同じ鍵を取らせる |
| `a_probe_measures_a_real_connect_and_a_refused_one` (`proxy-metrics-watch`) が混んだ機械で稀に落ちる (`dns_ms == 0` の判定。IP リテラルの名前解決が 1 ms に丸め上がる。main でも同じ) | T17.7 | 0 ではなく「数 ms 以下」で見るか、IP リテラルでは測らない |
| `happy_eyeballs_skips_unreachable_first_candidate` (`proxy-net-conn`) の「2 回目 < 50 ms」が混んだ機械で稀に落ちる | T17.6 | 時間ではなく試行の順で見る |
| `tests/proxy_test.rs` の `test_integration_request_body_on_a_reused_connection` が全体テストの負荷の下で 1 回 502 (単独 5 / 5 通過) | T17.11 の全体テスト | 原因を先に見る (再利用した接続の生存確認と本文の送り直し) |
| `scripts/mx` の TERM の trap を張るのが命令の起動後 (隙間の TERM で子が残る)、`others()` の子孫除外が `CHILD` 未設定の時点で呼ばれて効いていない | T17.15 | trap を起動前に張る。子孫除外は手順 3 の後で数えるか外す (動きが変わるので単独のタスクで) |
| `warm_promote` が次の予定を「上げた時刻 + 45 秒」に置く (答えを引いた時刻ではない。齢 60〜75 秒が期限切れになりうる。実機の `warm_stale` の一部かも) | T17.5 の模型 | 予定を答えの齢から出す。`scripts/dns-replay.py` で先に数える |
| T17.11 で移した `slo.rs` の `[crate::anomaly::check]`・`profile.rs` の `[crate::history…]` の doc リンクが新しいクレートから解決しない (rustdoc の警告だけ) | T17.11 | 次にそのファイルを触るとき、上のクレートの道に直す |

### Phase 15 (2026-09-18 に組み替えた: まず載せる、それから決める)

**Phase 15 の渡し方 (2026-09-18 版。§5 冒頭の「実装を Opus に渡すときの決まり」より新しく、食い違う所はこちらが正)**:
Phase 14 で固まった運用を 1 つの型にした。親は下の型を指示文に貼り、`<…>` を埋める。**1 単位 (または 1 タスク) = 1 エージェント = 1 worktree**。

1. **作業場所**: 最初に worktree を自分で作る — `git -C /home/takano32/GitHub/rust-http-proxy worktree add -b <波の名前>/<id> /home/takano32/GitHub/rust-http-proxy-wt/<id> main`
   (波の名前は親が指示文に書く。T15.0 の波は `wave8`、T15.4 / T15.6 は `wave9`)。
   以後はそこだけで作業し、main のチェックアウトは触らない。`git push` はしない。**`TODO.md` は編集しない** (`結果:` の文面は最後の報告に書き、親が貼る)。
   デプロイ先 (`nagoya.sorahost.net`)、`~/rust-http-proxy-status/`、本物の `~/.rust-http-proxy.*` には触らない。
2. **最初に読むもの**: `TODO.md` の §0、§1 の「速さの測り方」、この「渡し方」、T15.0 の「決まり」と「欄の名前」、自分の単位の本文。
   本文が挙げるソースは**関数の全体と呼び出し元**まで読む。**本文の行番号は書いた時点の main のもの** (T15.0 の本文は `71d1d0b`、T15.4 / T15.6 / T15.99 は T15.0 のあとの `5f6523b` で確かめ直した。T15.12 は着手の前にもう 1 回確かめる) なので、ずれていたら近くを
   `grep` して確かめてから触る。本文と違う事実を見つけたら、直す前に「どこがどう違うか」を控えて報告に書く。
3. **確かめ**: `cargo fmt --all` → 触ったクレートだけ `cargo clippy -p <crate> --all-targets -- -D warnings` (結合テストを足したら
   `-p rust-http-proxy --all-targets` も) → **自分が書いたテストと、本文が名指しした既存のテストだけ**を名前で回す
   (`cargo test -p <crate> <名前>` / `cargo test --test <ファイル名>`)。**`cargo test --workspace` は回さない** (全体テストは波の最後に親が 1 回)。
   cargo とベンチは**必ず `scripts/mx` 経由** (2026-09-26 まで `~/.local/bin/mx` にしか無かったのをリポジトリに入れた) (機械のロック。`mx cargo test --test foo`)。A/B の一式は 1 回の `mx bash -c '…'` の中で。
   `.cargo/config.toml` の `jobs = 1` はそのまま。
4. **プロセス**: 手でプロキシを起動するときは `timeout 120`、`HOME` **と `XDG_CACHE_HOME` (か `PROXY_CACHE_DIR`)** は一時ディレクトリ (既定プロファイルの
   ディスクキャッシュは `HOME` を見ないので、`HOME` だけだと本物の `~/.cache/rust-http-proxy` を開く)、curl は `-m 5`、プロキシを自分宛てにしない。
   **止めるのは自分が起動した PID だけ** (`pkill -x rust-http-proxy` は使わない)。終わったら自分のプロセスを残さない。作業ファイルはスクラッチパッドに
   `<id>-` で始まる名前で置く。
5. **守ること**: 外部クレートは足さない。システムコールは `crates/sys/src/sys.rs` と同じ `unsafe extern "C"` + `#[cfg(target_os = "linux")]` (他 OS は従来の動き)。
   認証は入れない。**接続元・宛先・ポートの制限を既定で入れない** (利用者の決定 2026-09-18)。要求の経路は増やさない (本文が「例外」と書いた所だけ)。
   JSON は足すだけ (`"schema":1` のまま、欄を消さない・意味を変えない・既存の鍵の順を変えない)。コメントは日本語、ログの文字列は英語。
6. **性能に関わるとき** (本文が「測る」と書いた単位だけ): 変更前のバイナリを `rust-http-proxy` という名前のまま別ディレクトリに退避し、
   `scripts/cpu-per-request.sh` を前後交互に 3 組 (`--only connect` は **6 組**)・各 10 秒、ビルド直後は捨て走り 1 本、中央値で比べる。
   差が ±4% の中なら「ぶれの中」と書く。
7. **コミット**: 既存の形式 (`feat:` / `fix:` / `test:` / `docs:` / `refactor:` + 日本語 1 行 + 本文)。1 項目 1 コミットが基本。末尾に
   `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。手放す前に `git merge main` を 1 回 (親が `TODO.md` を動かしている)。
8. **できなかったとき**: 再現や計測が本文のとおりにならなかったら、**別の直しを自分で考えて入れない**。試したことと起きたことをそのまま報告して終わる。
9. **最後の報告に入れるもの**: コミットの hash・worktree と枝、前後の表 (測った単位)、回したテストの名前と本数、やり残しと気づいた問題、
   `TODO.md` に貼る `結果 (<日付>、<hash>): …` の 1 段落。**T15.0 の単位はこれに加えて「後ろの単位のために残すもの」**:
   (a) 足した欄の名前・型・単位・「通算か区間か」の表、(b) その欄を含む**作り物の JSON の例**を 1 つ (値は架空。本物の雪像から写さない)、
   (c) README の該当する口の説明に自分で足した行 (`scripts/check-docs.sh` は欄の説明を見ないので、足し忘れは誰にも見つからない)。
   **(c) は T15.0 以外のタスクにも効く** (`check-docs.sh` は README の説明文も既定値も見ない)。README の環境変数の表は**自分の行だけ**触る (並列のマージの衝突を避ける)。

**親がすること** (マージごと): `git merge --no-ff` → **触ったクレートごとに** `cargo check -p <crate> --all-targets` (ワークスペース全体の check は
テストの中の壊れを 1 回見逃した) → `python3 -m unittest discover -s scripts`、`node scripts/check-dashboard.js`、`./scripts/check-docs.sh` →
`結果:` を貼って `[x]` → 走っているエージェントに main が動いたことを知らせる → worktree を消す (枝は残す)。
**波の最後に 1 回だけ**: `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets -- -D warnings`、
`cargo test --workspace --no-fail-fast` (落ちた binary だけ 5 回繰り返して flake を切り分ける)、`cargo build --release`、
`scripts/build-memory.sh 180`、`--lite` のシステムコール数と確保数、`--only connect` の CPU/本。

- [ ] **T15.12 ビルドメモリの関門を 120 MB にする (理想値。旧 T15.0 — 2026-09-18 に番号を移した。デプロイのあと、24 時間の待ちに充てる)**
  - 目的: 関門は `jobs = 1` なので効くのは**いちばん重い 1 クレート**。T10.9 の 26 クレート構成では 100 MB で通っていた (最大 85.5 MB、最小 36 MB)。
    120 MB を切るには**全クレートを 100 MB 未満**に保つ必要がある (T14.55 の実測: 全部 100 MB 未満なら通る最小は約 120)。
  - **目標の決め直し (2026-09-19、利用者の決定)**: 関門の目標は 120 MB のまま、**目安は「通る最小が 100 MB 未満」** (関門に対して余裕を持たせる。120 ぎりぎりで通しても CI が揺れるだけ)。
    経験則は「通る最小 ≒ いちばん重い 1 クレートの RssAnon + 18〜20 MB」(T14.55 と 2026-09-19 の実測: 121.7 → 140) なので、**全クレートを約 80 MB 未満**にする必要がある
    (T10.9 の 26 クレート構成は最大 85.5 MB で通る最小 100 MB だった)。2026-09-19 の段 1〜3 のあとの実測で 80 MB を超えているのは 11 クレート:
    `proxy-metrics-watch` 121.7 / `proxy-metrics-window` 106.8 / `proxy-endpoints` 97.9 / `proxy-metrics-recent` 95.5 / 本体 94.6 / `proxy-net-dns` 89.7 / `proxy-metrics-core` 88.6 /
    `proxy-http` 86.3 / `proxy-metrics-types` 85.1 / `proxy-sysinfo` 82.8 / `proxy-blocklist` 81.9。**段 1〜3 だけでは足りず、段 5 以降を足す** (上から順に割り、割るたびに測る。
    RssAnon は行数に比例しないので、何が重いのか (単相化、`format!`、大きな `match`、テーブル) を先に調べてから線を決める)。
  - **T15.0 / T15.4 / T15.6 と並行しない**: このタスクが割るのは `proxy-endpoints` / `proxy-metrics-core` / `proxy-net` の 3 つで、しかも**ファイルを割る**ので、同じクレートに
    行を足すタスクと並行すると delete / modify の衝突になる。T15.4 (`crates/net/src/dns.rs`) が main に入ってから始める。**着手の前に、まず `scripts/build-memory.sh --find` と
    クレートごとの RssAnon の表を測り直す** (T14.55 の表は T15.0 の波の前のもの。T15.0 は 3 クレートとも太らせた)。
  - **いまの大きさ (2026-09-19 の下読み。main `5f6523b`、総行数 / 非テスト行)**: `proxy-endpoints` **4,808 / 3,001** (`endpoints/mod.rs` 1,158 / 813、`recent.rs` 1,932 / 838、
    `explain.rs` 817 / 694、`health.rs` 265 / 206、`profile.rs` 201 / 201、`config.rs` 214 / 97、`pac.rs` 130 / 61、`blocklist.rs` 67、`lib.rs` 24。ほかに `src/web/` の HTML 3 本 =
    81,673 + 58,188 + 23,754 = **163,615 B** を `include_str!`)。`proxy-metrics-core` **3,310 / 1,956** (`metrics.rs` 2,554 / 1,498、`canary.rs` 737 / 439、`lib.rs` 19)。
    `proxy-net` **3,725 / 2,088** (`dns.rs` 2,028 / 1,176、`net.rs` 1,235 / 589、`acl.rs` 448 / 309、`lib.rs` 14)。T14.55 の表で 100 MB を超えていたのはこの 3 つだけ
    (115.6 / 110.2 / 108.5 MB)。**RssAnon は行数に比例しない** (net は endpoints の 2/3 の行数でほぼ同じ)。
  - **割るときに当たる所 (下読みが見つけた。知らずに始めると途中で設計をやり直すことになる)**:
    - `recent.rs` は **`mod.rs` を上向きに呼ぶ**: `recent::snapshot` の中の `recent.rs:724-726` → `super::status_body`、`730,734,738` → `super::history_body`、
      **`751` → `super::profile::profile`**。`profile.rs:64` も `super::recent::MAX_BODY`。submodule が使う私有ヘルパは `pub(super)` の `has_flag` (`mod.rs:272`) / `offset_param` (285) /
      `next_offset` (295) / `status_body` (601) / `history_body` (623) / `history_summary` (636) と `pub` の `parse_query` (771) / `percent_decode` (783)。
    - `health.rs:173` は `crate::anomaly::cpu_window_is_full` (= `proxy-metrics-watch`) を見る (T15.0 (6))。facade 越しなのでクレートの層は壊さない。
    - **`pub(crate) static RESOLVE_TEST_LOCK`** (`crates/net/src/dns.rs:256`) を `net.rs:1185` と `acl.rs:436` のテストが取る → 割るとコンパイルが通らない (`pub` にする。
      T14.55 の `events::TEST_LOCK` と同じ)。割ったあとは 3 クレートのテストが**別プロセスになり、この鍵では直列化が効かなくなる** (`dns.rs:253-256` のコメントが前提を書いている)
      ので、`mx cargo test -p proxy-net-dns` / `-p proxy-net-conn` / `-p proxy-net` を別々に回す。
    - **`net.rs:1203` のテストが `crate::acl::resolve_target` を呼ぶ** → そのまま割ると `proxy-net-conn` が dev-dependency で acl 側を要求し、**T14.55 が踏んだ
      「dev-dependency の輪でクレートが 2 回コンパイルされてトレイトが別物になる」そのもの**になる。このテスト (`net.rs:1185-1225` 付近) は acl 側へ移す。
    - 逆向きの依存は本文どおり 1 か所: `crates/net/src/dns.rs:644` の `crate::net::split_host_port_ref`。`proxy-sys` を使うのは `net.rs:158,299` だけなので、
      `proxy-net-dns` の依存は `proxy-base` だけで済む。
  - やること (**4 つに分けて、1 つ = 1 エージェント = 1 worktree。1〜3 は単独でマージできて価値がある。順番は 段 2 と段 3 を同時に着手 → 段 2 を先に取り込み → 段 3 →
    段 1 → 段 4**。段 2 は新しいクレートを作らないので `Cargo.toml` の `members`・`Cargo.lock`・README のクレートの表・`check-docs.sh` の (e) に一切触らず、ほかの段と完全に並列でよい。
    段 1 と段 3 はその 4 つを共有する (行の追加なので衝突は自明に解ける)。段 1 は `explain.rs:92,176` / `pac.rs:17` / `blocklist.rs:18` で `split_host_port_ref` を呼ぶので、
    段 3 を先に取り込めば段 1 のリベースは 1 回で済む):
    1. **`proxy-endpoints` を割る** (逆依存は本体だけ)。**段階的に、測りながら**: (i) まず HTML 3 本だけを葉クレート `proxy-web` (中身は `include_str!` の 3 つの const だけ) に出して
       `--find` で測る (160 KB の文字列定数が rustc に効くかは未測定。逆依存ゼロ・20 行で、効けばここで終わる)。(ii) 足りなければ `explain.rs` (694 行。`super::{Endpoint, parse_query}` しか
       使わず、呼ぶのは `mod.rs:484` の 1 か所) を下ろす。(iii) それでも足りなければ `Endpoint` (`mod.rs:32-59`) + 私有ヘルパ + `status_body` / `history_body` / `history_summary` +
       recent / profile / health / config / pac / blocklist をまとめて下の 1 クレート (`proxy-endpoints-detail`) へ移し、`mod.rs` には `handle` (313 行〜)・`local_path`・`endpoint_list`・
       `is_heavy` / `begin_heavy`・`register_snapshot`・`purge_url`・`lookup` を残す (`path == "/…"` の 33 本が `mod.rs` に残るので `check-docs.sh` の (a) が生きる。recent → mod の
       上向き 3 本もこれで一方向になる)。
    2. **`canary` を `proxy-metrics-core` から `proxy-metrics-watch` へ上げる** (逆依存は `metrics-watch` だけ)。`crates/metrics-core/src/metrics.rs:1274` (`push_env` の中) の
       `crate::canary::status_json()` は、**`crates/metrics-types/src/kernel.rs:312-318` の `set_state_file_errors(f: fn() -> Option<u64>)` を写した口**
       (`metrics::set_canary_status(fn() -> String)`) でほどく。**既定 (預けていないとき) は canary が off のときと 1 バイト同じ文字列を返すこと** (預け忘れると `/status` の `canary` が
       静かに変わる)。`crates/metrics-watch/src/lib.rs:16` の再輸出から `canary` を外して `pub mod canary;` にし、`crates/metrics/src/lib.rs:24-27` の再輸出はそのまま →
       `prom.rs:589` / `reload.rs:228` / `tick.rs:85` / `tests/canary_test.rs` は 1 行も変わらない。`canary.rs` のテスト (440〜737 行) は `Metrics` を使うが、上へ上げれば向きが揃う。
       ~~`metrics-watch` は T14.55 で 90.5 MB だったので、canary を足して 100 を超えたら `push_head` / `push_tables` / `push_env` / `memory_json` を新しい葉クレートへ出す。~~
       **(2026-09-19 訂正: この保険は宛先違い。4 つとも `crates/metrics-core/src/metrics.rs` に居るので、出して減るのは `metrics-core` の方で `metrics-watch` には効かない。
       親の書き間違い。`metrics-watch` を軽くするのは段 5。)**
    3. **`proxy-net` を `proxy-net-dns` → `proxy-net-conn` → `proxy-net` (acl + facade) に割る** (逆依存 11 クレート)。`split_host_port` と `split_host_port_ref` (とそのテスト) を
       **`proxy-base` の新しい `hostport` モジュール**へ移し、`proxy-net-conn` の `net` モジュールで `pub use` して**綴りを維持**する (使っているのは 12 か所)。
       **facade の形 (`crates/net/src/lib.rs:6-14`) を崩さなければ 11 クレートは 1 行も変わらない**。
    4. **3 つ入って `--find` が 120 で通ったら**関門を 120 に下げる。書き換え先: `scripts/build-memory.sh:26,31,32`、`.github/workflows/ci.yml:54,61,163` (+ ladder の既定の 14 行と 217 行)、
       `src/lib.rs:6`、**`crates/*/src/lib.rs` の冒頭コメント ちょうど 30 本** (`grep -l "180 MB" crates/*/src/lib.rs`)、`README.md:1717,1718,1724,1728,1729,1767,1768,1910,1925,1937` と
       1674 行の「(32 + 本体)」・1720〜1721 行の「いちばん大きいクレート」・1886 / 1926〜1927 行の「26 クレート」、この TODO.md の §0 (57 行) / §1 (267・280 行) / §2 (368 行。親が書く)。
       手本は `2ac988f` (関門の数字を 29 ファイル一括で書き換えた)。通らなければ 4 はやらず、測った表だけ残す。
    - どの段も**中身は 1 バイトも変えない**。手本は `6967236` (`proxy-metrics` を 6 つに割った。`git mv` で rename の検出を効かせ、各 `lib.rs` で下の層を今までの名前で再輸出) と
      `13fa546` (層の制約を分けてほどいた形)。割るたびに `mx bash -c 'cargo clean --release && scripts/build-memory.sh --find 110 115 120 125 130 135'` (1 周 約 1 分 24 秒。いまの最小は 135 MB) と
      クレートごとの RssAnon の表を `結果:` に貼る。配布用 `dist` (LTO) は関門の外のまま。
  - **直値で見張っているもの** (動かすと落ちる): `scripts/check-docs.sh` の (e) (README のクレートの表と「(32 + 本体)」の数 → 段 1 と 3 で必ず落ちるので表を直す)、同 26 行の
    `MOD=crates/endpoints/src/endpoints/mod.rs`・27 行の `REC=…/recent.rs`・170 行の `open('crates/endpoints/src/web/' + html)` (段 1 でファイルを動かしたら 3 か所とも書き換える)、
    `scripts/check-dashboard.js:46 / 757 / 1134` (HTML 3 本を直に開く。**`dashboard.html` は上限まで余り 247 B なので、移すときに 1 バイトも増やさない**)、
    `crates/endpoints/src/endpoints/mod.rs:880` と `:1043` のテスト (私有の `super::DASHBOARD_HTML` / `super::INSPECT_HTML` を読み、`inspect.html <= 64 KiB` を直値で見る → const と一緒に動かす)。
  - テスト: 新しいテストは要らない (移したテストが同じ名前で残ることを確かめる)。段ごとに `mx cargo test --test schema_test` (`/` の案内から全部の口を機械的に取り出して触る、いちばん安い全口の確かめ)、
    `node scripts/check-dashboard.js`、`./scripts/check-docs.sh`。**このタスクは段ごとに全体テストを回す** (`cargo test --workspace --no-fail-fast`。中身を変えないことの証拠がそれしか無い)。
    段 1 は、24 本のエンドポイントを分割の前後で取り、入れ子まで鍵の並びを突き合わせる (`/status` の出力の順序)。最後に `cargo build --profile dist`。
  - 受け入れ基準: 1〜3 はそれぞれ、割ったクレートの RssAnon が 100 MB 未満になったことを表で示すこと。4 は `scripts/build-memory.sh 120` が通ること。
    ビルド時間の増分を報告 (クレートが増えるぶん)。全体テスト全通過、24 本の JSON の鍵の並びが前後で同じ。
  - 結果 (段 1〜4、2026-09-19、波 `wave10`。取り込み `085cee0` 段 3 → `cf908a1` 段 2 → `9626c8b` 段 1、段 4 は変更なし): **段 1〜3 はそれぞれ狙ったクレートを 100 MB 未満にしたが、
    関門 120 MB は通らない。通る最小は 135 → 140 MB に上がった** (135 は 2 回とも本物の cgroup OOM-kill)。クレートは 32 → **37 + 本体**、release ビルド 89 → 95 秒、テスト 790 → 792 本。
    中身は 1 バイトも変えていない (全体テスト全通過、`--lite` の費用は システムコール 5.028 回/要求・確保 10.006 回/要求で不変、`/` の案内の 32 経路すべて応答)。
    - 測り方はどれも同じ: 手元 aarch64、`cargo clean --release` → `cargo build --release`、`jobs = 1`、20 ms ごとに `/proc/<pid>/status` の RssAnon の最大。
  - 結果 (2026-09-26、CI run 36249911685、`build-memory-find`、ubuntu-24.04 / x86_64、main `e8aef0a` = T17.11 の後、`-f ladder="80 85 90 95 100 110 120"`): **80・85・90・95 MB は落ち (exit 143)、100 MB で通った** (通る最小の上限 100 MB)。手元 (aarch64、2 コア) は毎回落ちる最大 85 / 毎回通る最小 90 MB なので、**CI は手元より 10 MB 重い**。関門 120 MB に対し CI で 20 MB、手元で 30 MB の余裕。push の `build-memory` ジョブ (120 MB) も通過 (run 36249391531)。

      | クレート | 着手前 (`930b90f`) | 段 1〜3 の後 (`9626c8b`) | 何が起きたか |
      |---|---|---|---|
      | `proxy-metrics-watch` | 94.9 MB | **121.7** | 段 2 で canary を受け取って +26.8 (いまの最大) |
      | `proxy-metrics-window` | 107.5 | **106.8** | 手を付けていない (T15.0 の波で 97.1 → 107.5 と 100 を超えていた。計画に段が無かった) |
      | `proxy-endpoints` | 119.0 | 97.9 | 段 1 (ii) で -21.1 |
      | `proxy-metrics-recent` | 94.6 | 95.5 | — |
      | 本体 | 93.8 | 94.6 | — |
      | `proxy-net-dns` | (旧 `proxy-net` 111.2) | 89.7 | 段 3 で新設。`proxy-net-conn` と `proxy-net` は 80 未満 |
      | `proxy-metrics-core` | 111.7 | 88.6 | 段 2 で -23.1 |
      | `proxy-http` / `metrics-types` / `sysinfo` / `blocklist` | 86.4 / 84.5 / 82.6 / 83.6 | 86.3 / 85.1 / 82.8 / 81.9 | — |
      | 新しいクレート | — | `endpoints-explain` 61.3 / `endpoints-core` 30.3 / `web` 7.2 | 段 1 |

    - **段 1** (`31dd143` / `e1168cb`): (i) HTML 3 本 (163,615 B) を葉クレート `proxy-web` へ → **-0.9 MB しか効かない。160 KB の文字列定数は rustc のメモリにほぼ関係ない** (本文の「未測定」への答え)。
      (ii) `explain.rs` と `Endpoint` + 問い合わせ文字列のヘルパを `proxy-endpoints-explain` / `proxy-endpoints-core` に下ろして 119.0 → 98.4 MB。100 を切ったので (iii) はやっていない。
    - **段 2** (`37ffb70`): `canary.rs` 737 行を `git mv` で `metrics-watch` へ。`push_env` からの呼び出しは `metrics::set_canary_status(fn() -> String)` の預け口でほどき、預かっていないときの既定
      `CANARY_STATUS_UNSET` が canary off の `status_json()` と 1 バイト同じことを新しいテスト 2 本で縛った。`metrics-core` は 100 未満になったが、**同じ重さが `metrics-watch` に移っただけ**
      (-23.1 と +26.8。canary はどこに置いても約 25 MB)。レビューの must_fix 2 件: README のクレートの表の説明が古いまま (直した)、本文の保険が宛先違い (上の訂正)。
    - **段 3** (`435bcee` / `938072c`): `proxy-net` を `proxy-net-dns` → `proxy-net-conn` → `proxy-net` (acl + facade) に。逆依存 11 クレートと本体は 1 行も変えていない。本文に無かった判断:
      `IPV6_TEST_LOCK` も `pub` にした (`crates/net-conn/src/net.rs:27`)、`proxy-net-conn` は `proxy-base` にも直接依存する (`proxy_base::timeout` を名指しで呼ぶ)。
      古いパス (`crates/net/src/net.rs` / `dns.rs`) を指すコメント 8 か所を直した。
    - **段 4**: `--find 105…130` は全部落ち、`135` も落ち、**通る最小は 140 MB** (= 121.7 + 18)。関門は 180 のまま、書き換えはしていない。
    - 気づき: (1) 重さは「移す」だけでは減らない。canary の約 25 MB のように、**どのモジュールが何 MB 持っているかを先に測ってから線を引く** (段 5 以降の前提)。
      (2) 計測スクリプトは機械上の全 rustc を拾うので、`mx` が 2 口のときは 1 回の値を信じない (2 回取る)。(3) 揺れるテストが 1 本見つかった (`clientacl_test`。Phase 14 の「既知の小物」に足した)。
  - **何が重いのかの調査 (2026-09-19、親が測った。段 5 以降の前提)**: `RUSTC_BOOTSTRAP=1 mx cargo rustc --release -p proxy-metrics-watch -- -Ztime-passes` (安定版の rustc 1.96.0 でそのまま動く。
    `rss:` は VmRSS でファイル由来のページを含むので、絶対値ではなく段の間の差を見る) で、**山はコード生成 (LLVM) の段**だった: 型検査の終わり 151 MB → メタデータ 187 MB →
    `codegen_to_LLVM_IR` + `LLVM_passes` で **259 MB (+72 MB)**。`[profile.release]` の `codegen-units = 1` (最初のコミットからあり、**一度も測って決めていない**。`git log -S` で確認) が
    クレート全体の LLVM IR を 1 つのモジュールとして一度に抱えさせている。
    - **`codegen-units` を増やすと山が削れる** (`jobs = 1` なので rustc の中の LLVM の並列もトークン 1 つで、モジュールは順に最適化して捨てられる)。全クレートに当てた実測
      (`CARGO_PROFILE_RELEASE_CODEGEN_UNITS=<n>`、測り方は上の表と同じ):

      | | `codegen-units = 1` (いま) | `= 4` | `= 16` |
      |---|---|---|---|
      | `proxy-metrics-watch` | 121.7 MB | **92.5** | 84.3 |
      | `proxy-metrics-window` | 106.8 | 83.2 | 81.4 |
      | `proxy-endpoints` | 97.9 | 80.2 | 80.2 |
      | 本体 (`rust_http_proxy`。lib と bin が同じ名前で混ざる) | 94.6 | 79.2 | 79.3 |
      | `proxy-metrics-recent` | 95.5 | 76.9 | 75.4 |
      | `proxy-config` / `proxy-http` / `metrics-core` / `metrics-types` / `net-dns` | 76.0 / 86.3 / 88.6 / 85.1 / 89.7 | 74.6 / 72.4 / 70.5 / 68.9 / 68.1 | 74.6 / 71.8 / 69.7 / 66.6 / 66.6 |
      | **通る最小の上限** (`--find`) | **140 MB** | **120 MB** (115 は落ちる) | 115 でも通らない (それ以上は未測定) |
      | release ビルドの時間 / バイナリ | 95 秒 / 2,498,360 B | 106 秒 / 2,694,968 B | 133 秒 / 2,760,504 B |

      4 → 16 はほとんど増えない (コード生成の前の段 = 型検査とメタデータの床が約 80 MB)。**4 で十分**。
    - **`= 4` にすると床が「最後の 1 段」に移る**: 115 MB で落ちるのは `proxy-metrics-watch` ではなく、**bin の `rust-http-proxy` (`src/main.rs` 599 行) をコンパイルしてリンクする最後の段**。
      `touch src/main.rs` してこの段だけを cgroup に入れると 110 MB で落ち 118 MB で通る (110〜118 の間は回ごとに揺れる)。`dmesg` の OOM の記録は毎回
      `Killed process (rustc) anon-rss:55 MB, file-rss:79 MB`。`-Ztime-passes` では bin なのに `codegen_crate` が **+62 MB** (130 → 192 MB、LLVM 1.5 秒) ある = lib から bin へ単相化・インライン化されて
      来るものが多い。~~リンカを変えても効かない~~ (`-fuse-ld=gold` は 95 MB で落ちる。`-Wl,--no-keep-memory` は 110 MB が通るようになる)。
      **(2026-09-19 訂正、段 6 の実測: 最後の段の山はコード生成ではなく「リンク中」= rustc が 55〜59 MB を握ったまま `ld` が 38〜49 MB を積む。`ld` は子プロセスなので `-Ztime-passes` の `rss:` に入らず、親が見落とした。下の段 6 の結果)****ここを削らないと、クレートをどれだけ割っても通る最小は約 115 MB から下がらない。**
    - 代償は実行時の速さの可能性 (クレートの中でのインライン化の範囲が狭まる)。CPU/要求 の 7 割はカーネル側 (T9.1) なので大きくは動かないはずだが、**測って決める**。
      いまこの機械は利用者のほかの作業で CPU が汚れていて A/B が取れない (2026-09-19) → **要求の経路に乗らないクレートだけ先に 4 にする** (段 5)。要求の経路のクレートは段 7 で (手元の A/B ではなくデプロイ先で見る。段 7 の本文)。
  - **続きの段 (2026-09-19 に足した。段 5 と段 6 は触るファイルが別なので並列でよい。枝は `wave11/<id>`)**:
    5. **要求の経路に乗らないクレートの `codegen-units` を 4 にする** (`t1512s5`)。ルートの `Cargo.toml` に `[profile.release.package.<名前>]` `codegen-units = 4` を並べる (`dist` は `inherits = "release"` なので
       同じ上書きが効く。`dist` は関門の外で fat LTO なので、`[profile.dist.package.<名前>]` で 1 に戻す必要があるかを `cargo build --profile dist` のバイナリの大きさで確かめ、変わるなら戻す)。
       **(2026-09-19 訂正: 下の 14 個のうち `proxy-endpoints` / `proxy-metrics-window` / `proxy-selfbench` / `proxy-reload` / `proxy-sysinfo` の 5 つは要求の経路から呼ばれていた。親の思い込み。`src/lib.rs` に `handle_client` という関数も無い。段 5 の結果)**
       **対象 (親が決めた。1 要求・1 接続ごとには呼ばれない = tick スレッド・管理用の口・起動と再読込・道具)**: `proxy-metrics-watch`、`proxy-metrics-window`、`proxy-endpoints`、`proxy-endpoints-explain`、
       `proxy-endpoints-core`、`proxy-config`、`proxy-sysinfo`、`proxy-prom`、`proxy-reload`、`proxy-selfbench`、`proxy-rrd`、`proxy-diskprobe`、`proxy-capacity`、`proxy-bench`。
       **対象にしない (要求か接続ごとに通る。段 7 まで 1 のまま)**: 本体、`proxy-http`、`proxy-tunnel`、`proxy-net` / `-conn` / `-dns`、`proxy-base`、`proxy-sys`、`proxy-metrics` / `-types` / `-core` / `-recent`、
       `proxy-blocklist`、`proxy-cache*`、`proxy-origin`、`proxy-msg`、`proxy-tls`、`proxy-workers`、`proxy-freshness`、`proxy-web`。着手したら、対象のクレートの関数が要求の経路から呼ばれていないことを
       `src/lib.rs` の `handle_client` からたどって確かめ、呼ばれているものがあれば対象から外して報告する (`proxy-config` の `Config` の読み出しはフィールドの参照なので対象のままでよい)。
       受け入れ基準: 対象のクレートが全部 95 MB 未満 (表)、`--find` の通る最小が 140 MB から下がること (見込みは 115〜120。`metrics-recent` 95.5 + 18 と最後の段のどちらかで決まる)、
       `--lite` の システムコール/要求 と 確保/要求 が不変 (`scripts/cpu-per-request.sh`。CPU/要求 は機械が汚れているので参考値)、全体テスト全通過、`cargo build --profile dist` が通ること。
       `Cargo.toml` の `[profile.release]` のコメントに、この実測の表 (1 / 4 / 16) と「要求の経路のクレートは 1 のまま」の理由を書く。
    6. **最後の段 (bin のコンパイルとリンク) を 95 MB で通るようにする** (`t1512s6`)。まず再現と切り分け: `mx cargo build --release` のあと `touch src/main.rs` →
       `mx systemd-run --user --scope --quiet -p MemoryMax=<N>M -p MemorySwapMax=0 "$(command -v cargo)" rustc --release --bin rust-http-proxy` を N = 90 / 100 / 110 / 120 で (各 3 回。110〜118 は揺れる)。
       `-Ztime-passes` と、cgroup の `memory.peak` / `memory.stat` (anon / file) を 20 ms ごとに読んで、**山が rustc のコード生成なのかリンカなのか**を先に決める。コード生成なら、bin に単相化されて来るものを
       `RUSTC_BOOTSTRAP=1 cargo rustc --release --bin rust-http-proxy -- -Zprint-mono-items=lazy` (または `--emit=llvm-ir` の大きさ) で数え、`src/main.rs` の `main` の中身 (`check_environment` / `run_self_bench` /
       起動の組み立て) を **lib の側の、総称でも `#[inline]` でもない `pub fn`** へ移して、bin は `fn main() { std::process::exit(rust_http_proxy::run_main()) }` に近い形にする
       (lib の側が太るので、lib の RssAnon が 95 MB を超えないことを表で確かめる。超えるなら移す先を新しいクレートにする)。リンカが山なら `.cargo/config.toml` の `[target.…] rustflags` ではなく
       (全クレートが作り直しになり、利用者の環境のリンカに依存する) まず rlib の大きさ (`target/release/deps/*.rlib` 合計 26 MB) と `strip` / `debuginfo` の設定を見る。
       **挙動は 1 バイトも変えない** (`--check` の 9 行、`--help`、終了コード、起動ログの順序。`tests/` の起動系のテストと `scripts/check-docs.sh` が見張っている)。
       受け入れ基準: 最後の段だけを入れた cgroup が **95 MB で 3 回中 3 回通る**こと、前後の `-Ztime-passes` の比較、全体テスト全通過、`--lite` の費用が不変。届かなければ、測った内訳と「次に何を動かせば何 MB 減るか」を報告する (変更なしでもよい)。
       - 結果 (2026-09-19、波 `wave12`。`af74634` (server) → `434f7e1` (run) → `482c651` (レビューの直し)、取り込み `309fe81`): **最後の段の床を 106 → 65〜70 MB に下げた。通る最小は動かない (見込みどおり) —
         ただし「120」は通ったり落ちたりする境目だと分かった**。`src/lib.rs` / `src/idle.rs` / `build.rs` を `crates/server/` へ、`src/main.rs` を `crates/run/src/lib.rs` へ `git mv`。ルートは
         `pub use proxy_server::*;` の facade と `fn main() { proxy_run::main() }` だけ。`pub` を足した項目は 0 (`fn main` → `pub fn main` だけ)。`.rs` の差分は doc リンク `[proxy_stats]` → `[proxy_metrics]`、
         `pub fn main`、rustfmt が 2 か所を 1 行に詰め直した (綴りが 4 文字短くなったため) の 4 つ。
         **最後の段だけの cgroup: 70 MB 3/3 (別の組で 2/3)・75 MB 3/3・65 MB 0/3** (前は 110 MB 2/3・120 MB 3/3)。RssAnon: `proxy-server` 94.4 / `proxy-run` 94.2 / ルート (facade + bin) 93.5 → **18.6**。
         上位は `metrics-window` 107.8 > `endpoints` 98.4 > `metrics-recent` 95.7 で変わらず。**`--find`: 同じ日に main (`e797376`) と交互に 6 回ずつで、120 MB は main 2/6・枝 0/6、直しの担当の測り直しで枝 1/2、
         125 MB は毎回通る、115 以下は毎回落ちる、落ちるのは毎回 `proxy-metrics-window`** → README / `Cargo.toml` / `build-memory.sh` の「120 MB で通る」を「120〜125 MB (120 は境目)」に直した。
         挙動は同一: `--help` / `-h` / `-V` / `--version` / `--check` / 未知のフラグの出力と終了コードは一時パス・版の hash・`resolver_ms` 以外 `diff` なし、`--lite` は 5.021 → 5.020 回/要求・確保 10.006 → 10.006、
         release 2,563,896 B で不変 (dist −8 B)。版の文字列は `build.rs` ごと移して `--version` が新しい HEAD になることを確認。`Dockerfile` の `COPY` と README の Docker の節から `build.rs` を外し、
         README のクレートの表に 2 行 (39 + 本体)、パスのコメント 19 か所。全体チェック (絞った版、約 25 分): fmt / clippy 警告 0 / **792 本通過** / 関門 180 / `dist` / scripts 213 本 / dashboard / check-docs 差分 0 /
         起動して `--version` と `/status` の版が `0.1.0+309fe81`。実装中に `proxy-sys` の fd を数えるテストが 1 回揺れた (「既知の小物」に足した)。残り: `crates/web/src/probe.html:137` の `src/lib.rs` という文言は
         配る HTML の中身なので直していない (次に probe.html を触るときに)。
       - 気づき: **cgroup の合否も境目では揺れる** (120 MB が 2/6)。通る最小は 1 回の `--find` の値ではなく「毎回通る最小 / 毎回落ちる最大」の幅で書く (いまは 125 / 115)。関門を下げるときは「毎回通る最小 + 余裕」で決める。
    7. **全クレートを `codegen-units = 4` にする (T15.99 のあと、段 6' が入ってから。手元の CPU の A/B はやらない)**。
       **2026-09-24、利用者の決定: 4 で次の再デプロイに入れて試す。問題があれば利用者が戻すよう指示する** (戻し方は `[profile.release]` の `codegen-units` を 1 にする 1 行。段 5 の上書きは消したままでよい)。
       - 結果 (2026-09-24、`3acd5b5`、取り込みは段 7 のマージ): `[profile.release]` を `codegen-units = 4` にし段 5 の上書き 9 個を消した (戻すときはこの 1 行)。`dist` は release の 4 を継ぐとファイルは 1,842,976 B のまま
         (ページの詰め物で隠れる) だが `.text` が 1,769,784 → 1,786,700 B に太ったので `[profile.dist]` に `codegen-units = 1` を足した (`.text` 1,766,292 B)。**段 5 の「dist は前後で不変」もファイルの大きさだけの判定だった**。
         RssAnon の最大は `metrics-window` 108.0 → **`metrics-watch` 92.3 MB** (以下 window 82.9 / endpoints 79.9 / server 79.6 / recent 75.4 / config 73.9 / http 72.2 / core 70.8 / net-dns 68.0 / types 66.9 / run 64.5)。
         release 2,563,896 → 2,694,968 B、クリーンの release 97 → 103 秒。`--find` (2 回): 95 MB 2/2 落ち (metrics-window)、100 MB 2/2 落ち (metrics-watch)、**105 MB 1/2**、2 回目の 110 MB は**この機械のメモリ不足** (利用者のほかの作業。空き約 0.5 GB)
         で Claude Code が止めたので未測定 → **毎回落ちる最大 100 / 105 は境目 / 毎回通る最小は 110 以上で未確定**。README は「約 110 MB (105 は境目)」と書いた。`--lite` 5.022 → 5.023 回/要求・確保 10.006 → 10.006、
         テスト 792 本通過、clippy 警告 0、check-docs 差分 0。CPU の A/B は方針どおり取っていない。**残り: 機械のメモリに余裕があるときに `--find 105 110 115` を 2 回** → 毎回通る最小を決めてから段 4 / 段 8。
         - **測り直し (2026-09-26、rustc stable 1.98.1、この機械 aarch64 2 コア・空き 9.6 GB)**: T16.99 で `codegen-units = 4` を残すと決めたあと、`cargo clean --release` +
           `scripts/build-memory.sh --find 100 105 110 115 120` を 2 回: **100 MB は 2/2 落ち (cgroup の OOM で exit 143)、105 MB は 2/2 通った** (1.96 では 1/2)。
           **毎回落ちる最大 100 / 毎回通る最小 105**。利用者の目安 (「通る最小が 100 MB 未満」、2026-09-19) にはまだ届かないので、**段 4 (関門を 120 に下げる) はやらない**。関門は 180 のまま。
           次に下げるなら上位 (`proxy-metrics-watch` 92.3 MB ほか、段 7 の表) を割る段 8 から。
       **2026-09-19、利用者の指摘で方針を変えた**: デプロイ先とこの機械は別物 (CPU も kernel も違う) で、しかも手元の CPU/要求 はぶれ ±4%。`codegen-units` の効きはユーザー空間 (CPU/要求 の約 3 割、T9.1) の
       さらに一部で、fat LTO でも −4.5% だった (T9.1) ことからぶれより小さい見込み → **手元で A/B を取っても何も決められないので測らない**。代わりに:
       (i) 手元では**揺れない数字だけ**を確かめる (`--lite` のシステムコール/要求と確保/要求が不変、全体テスト、バイナリの大きさ)。(ii) `[profile.release]` を 4 にして段 5 の上書き 9 個を消す。
       (iii) **速さはデプロイ先で見る**: 次の再デプロイの前後の雪像で conn 役の CPU (`scripts/snapshot-diff.py` の `_p15_conn_cores` と同じ見方: コア/要求) と `/profile` の段階の内訳を比べ、その Phase の `Tn.99` で判定する。
       **ただしデプロイ先もほかの利用者が出入りする共有の機械** (2026-09-19、利用者) なので、そこでの CPU も隣人しだいで揺れる。**数 % の差は読まない。見るのは「桁で悪くなっていないか」だけ**
       (conn 役のコア/要求が前の 1.3 倍を超える、`/profile` のユーザー空間の段階が目に見えて太る、`cpu_throttled` が出る、のどれか)。それ未満は「差なし」として 4 のままにする。
       悪くなっていたら `[profile.release]` を 1 に戻す 1 行 (と段 5 の上書きを戻す) で済む。T15.99 の 24 時間の観測を乱さないよう、**T15.99 の判定が終わるまでデプロイ先には入れない**。
       見込み: 全クレート 4 + 段 6' で、最大は `proxy-metrics-watch` 92.5 MB 前後 / 最後の段 約 77 MB → 通る最小 約 105〜110 MB。100 未満にはさらに上位 (`metrics-watch` / `metrics-window` / `endpoints`) を割る段が要る
       (割る前に、どのモジュールが何 MB 持っているかをモジュールを 1 つ抜いたビルドで測る)。
    8. 5〜7 のあと `--find` が 100 未満で通るようになったら、段 4 (関門を 120 に下げる書き換え) をやる。
  - 結果 (段 5・段 6、2026-09-19、波 `wave11`。段 5 は `2746075` / `687820f`、取り込み `65b7667`。段 6 はコミット無し): **通る最小 140 → 120 MB。関門の目標 120 にちょうど届いたが余裕は 0 で、床は bin の最後の段 (リンク中)**。
    全体チェック: fmt / clippy 警告 0 / **792 本通過** (1 回目に `history_depth_test` が 1 本揺れた。下) / 関門 180 通過 / `--find` は 100〜115 が本物の OOM で 120 が通る / `--lite` は 5.026 回・9.99 回で不変 /
    release 2,563,896 B (+65 KB)、`dist` 1,842,984 B (fat LTO が CGU の分割を呑むので前後で不変)。`.rs` は 1 本も変わっていない。
    - **段 5**: 親が挙げた 14 個のうち **5 個は要求の経路から呼ばれていた**ので実装担当が外した: `proxy-endpoints` (`endpoints::handle` → `local_path`。プロキシとして通す要求も毎回通る、`src/lib.rs:1639`)、
      `proxy-metrics-window` (`profile::mark` ほか + `Window::observe` = 毎要求・毎接続)、`proxy-selfbench` (`is_client`、`src/lib.rs:1431` = 毎要求)、`proxy-reload` (`Live::config` = accept のたび)、
      `proxy-sysinfo` (`drop_page_cache` = キャッシュ HIT / 書き込みごと)。要求の経路は `serve` (`src/lib.rs:122`) → `run_conn` (:1267) → `pump` (:1060) → `serve_one` (:1296) → forward / `tunnel`。
      残りの 9 個 (`proxy-metrics-watch` / `-endpoints-explain` / `-endpoints-core` / `-config` / `-prom` / `-rrd` / `-diskprobe` / `-capacity` / `-bench`) を `[profile.release.package.<名前>] codegen-units = 4` に。
      `proxy-metrics-watch` 121.7 → **92.2 MB**。いまの上位: `metrics-window` **107.7** > `endpoints` 98.7 > `metrics-recent` 94.9 > 本体 93.5 > `metrics-watch` 92.0 > `net-dns` 89.5 > `metrics-core` 88.8 > `http` 86.6。
      経験則「最大 + 18〜20」は今回は当たらない (107.7 + 12 = 120。決めているのは最後の段)。レビューの should_fix: `Cargo.toml` と README の `dist` / release のバイナリの大きさが 2026-09-08 の古い値のままだった (直した)。
      全体チェックの指摘: `proxy-sysinfo` / `proxy-reload` / `proxy-selfbench` を外した理由が `Cargo.toml` のコメントに無い (理由はこの段落。次に `Cargo.toml` を触るときに 1 行足す)。
    - **段 6 (変更なし。測った内訳が収穫)**: 最後の段を 20 ms ごとに rustc と `cc` / `ld` の全子孫の `/proc/<pid>/status` で見ると、同時刻の RssAnon の合計の最大は**リンク中**:
      `codegen-units = 1` で rustc 59 + `ld` 38 + cargo 9 = **106.1 MB**、`= 4` では rustc 55 + `ld` 49 + cargo 9 = **112.9 MB** (オブジェクトが増えて `ld` が太る = 4 は最後の段を悪くする)。
      cgroup での合否 (各 3 回): 90 MB 0/3、100 MB 0/3、110 MB 2/3、120 MB 3/3。**bin が重い正体は `serve` が `config_of: impl Fn()` の総称 (`src/lib.rs:126`) で、`serve` 全体が bin の側で単相化されること**
      (bin の `codegen_crate` +103 MB、LLVM 3.3 秒。lib の 95 MB には `serve` のコード生成が入っていない)。
      試した直し: `src/main.rs` の中身を lib の `pub fn run::main()` へ → 最後の段 106.1 → **66.0 MB** (95 MB の cgroup 3/3、70 MB でも 3/3)、`--help` / `-V` / `--check` / 終了コード / バイナリ 2,498,360 B まで同一。
      だが **lib が 95.3 → 134.8 MB** になり `--find` は **140 → 155 MB** に悪化 → 捨てた。呼び出しのクロージャの型を揃えて単相化を 1 回にしても lib は −1.7 MB だけ。
      本文の「新しいクレートへ」はそのままでは組めない (`crates/run` → ルートの lib → … ルートの bin → `crates/run` でパッケージが輪になる)。`build.rs` の `cargo::rustc-link-arg-bins=-Wl,--no-keep-memory` は約 8 MB。
      cgroup の `memory.peak` は読めない (`systemd-run --user --scope` の scope がこの機械の cgroup 名前空間の外に出る)。
    - 気づき: (1) **親の 2 つの誤り (山はコード生成 / 14 個は経路の外) はどちらも「測らずに / たどらずに書いた」もの**で、実装担当が測って / たどって見つけた。下読みを省いた代償。
      (2) 総量は移しても減らない (段 2 の canary と同じ形): `serve` のコード生成 約 40 MB は lib に置いても bin に置いても要る。減らすには**別の rustc プロセスに分ける** = クレートを分けるしかない。
      (3) 全体チェックが 1 時間半かかった — エージェントが裏のコマンドを毎回 10 分ちょうど待った (6 回) のと、親が測るものを積みすぎた。設定だけの波は fmt / clippy / テスト / 関門 1 回に絞り、待ちは 60 秒刻みで確かめさせる。
      (4) 揺れるテストがもう 1 本 (`history_depth_test`。Phase 14 の「既知の小物」に足した)。
  - **次の段 (2026-09-19 に足した。段 6 の置き換え)**:
    6'. **ルートの lib の中身を下のクレートへ下ろして、bin を薄くする** (輪を切る形。`t1512s6b`、枝 `wave12/t1512s6b`。2026-09-19 の下読みで書き直した)。
       - **効果の範囲 (下読みの指摘)**: 床は 2 つある — 最後の段 (106〜113 MB) と `proxy-metrics-window` (107.7 + 12 ≒ 120)。この段が外すのは前者だけなので、**通る最小は 120 のまま動かない見込み**。
         100 未満へ行くには段 7 (要求の経路のクレートも 4 に) が要り、この段はその前提 (段 7 で 4 にすると最後の段は 112.9 MB に悪化するので、先に薄くしておく)。
       - **移すもの**: `src/` は `lib.rs` / `idle.rs` (620 行) / `main.rs` (599 行) の 3 本だけ。`src/lib.rs` の自前の中身は `VERSION` (:54)・`Accepted` (:65)・`Limiter` (:77)・`serve` (:122)・`Conn` (:672)・`Step` (:731)・
         `attach_origin_rtt` (:830)・`run_conn` (:1267)・私有の `pump` / `serve_one` と、17 本の `pub use` (`Upstream` は `proxy_origin` の再輸出で、ここの中身ではない)。
         `crates/` の中でルートの lib に依存しているものは 0 件 (`crates/bench` も本体を 1 行も参照しない)。`rust_http_proxy::…` の綴りを守る相手は `tests/` だけ (62 種。全部 glob の再輸出で届く)。
       - **決めたこと**: (a) **版の文字列**: `src/lib.rs:54` の `env!("PROXY_VERSION")` はルートの `build.rs:47` の `cargo:rustc-env` なので、移した先では見えずビルドが落ちる → **`git mv build.rs crates/server/build.rs`**。
         `proxy-server` の `version` は `0.1.0` (ルートと揃える、と両方の `Cargo.toml` に注。`tests/proxy_test.rs:131` がルートの `CARGO_PKG_VERSION` と比べる)。`build.rs:30` の `git rev-parse --git-path` が
         クレートの直下から解決できるか (コミットを 1 つ積んで `--version` の hash が変わるか) を 1 回確かめる。`Dockerfile:11` の `COPY Cargo.toml Cargo.lock build.rs ./` から `build.rs` を消す。
         (b) **facade**: ルートの `src/lib.rs` は冒頭のコメント + `pub use proxy_server::*;` だけ。ルートの `[dependencies]` は `proxy-server` と `proxy-run` の 2 つに減らす
         (`[dev-dependencies]` はテストが使うものを残す)。`log_*` マクロ (`crates/base/src/log.rs:457-490`、`$crate` = `proxy_base`) は glob の再輸出で通る。
         (c) **`serve` の総称の引数 (`src/lib.rs:126`) は変えない** (`&dyn Fn` にすると単相化 約 40 MB が `proxy-server` の側に出て 95 → 135 MB になる。段 6 の実測)。単相化は `proxy-run` の側に出る。
         (d) `proxy-server` / `proxy-run` の `codegen-units` は 1 のまま (要求の経路)。
       - **道筋 (コミット 2 つ、段ごとに測る)**: 1. **server**: `git mv src/lib.rs src/idle.rs build.rs` → `crates/server/` (`src/lib.rs` / `src/idle.rs` / `build.rs`)、ルートの lib を facade に、`members` に追加 →
         `mx cargo test --workspace --no-fail-fast` (`-p proxy-server` に単体テスト 5 本が同じ名前で残る) + クレートごとの RssAnon。2. **run**: `git mv src/main.rs crates/run/src/lib.rs` → `pub fn main()`、
         `rust_http_proxy::` → `proxy_server::` (約 40 か所)、ルートの `src/main.rs` は `fn main() { proxy_run::main() }` → `--help` / `-V` / `--check` / 未知のフラグの出力と終了コードを前後で `diff`、
         最後の段の cgroup、`--find`、`--lite` の費用。
       - **直値で見張っているもの**: `scripts/check-docs.sh:191-205` の (e) は必ず落ちる → README のクレートの表に 2 行、「(37 + 本体)」→「(39 + 本体)」(README の 2 か所)。パスのコメント:
         `crates/selfbench/src/lib.rs:29,221`、`scripts/ci-snapshot.sh:127`、ほか `grep -rn "src/lib.rs\|src/main.rs"` で当たる所 (TODO.md は親が直す)。`check-docs.sh` が `src/lib.rs` / `src/main.rs` を直に開いていれば道具の側のパスも直す。
         無傷のはず: `env!("CARGO_BIN_EXE_rust-http-proxy")` の 8 ファイル、`tests/` の綴り、`build-memory.sh`、`ci.yml`。ついでに `src/lib.rs:3` の doc リンク `[proxy_stats]` (存在しないクレート) を直す。
       - 受け入れ基準: **最後の段だけの cgroup が 70 MB で 3 回中 3 回通る**、`--find` の通る最小が 120 から悪化しない、`proxy-server` と `proxy-run` の RssAnon がどちらも 100 MB 未満 (表)、
         `--help` / `-V` / `--check` / 終了コードが前後で同一、全体テスト全通過 (792 本から減らない)、`--lite` のシステムコール/要求・確保/要求が不変、`mx cargo build --profile dist` が通る。

### Phase 16 (2026-09-24〜: 次の再デプロイ = `codegen-units = 4` を試す版。計器が先)

T15.99 の「次のターンの準備」(§0 の決まり) で、次の再デプロイで判定したいことに要る計器が 3 つ足りないと分かった。**T16.0 を入れてから 1 回で再デプロイする**
(中身は T15.12 段 5〜7 の `codegen-units = 4`、クレートの組み替え、T16.0)。「前」の雪像は `2026-09-24T093400Z` (T15.99 の「後」)。

- [x] **T16.0 次の判定に要る計器 3 つ (役ごとの CPU のユーザー空間、`dns_warm` の最大、平均の切り捨て。T15.16 を畳む)**
  - 目的: (i) `codegen-units = 4` が効くのはユーザー空間だけで、CPU の約 7 割はカーネル側なので、合計 (`utime + stime`) では差が埋もれる。(ii) T15.99 で「`MAX_WARM` 32 がバーストで埋まるか」が判定できなかった
    (`/history` の `dns_warm` は平均だけ、5 秒のリングは 1 時間ぶん)。(iii) `dns_warm` をはじめゲージ 9 つの平均が `u64` の割り算で 2 段切り捨てられ、下に偏る。
  - **決めたこと (2026-09-24 の下読みから。行番号は `dd3e6c6` のもの)**:
    1. **役ごとの CPU**: `crates/sysinfo/src/sysinfo/proc.rs:141` の `parse_task_stat` が utime も返すようにし `TaskSample` に `utime` を足す。`crates/metrics-window/src/profile.rs:943` 付近の前回値を (合計, user, run_delay) にし、
       役ごとの窓に `user_us`。`/profile` の `keys` の**末尾**に `user_us` (`roles` と同じ長さの配列。`run_delay_us` と同じ形)。プロセス全体は `parse_stat_cpu_split_us` を新しく足し (`parse_stat_cpu_us` は
       anomaly と `tunnel_spin_test` が使うので残す)、`keys` の末尾に `cpu_user_us` (スカラー。`summary=1` の段と `recent` にも)。**持つのは user だけ** (sys は `cpu_us − user`)。
    2. **cgroup**: `crates/sysinfo/src/sysinfo/cgroup.rs:107` の `parse_cpu_stat` で `usage_usec` / `user_usec` / `system_usec` も読む (同じファイル・同じ `path`。探し方 `find_up` は変えない)。
       `/status` の `kernel.cgroup_cpu` と `since_start` の**末尾**に `usage_usec` / `user_usec` / `system_usec`、`/history` の `kernel.keys` の末尾に `cpu_user_usec` / `cpu_system_usec` (区間の値、足し合わせで畳む)。
       `crates/metrics-types/src/kernel.rs:36-44` の `avail` の旗は 8 ビット埋まっているので **`u16` に広げて `SRC_CGROUP_USAGE`**。行が無いファイルではこの 3 欄を `null`。
    3. **`dns_warm` の最大と平均**: `/history` の `Sample` に `dns_warm_max` (`key_kinds` は `peak`)、`dns_warm_sum` と `gauge_n` (その行に畳んだ 5 秒の標本の数。どちらも `delta`)。
       `dns_warm` は `round(Σsum / Σn)`。**ほかのゲージ 8 つ** (`active` / `mem_used` / `mem_limit` / `disk_used` / `disk_limit` / `rss` / `threads` / `fds`、`history.rs:412-429`) も同じ `avg` で畳んでいるので、
       `avg` を合計と標本数から出す形にそろえる (四捨五入を 2 段かけると最悪 ±1.0 ずれるので、合計を持つ)。5 秒の標本は `sum = 値, n = 1, max = 値`。古いレコード (新しい欄が 0) は
       `gauge_n == 0` を `(値, 1)`、最大を `max(dns_warm_max, dns_warm)` で読む。**`.rrd` は版 3 のまま** (`SAMPLE_ITEMS` 83 → 86、`KEYS` 40 → 43、予備 44 → 41)。`.recent` の MAGIC も変えない。
    4. **warm の追い出しの通算**: `/status` の `dns` の末尾に `warm_evicted` (`crates/net-dns/src/dns.rs:334` 付近の、満杯で追い出す枝に原子の加算 1 つ)。**要求の経路の例外**として README に書く
       (通るのはミスで名前解決をした直後、枠が満杯のときだけ。名前解決そのもの (ms) に比べて無視できる)。
    5. **dashboard には足さない** (81,673 / 81,920 B)。判定は JSON に出ていれば足りる。
  - 道具 (最小): `scripts/snapshot-diff.py` の `HFIELDS` (:80) に 3 列、`aggregate` で最大と Σsum/Σn (無ければ今の平均)。`scripts/snapshot-summary.py` の CPU の表に「ユーザー / カーネル」の 1 行
    (`since_start.user_usec` / `system_usec` と `/profile` の `user_us` の役ごとの上位)。T15.15 (道具の 3 つの誤り) とは別 (同じ `aggregate` を触るので T15.15 はこのあと)。
  - **直値で見張っているもの (壊れるはず)**: `tests/profile_test.rs:38` (`keys` の文字列)・`:197-209` (構造体の書き方)、`crates/endpoints/src/endpoints/profile.rs:92-93`、`crates/sysinfo/src/sysinfo/proc.rs:231`、
    `crates/metrics-types/src/kernel.rs:47` (`[&str; 24]`)・`:771`、`crates/metrics-window/src/history.rs:140` (`[&str; 40]`)・`:1344` (`SAMPLE_ITEMS`)・`:1328-1365` (版 3 の往復)、`tests/history_columns_test.rs:101,103-115`。
  - 費用: 読むファイルは既に 5 秒ごとに読んでいるものだけ (`/proc/self/stat`、`/proc/self/task/*/stat`、cgroup の `cpu.stat`)。システムコールは増えない。メモリは満杯で +約 360 KB
    (`history_depth_test` の上限 3,407,872 B に対し 2,972,160 B で収まる見込み)。`/profile` の 1 行が 20〜40 B 太り、雪像の 256 KiB に入る本数が少し減る。
  - テスト: `parse_task_stat` / `parse_stat_cpu_split_us` / `parse_cpu_stat` (`user_usec` の有る版と無い版)、5 秒の値を並べた作り物で 60 秒と 3600 秒の平均が真の平均 ±0.5 に入ること、最大が残ること、
    古い形のレコード (新しい欄 0) を読み戻して値が変わらないこと、`warm_evicted` が満杯のときだけ増えること。手元のコンテナは `cpu.stat` の `user_usec` が読める (`nr_periods` は 0)。
  - 受け入れ基準: 全体テスト全通過 (792 本から減らない)、`--lite` のシステムコール/要求・確保/要求が不変 (5.023 / 10.006)、関門 180、`check-docs` 差分 0、`check-dashboard.js` 通過、
    手元で起動して `/profile` に `user_us` と `cpu_user_us`、`/status` に `user_usec` と `warm_evicted`、`/history` に `dns_warm_max` が出ること。**この機械はメモリが逼迫しているので `MX_SLOTS=1`**。
  - 結果 (2026-09-24、`2f52b71` / `317a5ed` / `721007d` / `88e496b` / `bbd5902`、取り込み `2a99e38`): 決めたこと 1〜5 のとおり入れた。`.rrd` 版 3 のまま `SAMPLE_ITEMS` 83 → 86 (予備 41)、`/history` の `keys` 40 → 43、
    `kernel.keys` 24 → 26。新しいテスト 5 本 (役ごとの user、`user_usec` の無い `cpu.stat`、cgroup の区間値、ゲージの平均が真の平均 ±0.5 に入り最大が残る、古いレコードの読み戻し) と `warm_evicted` の確かめ、
    scripts の unittest 213 → 218。**全体テスト 793 通過 / 0 失敗 / 1 ignored** (親が手元で数え直した値。実装の報告の 797 とは数え方が違う。足したテストは 5 本で、消したテストは 0)。
    `--lite`: `recvfrom` 3.000 + `sendto` 2.000 は不変、全体は 5.021〜5.042 回/要求 (要求数に関係ない固定分と `futex` の揺れ)、確保 10.006 回/要求で不変。関門 180 MB 通過、release 2,694,968 → 2,695,016 B、
    dashboard は触っていない。記録のメモリは満杯で `rings.total` 14.63 → 15.02 MB (history の標本 664 → 688 B、profile の標本 3,136 → 3,216 B)。
    **手元で起動して読んだ値** (版 `0.1.0+2a99e38`): `/profile` の `keys` の末尾 `run_delay_us, user_us, cpu_user_us`、`/status` の `kernel.cgroup_cpu` の末尾と `since_start` に `usage_usec` / `user_usec` / `system_usec`
    (手元のコンテナは cgroup がコンテナ全体なので、`since_start` にはビルドなどほかの仕事も入る。デプロイ先ではプロキシのコンテナの値)、`dns.warm_evicted` 0、`/history` の `keys` の末尾 `dns_warm_max, dns_warm_sum, gauge_n`。
    レビューは `ok` (should_fix 1 = README の `/profile` のメモリの数字の食い違い、直した)。nit: 追い出しの相手が既に表から消えた名前でも `warm_evicted` が 1 増える (実害は小さい。次に `dns.rs` を触るとき)。
    **手順の違反 1 つ**: 実装担当が手元の確かめでプロキシを自分宛てに使った (決まりでは禁止。手元の一時的なプロセスで実害なし)。全体チェックは 2 回の再起動で止まったので、最後は親が手で回した
    (fmt / clippy 警告 0 / テスト / 関門 180 / scripts 218 / dashboard / check-docs 差分 0 / 起動)。

- [x] **T16.99 締める (T16.0 と T15.12 段 5〜7 (`codegen-units = 4`) を入れた版を 47 時間走らせたあとに見る・決める・書く)**
  - 前提: 版 `0.1.0+2e57626` を 2026-09-24 12:36:19 UTC に起動 (利用者が再デプロイ。`TODO.md` への記録が漏れていたので 2026-09-26 に雪像の `uptime_secs` から起こした)。
    「前」= `status/2026-09-24T093400Z-snapshot.json` (版 `9f07de4`、起動から 126.6 時間。T15.99 の「後」)、「後」= `status/2026-09-26T114602Z-snapshot.json` (起動から 47.2 時間)
    と同じ時刻の `-{daily,healthz,slo,config,profile_res_60}.json` (直した `collect-deployed.sh` が一緒に取った)。判定表:
    `python3 scripts/snapshot-diff.py status/2026-09-24T093400Z-snapshot.json status/2026-09-26T114602Z-snapshot.json --criteria phase15 --daily status/2026-09-26T114602Z-daily.json`
  - **Phase 16 の完了の定義** (T15.99 の「次のターンの準備」の 3 つ。書かれていなかったのでここで決めた): (i) `codegen-units = 4` で**桁で**悪くなっていない
    (T15.12 段 7 の本文の基準: conn 役のコア/要求が前の 1.3 倍を超える・`/profile` のユーザー空間の段階が目に見えて太る・`cpu_throttled` が出る、のどれも無い)。
    (ii) `MAX_WARM` 32 がバーストで埋まって主要ホストが追い出されていない。(iii) 引き直しが名前ごとに 80 回/時以下 (T15.15 で直した物差し)。
  - 結果 (2026-09-26): **3 つとも満たした。`codegen-units = 4` はそのまま** (T15.12 の残りに進める)。phase15 の判定表も 6 行とも満たした。
    - (i) **差は読めないが、桁の悪化は無い**。`/profile?res=60` (どちらも 256 KiB で切れて約 5 時間ぶん) のプロセス全体は **0.0014 → 0.0013 コア**。conn 役は
      要求あたり 699 → 1,038 us (1.48 倍) だが MiB あたりは 30,783 → 17,238 us (0.56 倍) と**向きが逆**で、材料は 10 ms 刻みの 66 → 38 回ぶん・要求 944 → 366 本・
      転送 21.4 → 22.0 MiB しかない = 要求の中身の違いとぶれの中。CPU の絞りは起動から **0 / 412,474 周期**、`/events` に `cpu_throttled` / `tunnel_spin` は 0 件。
      「後」の起動からの cgroup は 374.1 秒 = 0.0022 コア (ユーザー空間 115.3 秒 = **31%**、カーネル 258.8 秒。T9.1 の「ユーザー空間は約 3 割」と同じ形)。
      **「前」の版には cgroup の `usage_usec` が無い** (T16.0 で足した欄) ので起動からの通算では比べられない。次の比較からは両側にある。
    - (ii) **取り合いは無い**。`dns_warm_max` は 5 秒 / 60 秒 / 1 時間のどの窓でも最大 **15** (上限 32)、`dns.warm_evicted` **0**。1 時間 23,404 本のバーストでも
      warm の最大は 6 (1 つの接続元が少ない相手に集中するので、名前は増えない)。
    - (iii) 名前ごとの最大 **80.0 回/時** (`http-intake.logs.us5.datadoghq.com` 3,761 回 / 47.0 時間) = **閾ちょうどで満たした**。TTL 60 秒の 3/4 おきに引き直す作りなので、
      ずっと warm な名前は物理的に 80 回/時に張り付く (超えることは無い)。引き直しの全体は 436 回/時で、CPU は上の 0.0022 コアに含まれる。
  - 気づいたこと (判定の外):
    - **`dns_slow` の anomaly が 0.09 → 0.62 件/時** (前 126 時間で 11 件、後 47 時間で 29 件)。ほとんどが「5 分で 1 回のミスが 240〜470 ms」で立っている。
      リゾルバは遅くなっていない (canary の名前解決は前後とも p50 9 / p99 14 ms)。遅いのは**たまにしか引かない名前** (`/hosts` の差分で 236 回、平均 98 ms) で、
      主要な相手のミスは warm でほぼ消えた (`discord.com` 8 回・平均 9 ms)。**安いミスが消えて高いミスだけが残った** (T15.99 の「ミス 1 回 13.3 → 33.2 ms」と同じ理由、
      平常時のミス 1 回は 107 ms まで上がった) ので、`DnsSlow` (`crates/metrics-watch/src/anomaly.rs`) の「5 分の平均が 100 ms 超」が**ミス 1 回で立つ**ようになった → T17 の候補。
    - RSS 47.0 → 54.2 MB は `heap_free` 11.9 → 21.2 MB (解放したあとも持っているヒープ) の増えで、`heap_used` 9.9 → 10.1 MB・`mmap` 14.0 → 14.4 MB・`rings_used.total` 13.25 → 13.53 MB は
      ほぼ同じ = リークではない。
    - 平常時の CONNECT 確立 p50 は 8.2 → **7.8 ms**、ミスは 0.06 回/接続 (日ごとの幅 0.00〜0.15)、`timeout` 0 件。
  - **次のターンの準備 (§0 の決まり)**: 版 `2e57626` からプロキシ本体のコードは変わっていない (`2e57626..` の差はテスト 1 本、`scripts/`、CI、文書だけ) ので、
    **いま再デプロイしても判定することが無い**。次の再デプロイは、本体を変える T17 の最初の仕事 (候補は `DnsSlow` の立ち方) が入ったとき。そのときは rustc も 1.96 → stable (1.98) に
    変わるので、(i) と同じ「桁で悪くなっていないか」をもう一度見る (今回から cgroup の `usage_usec` / `user_usec` が両側にあるので、起動からの通算で比べられる)。
    計器で足りないもの: `/profile?res=60` が 256 KiB で切れて 24 時間のうち約 5 時間しか残らない (`collect-deployed.sh --full` が追うのは雪像の部だけ)。
    **T15.12 の残り** (stable で `--find` を測り直して関門を下げる) は、`codegen-units = 4` を残すと決まったのでこの次に進める (デプロイは要らない)。

### Phase 17 (2026-09-26 に決めた: T16.99 の持ち越しと、Phase 14〜16 のやり残しを全部やる)

T16.99 のあとに候補を 22 挙げ (§4・Phase 15 の「測ってから決める」・T14.99 / T15.99 の穴・既知の小物・T16.0 のレビューの nit・T16.99 の気づき)、
**利用者の決定 (2026-09-26): 全部やる。実装は Opus (medium) に渡し、投入は利用者が手で行う** (指示文は「Phase 15 の渡し方」の型 + そのタスクの本文。worktree は `wave15/<id>` から、`mx` は `scripts/mx`)。
親 (この文書を書く側) がやるのは T17.10 / T17.17 / T17.18 / T17.19 / T17.21 と、マージ・全体チェック・この文書。**まだ着手していない** (この節を書いた時点)。
**2026-09-26 の進み**: 波 15〜17 (T17.0a〜T17.8・T17.13〜T17.16・T17.20) は親が Opus を並列に投入して main に入れた (`a82799d`。全体テスト 133 バイナリ 814 通過 / 0 失敗 / 1 ignored、clippy 警告 0、scripts 297 本、check-docs 差分 0)。T17.5 は模型だけ入れて `dns.rs` は採らない。T17.11 も入れて関門を 120 MB に下げた (`ed19678`)。push・再デプロイはまだ。

**Phase 17 の指示文 (利用者が Opus (medium) に貼る型。`<ID>` `<題>` `<N>` `<id>` を埋める。2026-09-26)**:

```
あなたは rust-http-proxy の Phase 17 のタスク <ID> (<題>) を実装する。リポジトリは /home/takano32/GitHub/rust-http-proxy。
受け入れ基準を全部満たしてから、最後に「報告」だけを返す。

## 最初に読むもの (この順で。読み終わるまで何も書かない)
1. TODO.md の §0「ゴールと前提」(守ること・作業の進め方)
2. TODO.md の「Phase 15 の渡し方 (2026-09-18 版)」の 1〜9 (`grep -n "Phase 15 の渡し方" TODO.md` の 1 つめ)
3. TODO.md の「### Phase 17」の冒頭 (制約・波・順番) と、自分のタスク <ID> の本文 (目的・変更箇所・やること・受け入れ基準)
4. 本文が挙げるファイルは、関数の全体と呼び出し元まで読む。本文の行番号は 2026-09-26 の main (71f7087) のもの。
   ずれていたら近くを grep して確かめる。本文と違う事実を見つけたら、直す前に「どこがどう違うか」を控えて報告に書く。

## 作業場所
git -C /home/takano32/GitHub/rust-http-proxy worktree add -b wave<N>/<id> /home/takano32/GitHub/rust-http-proxy-wt/<id> main
以後そこだけで作業する。main のチェックアウトは触らない。git push はしない。TODO.md は編集しない (「結果:」の文面は報告に書く)。
デプロイ先 (nagoya.sorahost.net) には触らない。status/ の雪像は読むだけ (コピーしない・コミットしない・中の IP やホスト名を報告に書かない)。
$HOME に書かない (プロキシを起動するときは HOME と XDG_CACHE_HOME を一時ディレクトリに向ける。~/.cargo と ~/.rustup は例外)。
権限を上げない (sudo / sysctl は使わない。unshare は使えない)。

## 確かめ
cargo とベンチは必ず scripts/mx 経由 (scripts/mx cargo test …)。手順: cargo fmt --all → 触ったクレートだけ
scripts/mx cargo clippy -p <crate> --all-targets -- -D warnings → 自分が書いたテストと本文が名指しした既存のテストだけを名前で回す。
cargo test --workspace は回さない (波の最後に親が 1 回)。scripts/ や README を触ったら python3 -m unittest discover -s scripts と ./scripts/check-docs.sh。
手でプロキシを起動するなら timeout 120、curl は -m 5、プロキシを自分宛てにしない。止めるのは自分が起動した PID だけ (pkill -x は使わない)。
作業ファイルは mktemp -d に <id>- で始まる名前で置き、終わったら自分のプロセスを残さない。

## 守ること
外部クレートは足さない。認証は入れない。接続元・宛先・ポートの制限を既定で入れない。要求の経路は増やさない (本文が「例外」と書いた所だけ)。
JSON は足すだけ (schema 1 のまま、欄を消さない・意味を変えない・鍵の順を変えない)。コメントは日本語、ログの文字列は英語。
README の環境変数の表は自分の行だけ触る。TODO.md §0 の「守ること」に反することはしない。

## できなかったとき
再現や計測が本文のとおりにならなかったら、別の直しを自分で考えて入れない。試したことと起きたことをそのまま報告して終わる。

## コミットと報告
1 項目 1 コミット (feat: / fix: / test: / docs: / refactor: + 日本語 1 行 + 本文)。末尾に Co-Authored-By: <自分のモデル名> <noreply@anthropic.com>。
手放す前に git merge main を 1 回 (親が TODO.md を動かしている。衝突したら直して、そのことを報告に書く)。
最後の報告に入れるもの (これだけを返す):
(1) コミットの hash と枝・worktree、(2) 前後の表 (測った単位だけ)、(3) 回したテストの名前と本数、
(4) 本文と違った事実・やり残し・気づいた問題、(5) TODO.md に貼る「結果 (2026-MM-DD、<hash>): …」の 1 段落、
(6) README に自分で足した行 (check-docs.sh は説明文を見ないので、ここに書かないと誰にも見つからない)。
```

| `<ID>` | `<N>` (波) | `<id>` | 先に main に入っていること |
|---|---|---|---|
| T17.0a | 15 | t170a | — |
| T17.0b | 15 | t170b | `CRITERIA` の既定の書き換えだけ T17.0a のあと (手放す前の `git merge main` で確かめる) |
| T17.0c | 15 | t170c | — |
| T17.1 | 15 | t171 | — |
| T17.3 | 15 | t173 | — |
| T17.4 | 15 | t174 | — |
| T17.14 | 15 | t1714 | — |
| T17.15 | 15 | t1715 | — |
| T17.5 | 16 | t175 | T17.3 (同じ `dns.rs`) |
| T17.6 | 16 | t176 | — |
| T17.13 | 16 | t1713 | 波 15 の `scripts/` 全部 |
| T17.16 | 16 | t1716 | T17.0a・T17.0b |
| T17.20 | 16 | t1720 | — |
| T17.7 | 17 | t177 | T17.6 (同じ `net.rs`) |
| T17.8 | 17 | t178 | T17.1 (`metrics-watch`) |
| T17.11 | 18 (1 本だけ、`MX_SLOTS=1`) | t1711 | T17.1・T17.7・T17.8 と、波 15〜17 の全部 |

**Phase 17 の制約 (着手前に分かっていること)**:
- **この機械は 2 コア** (`nproc` 2、11.9 GB)。§1 のレシピ (プロキシ cpu4-7 / ベンチ cpu0-3) は回せないので、**手元で CPU/要求 の A/B は取らない** (2026-09-19 の方針と同じ)。
  速さの判定はデプロイ先の前後 (T16.99 の「桁で悪くなっていないか」) だけ。§2 の loopback の表も stable では測り直さない (測れる機械が来たら)。
- **`unshare -rmnC` が使えない** (Ubuntu の AppArmor。権限を上げない決まり) ので、`scripts/deployed-like.sh` と `crates/bench` の `--only replay` は **CI (`deployed-like-snapshot`) でしか回らない**。
- rustc は stable のまま動く (1.98.1 で着手)。ビルドメモリと速さは版で少し動くので、関門は「毎回通る最小 + 余裕」で決める。
- ダッシュボードは上限まで 247 B (81,673 / 81,920 B)。この Phase では**画面に足さない** (判定は JSON と道具で)。

**波と順番** (同じファイルを触るものは同じ波に入れない。1 波 = 並列、波の間で親がマージ):
- **波 15** (道具と小さな直し。7 本並列): T17.0a・T17.0b・T17.0c・T17.1・T17.3・T17.4・T17.14・T17.15
- **波 16**: T17.5 (T17.3 のあと、同じ `dns.rs`)・T17.6・T17.13 (T17.12 を含む。波 15 の `scripts/` が入ってから)・T17.16 (T17.0a/0b のあと)・T17.20
- **波 17**: T17.7 (T17.6 のあと、同じ `net.rs`)・T17.8 (T17.1 のあと)
- 波 17 のあと: 親の全体チェック → push → **再デプロイをお願いする** (本体を変えるのは T17.1・T17.3・T17.4・T17.5・T17.6・T17.7・T17.8・T17.20。次の版は rustc 1.98)
  → 24 時間 → **T17.99**。再デプロイの直前に「前」の雪像を 1 枚 (T17.19 の決まり)。
- 24 時間の待ちの間に (デプロイ不要): **T17.11** (1 本だけ、`MX_SLOTS=1`)、親の T17.10 / T17.17 / T17.18。T17.21 は利用者の判断のあと。

- [x] **T17.0 計器と道具 (デプロイの前に。3 単位、並列)**
  - [x] **T17.0a `snapshot-diff.py --criteria phase17` (T17.9 を畳む)**
    - 目的: T16.99 の判定 (i)〜(iii) は手で引いた。次から自動で出す。`/events` の種類別 件/時 が表にあれば `dns_slow` の 7 倍は道具が見つけていた。
    - 変更箇所: `scripts/snapshot-diff.py` (`RULES` / `CRITERIA` / `judge` / `render`。`PHASE15` 731〜758 行の隣に `PHASE17`)、`scripts/test_snapshot_diff.py`、`scripts/collect-deployed.sh` は**触らない** (T17.0b が触る)。
    - やること: `RULES["phase17"]` に 7 行: (1) `_p15_watch_host` / `_p15_refresh_rate` / `_p15_miss_band` / `_p15_timeout` はそのまま再利用、(2) **conn 役のコア/要求が前の 1.3 倍以下** (材料は `--profile FILE` = `/profile?res=60` の JSON。無ければ雪像の `/profile` 部 (38 分) で「参考」と書く)、
      (3) **`dns_warm_max` の最大が 32 未満かつ `dns.warm_evicted` が 0** (`aggregate` の `dns_warm_max` と `/status`)、(4) **`/events` の種類別 件/時** (`kind == "anomaly"` の `text` の先頭 `<kind>:` で分け、`cleared:` は数えない。起動からの時間で割る。閾は `dns_slow` 0.1 件/時、ほかは表示だけ)、
      (5) **cgroup の起動からの CPU** (`since_start.usage_usec` を `uptime_secs` で割ったコア数と user の割合。前の雪像にも欄があれば前後を並べる、無ければ「前の版に欄が無い」)。
      `--profile FILE` を足す (`--daily` と同じ形。`build` で `b["profile_res_60"]` に入れる)。`snapshot-diff.py --criteria phase16` は作らない (T16.99 は手で済ませた)。
    - 受け入れ基準: `scripts/testdata/snapshot-{a,b}.json` に作り物の欄を足したテストで 7 行とも `MET` / `MISSED` / `UNKNOWN` が出ること。`status/2026-09-24T093400Z` → `2026-09-26T114602Z` の 2 枚 (+ `--profile status/2026-09-26T114602Z-profile_res_60.json`) で、T16.99 の `結果:` と同じ数字 (0.0013 コア、`dns_warm_max` 15、`warm_evicted` 0、`dns_slow` 0.62 件/時) が出ること。`python3 -m unittest discover -s scripts` 全通過。
    - 結果 (2026-09-26、`a6a5e1d`): `scripts/snapshot-diff.py --criteria phase17` を足した (8 行。本文の「7 行」は (1) の 4 行 + (2)〜(5) の 4 行で 8)。前の 4 行は phase15 の関数をそのまま使う。後ろの 4 行: conn 役の CPU/要求 が前の 1.3 倍以下 (`--profile FILE`、前は新しく足した `--profile-before FILE`、プロセス全体のコア数も並べる)・`dns_warm_max` の最大 < 32 かつ `warm_evicted` = 0・`/events` の anomaly の種類別 件/時 (`cleared:` と起動より前は数えない、閾は `dns_slow` 0.1)・cgroup の起動からの user / sys が前の 10 倍以下 (前に欄が無ければ「判定できず」)。`2026-09-24T093400Z` → `2026-09-26T114602Z` + `--profile` で: プロセス全体 0.0013 コア、`dns_warm_max` 15、`warm_evicted` 0、`dns_slow` 0.61 件/時 (29 件 ÷ 47.16 時間。T16.99 の 0.62 は 47 時間で割った値)、cgroup 0.0022 コア・user 31% (判定できず)、conn 役は 556 → 1,038 us/要求 (参考)、`--profile-before` で 738 → 1,038 = 1.41 倍 (T16.99 の 699 とは合わない)。テストは `Criteria17` 20 本 + `Deployed17` 1 本、scripts の unittest 222 → 243 本、`check-docs` 差分 0。
  - [x] **T17.0b `collect-deployed.sh` が `/profile?res=60` を 24 時間ぶん取る**
    - 目的: T16.99 の「計器で足りないもの」。いまの `-profile_res_60.json` は 256 KiB で切れて 24 時間のうち約 5 時間 (308 標本)。`/profile` は `offset=` を持つ (`crates/endpoints/src/endpoints/profile.rs:21`) のでプロキシは触らない。
    - 変更箇所: `scripts/collect-deployed.sh` の「1a. 雪像に入らない口」と `--full` の続きを追う枝、README の `collect-deployed.sh` の説明 (1 行)。
    - やること: `profile_res_60` は `truncated` なら `next_offset` が `null` になるまで `/profile?res=60&offset=` で追い、**1 つの JSON に繋いで** `<時刻>-profile_res_60.json` に置く (`samples` を連結、`truncated` を `false` に。`keys` / `roles` は 1 枚目のもの)。`--full` を付けなくても追う (これは 1 日 1 回の口で、重い口は同時 1 本なので順に引く)。`MAX_PAGES` は共通。
      `CRITERIA` の既定を `phase15` → `phase17` にし、取れた `-profile_res_60.json` を `snapshot-diff.py --profile` に渡す (**T17.0a が main に入ってから**。手放す前の `git merge main` で確かめる)。
    - 受け入れ基準: 手元のプロキシ (`HOME` と `XDG_CACHE_HOME` は一時ディレクトリ、`timeout 120`) に回して `-profile_res_60.json` の `samples` が 1 枚に繋がり `truncated` が `false`。デプロイ先には**回さない** (親が回す)。README の説明が実物と合う。
    - 結果 (2026-09-26、ff04896 / 1a4dd4e / 26e82f6 / 6f06b50): `collect-deployed.sh` は `-profile_res_60.json` を、`--full` が無くても `next_offset` が `null` になるまで `/profile?res=60&offset=` で順に引き、`samples` を古い順に 1 つに繋いで置く。`truncated` は `false`、`shown` は繋いだ本数で、`keys` / `roles` は 1 枚目のまま。頁の間に 1 本増えた重なりは `t` で落とす。上限は `MAX_PAGES` (1 枚目を含む) で、途中で取れなければ残りを指したままにする。雪像に入らない口が busy (503) の JSON を取れた扱いにしていたのも直した。判定表の既定は `CRITERIA=phase17` にし (T17.0a のあと)、繋いだファイルを `snapshot-diff.py --profile` に、前回の雪像の隣に同じ時刻の `-profile_res_60.json` があれば `--profile-before` に渡す (`--from-server` の日には隣が無いので、雪像の `/profile` の部で参考になる)。確かめは、頁の切り方を写した偽のサーバーでの 6 本 (`scripts/test_collect_deployed.py`。1,440 標本 → 5 枚 → 1 つ、busy の引き直し、`MAX_PAGES`、`--profile` と `--profile-before` の受け渡し) と、手元のプロキシ (1 枚に収まり `truncated` は `false`)。デプロイ先には回していない。
  - [x] **T17.0c `snapshot-summary.py` に `/events` の種類別 件/時 と、接続元の見張り**
    - 目的: §4 の `/readers` の項の結論 — 走査は forward と CONNECT で来るので `/readers` には出ない。認証なしプロキシで実際に起きる危険は乱用なので、1 枚の要約で見えるように。
    - 変更箇所: `scripts/snapshot-summary.py`、`scripts/test_snapshot_summary.py`、README の要約の説明。
    - やること: (1) `/events` の `anomaly` を種類別に「起動からの件/時」で 1 表 (`cleared:` は数えない)。(2) 接続元の表に **新しく現れた接続元** (`--prev` があれば差分)、**宛先が IP リテラルの要求**、**非標準ポート (443 / 80 以外) の CONNECT** の件数を出す (材料は `/clients` の差分と `/recent` の `target`。`/recent` は 256 KiB で切れているので「窓の中で」と書く)。ホスト名と IP は**そのまま出す** (要約は手元に置くもの。匿名化は T17.16)。
    - 受け入れ基準: `testdata` の作り物で表が出る単体テスト。`status/2026-09-26T114602Z-snapshot.json` で `dns_slow` 0.62 件/時 と、走査とみられる接続元が出ること。`python3 -m unittest discover -s scripts` 全通過。
    - 結果 (2026-09-26、`5f9fa8f`): `scripts/snapshot-summary.py` の最後に 2 つの表を足した。(1) `/events` の anomaly を種類別に「起動からの件/時」(3 桁)。`cleared:` は数えず、状態ファイルから読み戻した起動 (`taken_at − uptime_secs`) より前の出来事は外す。リングが満杯か応答が切れていれば「起動直後のぶんが落ちているかもしれない」と書く。(2) 接続元の見張り。`/clients` の行ごとに、要求 (`--prev` があれば差分)・宛先の種類・IP リテラル宛てと 443 / 80 以外の CONNECT (`/clients` の起動からの通算 / `/recent` の窓の中)・初めて見た・最後・UA を出す。「新」は `--prev` があれば前の `/clients` に居ないか `first_seen` が前の取得より後、無ければ `first_seen` が起動より後。続けて `/events` の `new_client` (窓の外も印を付けて 5 件) と、`readers` に居て `/clients` に居ない接続元 (内部の口だけを引いた走査) を出す。IP とホスト名はそのまま出す。`status/2026-09-26T114602Z-snapshot.json` では `dns_slow` 29 件 / 47.2 時間 = **0.615 件/時** (T16.99 の 0.62 は時間を 47 に丸めた値)、`connect_p95` 0.148 件/時、`cleared` 36 件は数えず、起動より前の 61 件は外した。`/clients` の差分・IP リテラル・非標準ポートはどれも 0 件で、走査の手がかりは `new_client` の ForestEngine のクローラ (2026-09-18、窓の外) と、`GET /` だけを引いた接続元 3 件 (2026-09-25、`readers`) に出た。テストは `test_snapshot_summary.py` に 10 本 (`Anomalies` 4・`ClientWatch` 6)、`python3 -m unittest discover -s scripts` 232 本 OK、`check-docs.sh` 差分 0。 親の注記: 気づき: §4 の「走査は /readers に出ない」とは逆の例が出た。

- [x] **T17.1 `dns_slow` がミス 1 回で立つのを直す (T17.2 を畳む: `dns_miss_rate` の閾 0.40 → 0.20)**
  - 目的: T16.99: `dns_slow` が 0.09 → 0.62 件/時 (前 126 時間で 11 件、後 47 時間で 29 件)。立った 29 件はほぼ全部「5 分に 1 回のミスが 240〜470 ms」。リゾルバは不変 (canary の名前解決 p50 9 / p99 14 ms)。
    安いミスが warm で消えて高いミス (たまにしか引かない名前、平均 98 ms) だけ残ったので、平均が 1 回で閾を超える。`dns_miss_rate` (T15.0 (9)) は閾 0.40 で、実測 0.05 の 8 倍 = 0.30 に戻っても立たない。
  - 変更箇所: `crates/metrics-watch/src/anomaly.rs` — `DNS_MISS_MS` (71 行)、`DNS_MISS_RATE` (94 行)、判定 (487 行 `w5.dns_miss_avg_ms() >= DNS_MISS_MS`)、文面 (726・847 行)、module doc の表 (18 行)、テスト (1113 行付近と 1581〜1607 行)。README の `/events` の規則の表。
  - やること: `connect_p95` にある最小本数 (`CONNECT_MIN_SAMPLES` 20、60〜69 行の考え方) に倣い、**`DNS_MIN_MISSES` 3** (5 分に 3 回以上) を足す。あわせて **canary の名前解決 (`canary.dns_ms` の直近 1 時間の中央値) を基準線**にし、
    `avg >= max(100 ms, 3 × 基準線)` で立てる (canary が無い (`mode` off) ときは 100 ms だけ)。`dns_miss_rate` は 0.20 に。文面に「N misses」が既にあるので形は変えない。
  - 受け入れ基準: 単体テスト 3 本 (1 回 400 ms では立たない、3 回 400 ms で立つ、canary 200 ms のときは 500 ms でも立たない)。`scripts/mx cargo test -p proxy-metrics-watch anomaly` 通過。`./scripts/check-docs.sh` 差分 0。要求の経路は触らない (`--lite` の費用は不変のはず。親が波の最後に測る)。
  - 結果 (2026-09-26、5039b20 / d167909): `dns_slow` に `DNS_MIN_MISSES` 3 (5 分に 3 回以上) を足した。閾は max(100 ms, 3 × canary の名前解決の中央値 (直近 1 時間、`canaryhist::dns_p50_ms`)) にした (canary が off か窓に 0 行なら 100 ms)。基準線は `Counters::canary_dns_p50_ms` で渡す。説明の形は変えず、threshold の値がその周期の閾になる。`dns_miss_rate` の閾は 0.40 → 0.20 にし、解除の閾は 0.25 → 0.10 にした (本文の外。0.25 のままだと閾より上になるため。閾の半分で `cpu_throttled` と同じ比)。単体テストを 3 本足し (1 回 400 ms では立たない / 3 回 400 ms で立つ / canary 200 ms のとき 500 ms は立たず 700 ms で threshold 600 ms)、canaryhist の中央値のテストも 1 本足した。`scripts/mx cargo test -p proxy-metrics-watch anomaly` は 28 本通過し、`check-docs.sh` の差分は 0。要求の経路と JSON は変えていない。効き目 (`dns_slow` の件/時が 0.62 → 0.1 未満) は、再デプロイのあと T17.99 で見る。 親の注記: 本文との違い: 解除の閾 0.25→0.10 は本文の外。行番号 726/847/1113/1581 → 729/850/1116/1585。

- [x] **T17.3 `warm_evicted` の nit (追い出す相手がもう表に無くても 1 増える)**
  - 目的: T16.0 のレビューの nit。実害は小さいが、T17.0a が `warm_evicted == 0` を判定に使うので、誤って増えない形にしておく。
  - 変更箇所: `crates/net-dns/src/dns.rs` の満杯で追い出す枝 (`grep -n warm_evicted`。T16.0 の本文では `dns.rs:334` 付近)。
  - やること: 表から実際に消したときだけ加算する。1 コミット。
  - 受け入れ基準: 既存の `warm_evicted` のテストが通り、「相手が既に消えている」作り物で増えないテストを 1 本。`scripts/mx cargo test -p proxy-net-dns warm`。
  - 結果 (2026-09-26、6f91998): 加算を「表から実際に外したときだけ」にする直しは T16.0 のレビューの直し (bbd5902) で既に入っていた (`dns.rs:349-352`) ので、コードは変えずにテストを 1 本足した (`warm_evicted_does_not_count_a_victim_already_gone_from_the_table`: 枠を 32 件で埋め、1 件を表からだけ消してから 33 件目を warm にしても `warm_evicted` は 0 のまま、warm は 32 件)。bbd5902 の前の形に一時的に戻すと、このテストは 1 ≠ 0 で落ちることを確かめた。`scripts/mx cargo test -p proxy-net-dns warm` 6 本通過、clippy (`-p proxy-net-dns --all-targets -D warnings`) 警告 0。T17.0a の `warm_evicted == 0` の判定はこのまま使える。

- [x] **T17.4 forward のエラー経路で `client_read` が 0 のまま落ちる**
  - 目的: T15.0 単位 1 の気づき。`crates/http/src/http/mod.rs:698-703` がオリジンを掴めなかったときに `ctx.detail.stages` ではなく `shared.stages` を渡している。forward は要求の 1% なので急がなかったもの。
  - やること: 1 行の直し + そのときの個票で `client_read` が 0 でないテスト 1 本 (`tests/` の forward のエラーのテストの隣)。
  - 受け入れ基準: `scripts/mx cargo test -p proxy-http` と、足したテストが通る。
  - 結果 (2026-09-26、f2fadfc): `crates/http/src/http/mod.rs` の forward でオリジンを掴めなかった枝 (`acquire_origin` の `Err`) が、`origin_detail` に `shared.stages` を渡していた。これは本体が測った `queue` だけで、入口で測った `client_read` は `ctx.detail.stages` にしか無い。成功の枝と同じく `ctx.detail.stages` を渡すように 1 行直した。テスト `tests/stages_test.rs` の `a_failed_forward_keeps_its_client_read_stage` を足した。閉じたポートへ要求行と `Host` の間を 40 ms 空けた forward を送り、`/recent` の http の 1 件に `client_read` が 30 以上で出ることを見る。直す前は `"ms":{"dns":0,"connect":0}` で落ち、直した後は通る。テストは本文の「forward のエラーのテストの隣 (`recent_test.rs`)」ではなく、段の道具と `profile` の旗がある `stages_test.rs` に置いた。`scripts/mx cargo test -p proxy-http` は 15 本、`--test stages_test` は 2 本とも通過、clippy (`proxy-http`・`rust-http-proxy`) は警告 0。要求の経路の時計・確保は不変。

- [x] **T17.5 `promote` から `idle < window` を外す (先に再生の模型で測る。採らないこともある)**
  - 目的: Phase 15 の「測ってから決める」の筆頭。T15.4 の前の再生でミス 57 → 45 (−21%)、引き直し +5%。T15.4 (窓 3,600 秒) のあとに測り直してから、が条件だった。
    T15.4 のときの再生の模型は scratchpad にしか無く残っていない (§0 の決まりに反する) ので、**模型を `scripts/` に入れる**のがこのタスクの半分。
  - 変更箇所: 新規 `scripts/dns-replay.py` (標準ライブラリのみ)、`scripts/test_dns_replay.py`、採るなら `crates/net-dns/src/dns.rs:773` (`promote = !e.warm && !window.is_zero() && idle < window && !e.addrs.is_empty()`) とその doc、README の名前解決の説明。
  - やること: (1) 模型: 雪像の `/recent` (`at`・`target`) と `/hosts_series` から**名前の到着列**を作り、`dns.rs` の規則 (TTL 60、`WARM` 3,600、`MAX_WARM` 32、引き直しは TTL の 3/4、`promote` の条件) を写した状態機械で「ミス / 引き直し / warm の件数」を数える。
    規則は関数 1 つで差し替えられる形にし、**いまの規則で実機の値 (T16.99: 平常時 0.06 回/接続、引き直し 436 回/時) に ±30% で合うこと**を先に確かめる (合わなければ模型が間違いなので、そこで止めて報告)。
    (2) `idle < window` を外した規則で同じ列を回し、ミスと引き直しの差を表にする。(3) **ミスが −15% 以上、引き直しが +10% 以内**なら `dns.rs` を直す (1 行 + doc + 既存テストの期待値)。届かなければコードは触らず、表だけ報告 (親が §4 に 1 行)。
  - 受け入れ基準: `scripts/testdata` の作り物 (`replay-hosts.json` の形) で模型の単体テスト。`status/2026-09-26T114602Z-snapshot.json` での (1) の一致。採ったなら `scripts/mx cargo test -p proxy-net-dns` 通過。**雪像は `status/` から読むだけ** (コピーしない、コミットしない)。
  - 結果 (2026-09-26、`a55371d`): **採らない (模型は入れた)**。`scripts/dns-replay.py` を入れた。雪像 1 枚の `/recent` と `/connections` を、閉じた時刻の最小から撮った時刻までの「全部そろっている区間」の到着列にし、その前を `/hosts_series` の助走にして、`dns.rs` の規則 (TTL 60・窓 3,600・`MAX_WARM` 32・3/4 TTL の引き直し・先回り・`promote`) を写した状態機械で回す。`promote` の条件は `RULES` の関数 1 つで差し替えられる。`status/2026-09-26T114602Z-snapshot.json` (区間 6.53 時間、到着 558 件) で、いまの規則はミス 0.075 回/到着 (同じ区間の実機は 0.063、物差しの 0.06 に対し +25%)、引き直し 481 回/時 (436 に対し +10%) で、±30% に合った。`idle < window` を外すとミス 42 → 37 (−11.9%)、引き直し 3,143 → 3,613 (+15.0%) で、目安の「−15% 以上かつ +10% 以内」に両方の軸で届かない。これは前から居た名前を置いた上限側の値で、置かない下限側は −2.4% / +11.3%。T15.4 の前の再生の −21% は、窓 3,600 秒でほぼ食われていた。`dns.rs` は触っていない。テストは `scripts/test_dns_replay.py` 13 本、`scripts` 全体 267 本、`check-docs.sh` 差分 0。§4 に 1 行: 「`promote` から `idle < window` を外す — 窓 3,600 秒のあとの再生でミス −12%・引き直し +15% (T17.5)。採らない」。(気づき: `warm_promote` は次の予定を「上げた時刻 + 45 秒」に置くので齢 60〜75 秒が期限切れになりうる。実機の warm_stale 10 件の一部かも)

- [x] **T17.6 `STAGGER` 250 ms を設定で触れるようにする (`PROXY_HE_STAGGER_MS`)**
  - 目的: Phase 15 の一覧。効きは p99 で約 2 ms で根本は「最初の候補に SYN が返らない」だが、設定で触れるようにするのは安い (2026-09-18 の結論)。**既定は 250 のまま**。
  - 変更箇所: `crates/net-conn/src/net.rs:30` (`const STAGGER`)、`crates/config` (環境変数の表と `/config`)、`--check`、README の環境変数の表 (自分の行だけ)、`tests/config_test.rs`。
  - やること: 起動時に 1 回読む (再読込は要らない。`PROXY_MAX_THREADS` と同じ扱い)。範囲 10〜2,000 ms、範囲外は既定に戻して warn 1 行。要求の経路に読み取りを増やさない (起動時に `OnceLock` 等へ)。
  - 受け入れ基準: `/config` に `PROXY_HE_STAGGER_MS` が `default` で出る。`scripts/mx cargo test --test config_test` と `-p proxy-net-conn`。`./scripts/check-docs.sh` 差分 0。
  - 結果 (2026-09-26、4a38c15 / c6033b5 / e69875e): `PROXY_HE_STAGGER_MS` を足した (既定 250 ms、範囲 10〜2,000 ms は RFC 8305 §5 の範囲)。起動時に 1 回 `net::set_stagger` へ渡し、要求の経路は `AtomicU64` の Relaxed の load 1 回だけ (`connect_candidates` のループの外で読む)。範囲外と数でない値は `WARN` を 1 行出して既定に戻す (出どころは `default`)。`.env` の再読込では当てず、`restart_required` に出す。`/config` と `--check` では `PROXY_CONNECT_TIMEOUT_SECS` の次に出る。単体テストは範囲の境目と、黒穴の IPv6 + 生きている IPv4 で間隔 20 ms にすると 20〜250 ms で確立すること。結合テストは `--check` で `.env` の 100 が `env_file` で効き、5 と 5000 が warn + 250 に戻ること。`-p proxy-net-conn` 11 本、`--test config_test` 4 本、`-p proxy-config he_stagger` 1 本が通過。`check-docs.sh` 差分 0 件。本文との違い: `PROXY_MAX_THREADS` は実際には再読込で当たるので「同じ扱い」は「起動時に 1 回」と読んだ。reload と run も触った。(揺れ: 既存の `happy_eyeballs_skips_unreachable_first_candidate` の「2 回目 < 50 ms」が混んだ機械で 1 回落ちた)

- [x] **T17.7 IPv6 の探り (600 秒に 1 回) を利用者の経路から外して canary に任せる**
  - 目的: Phase 15 の一覧。`attempts` 12〜14 / `wins` 0、canary の `ipv6_connect_ms` は全部 `null` (IPv6 が無い網) なのに、利用者の CONNECT が 600 秒に 1 回 IPv6 を先頭に置いて `STAGGER` を払う。損失は 36.6 時間で約 3 秒と小さいが、利用者の経路から探りを消すのは筋がよい。
  - 変更箇所: `crates/net-conn/src/net.rs` (`IPV6_PROBE_SECS` 38 行、`v4_first` の判定 283 行付近、探りの予約 370 行)、`crates/metrics-watch/src/canary.rs` (canary が IPv6 を 1 本試す所)、README の IPv6 の説明。
  - やること: canary が動いているとき (`mode` が off でない) は利用者の経路で探らず、**canary の `ipv6_connect_ms` が数値になったら `v4_first` を解く**。canary が off のときは今までどおり 600 秒に 1 回。`/status` の `ipv6` の欄は足すだけ (`probe_by` = `"canary"` / `"request"`)。
  - 受け入れ基準: 単体テスト (canary あり → 探らない、canary が IPv6 成功 → 解ける、canary off → 従来)。`scripts/mx cargo test -p proxy-net-conn ipv6` と `-p proxy-metrics-watch canary`。**T17.6 が main に入ってから** (同じ `net.rs`)。
  - 結果 (2026-09-26、77a7f23): 変更は net.rs と canary.rs。canary の IPv6 側の 1 本 (T14.37) が、試した結果を `net::note_canary_ipv6` で下の層に渡すようにした。直近 600 秒に canary が IPv6 を試していれば、`v4_first` の間でも利用者の CONNECT は 600 秒に 1 回の探り (IPv6 を先頭にして stagger を払う) をしない。探りの予約時刻は動かさず、`ipv6.attempts` も増えない。canary の 1 本が繋がったら `v4_first` を解く (利用者の要求で 1 度勝ったときと同じく以後戻らない。勝敗の数には足さない)。canary が IPv6 を見ていないときは従来どおり 600 秒に 1 回、利用者の経路で探る (off、`PROXY_CANARY_IPV6=off`、`--lite`・`PROXY_STATS_PERSIST=off`、宛先に AAAA が無い、最後の 1 本から 600 秒)。本文の「mode が off でない」は、実際に試したかで判定する形にした。`/status` の `ipv6` の末尾に `probe_by` (`"canary"` / `"request"`) を足した (足すだけ)。要求の経路で増えるのは `v4_first` のときだけ原子の load 1 つ。テストは新しく 4 本: `proxy-net-conn` の `ipv6_probe_is_left_to_the_canary_while_it_probes` / `ipv6_canary_success_lifts_v4_first` / `ipv6_probe_stays_on_requests_without_the_canary`、`proxy-metrics-watch` の `the_canary_ipv6_side_takes_over_the_ipv6_probe`。既存の `a_probe_measures_a_real_connect_and_a_refused_one` は機械が混むと `dns_ms == 0` の判定で稀に落ちる (main でも同じ)。デプロイ後の確かめ: `/status` の `ipv6.probe_by` が `"canary"` で、`attempts` が増えないこと (T17.99 の (e))。

- [x] **T17.8 リングの確保を起動時にまとめて触って RSS の天井を先に見せる**
  - 目的: §4 の `malloc_trim` の項の結論 (「やるならこちら」)。T16.99 でも RSS 47 → 54 MB の増分は `heap_free` 11.9 → 21.2 MB で、毎回「リークではない」と読み解いている。起動直後から天井に近ければ「増えた」がそのまま信号になる。**速さは変わらない**。
  - 変更箇所: 各リングを作る所 (`crates/metrics-window` の `window.rs` / `profile.rs`、`crates/metrics-recent/src/recent.rs`、`hostseries` など。`/status` の `memory.rings` の内訳を出している所が一覧になっている)。README の `memory` の説明。
  - やること: 容量ぶんを起動時に `with_capacity` + 1 ページごとに 1 バイト触る (Linux のみ、`--lite` では**しない**: `--lite` は「使わないときに一切コストがかからない」の対象)。起動の時間が増えるなら (見込み数 ms) 報告に書く。`/status` の `memory` に `rings_touched` (bool) を足すだけ。
  - 受け入れ基準: 手元で起動直後の RSS と、`/history` を満杯にしたあと (`history_depth_test` の作り方) の RSS の差が **1 MB 未満**になること (前後の表)。`scripts/mx cargo test --test history_depth_test` は上限 3,407,872 B のまま通ること (**起動直後に触るぶんはテストの「差」に入らない位置で測る**か、テストの前提を直して報告)。`--lite` の RSS は不変。
  - 結果 (2026-09-26、6443182): 起動時に記録のリングの置き場を満杯のぶん `reserve_exact` し、1 ページに 1 バイトずつ触る (`proxy-base` の `prefault`、`Metrics::prefault_rings`)。呼ぶのは Linux の既定のプロファイルの `main` だけで、`--lite` では呼ばない。触るのは history (`PROXY_STATS_PERSIST=off` では触らない) と /profile の窓、recent / errors / bursts / events / log の固定部 (`PROXY_RECORDS=off` では触らない)。trace / hostseries / quantiles (合わせて約 0.7 MB) は使い始めに確保する作りのまま触らない。`/status` の `memory` の末尾に `rings_touched` (bool) を足した。手元の起動直後の VmRSS は 27.2 → 39.9 MB で、13,239 KiB を 4.4 ms で触る。`--lite` は 21.1 → 21.2 MB でぶれの中。history と /profile を 1 日ぶん満杯にしたときの RSS の増分は、触らない場合の +11.8〜13.1 MiB が +36 KiB になった (`tests/ring_prefault_test.rs`、1 MiB 未満を検査)。件数は増えないので `rings_used` は変わらず、`history_depth_test` は上限 3,407,872 B のまま通る。デプロイ先での 1 時間後と 24 時間後の差 (T17.99 (d)) はまだ見ていない。

- [x] **T17.9 (T17.0a に畳んだ) `_p15_conn_cores` の材料を `--profile FILE` に**

- [x] **T17.10 CI の `build-memory-find` を stable で回す (親)**
  - やること: `gh workflow run ci.yml --ref main -f ladder="95 100 105 110 120"` を 1 回。手元 (aarch64、2 コア) の「毎回落ちる最大 100 / 毎回通る最小 105」と CI (x86_64) の値を T15.12 の `結果:` に並べる。月曜の cron でも回るので急がない。
  - 受け入れ基準: CI の値が T15.12 に書いてあること。
  - 結果 (2026-09-26、run 36249911685): ladder は関門が 120 になったので本文の `95 100 105 110 120` ではなく `80 85 90 95 100 110 120` にした。CI (x86_64) は 95 まで落ち 100 で通る、手元 (aarch64) は 85 まで落ち 90 で通る。T15.12 の `結果:` に並べた。

- [x] **T17.11 T15.12 段 8: 上位のクレートを割って通る最小を 100 MB 未満に → 関門 120 (1 本だけ、`MX_SLOTS=1`、24 時間の待ちの間に)**
  - 目的: T15.12 の残り。stable 1.98 で 100 は 2/2 落ち、105 は 2/2 通る。目安 (通る最小 100 未満) に届いていない。
  - 変更箇所: `proxy-metrics-watch` 92.3 MB → `proxy-metrics-window` 82.9 → `proxy-endpoints` 79.9 → `proxy-server` 79.6 (`scripts/build-memory.sh` の冒頭の表)。手本は `6967236` (`git mv` + 再輸出、中身は 1 バイトも変えない) と `13fa546`。
  - やること: 上から 1 つずつ割り、割るたびに `scripts/mx bash -c 'cargo clean --release && scripts/build-memory.sh --find 90 95 100 105 110'` を **2 回**と RssAnon の表。**毎回通る最小が 100 未満**になったら段 4 (関門を 120 に下げる書き換え。書き換え先は T15.12 の 4 の一覧、`grep -l "180 MB" crates/*/src/lib.rs` の 30 本を含む) まで。届かなければ表だけ残す。
    **T17.1 と T17.7 (`metrics-watch`)、T17.8 (`metrics-*`) が main に入ってから**始める (delete / modify の衝突)。
  - 受け入れ基準: 割ったクレートの RssAnon が全部 100 MB 未満の表。全体テスト全通過 (本数が減らない)。`--lite` の費用が不変。関門を下げたなら `scripts/build-memory.sh 120` が手元で 2/2 通り、CI の `Build memory` も通る。
  - 結果 (2026-09-26、`9269394` → `a25f2cd` → `0633f7d`、取り込み `ed19678`): **段 8 で上位の 2 クレートを割り、手元の通る最小が 105 → 90 MB になった (目安の 100 MB 未満)。段 4 として関門を 180 → 120 MB に下げた。** (1) `proxy-metrics-watch` 95.4 MB から `daily` / `slo` / `snapshots` を新しいクレート `proxy-metrics-slo` へ `git mv` した → watch 72.4 / slo 63.1 MB、`--find` は 2 回とも 100 MB で通る (95 は window で落ちる)。(2) `proxy-metrics-window` 91.9 MB から `window` / `profile` を新しいクレート `proxy-metrics-profile` へ `git mv` した → window 67.7 / profile 69.2 MB、`--find 90…110` は 2 回とも 90 MB で通り、`--find 75 80 85 90` は 2 回とも 85 まで落ちて 90 で通る (**毎回落ちる最大 85 / 毎回通る最小 90**)。いまの最大は `proxy-endpoints` 82.5 > `proxy-server` 81.5 > `proxy-metrics-recent` 76.9 MB。中身は 1 バイトも変えていない (rename 100%、上の層が今までの名前で出し直す)。`scripts/build-memory.sh 120` は `cargo clean` から 2/2 通った。書き換えたのは `build-memory.sh` の既定と表、CI の `Build memory` と `build-memory-find` の ladder の既定 (`80 85 90 95 100 105 110 120`)、`crates/*/src/lib.rs` 38 本 (本文の 30 本 + 段 1〜3 で増えた分 + 新しい 2 本) と `endpoints-core/src/core.rs`、`crates/server/src/lib.rs`、`Cargo.toml`、README。着手前の main は本文の表より重かった (watch 95.4 / window 91.1。T17.8 などの後)。クレートは 39 → 41 + 本体、全体テスト 137 バイナリ 814 通過 / 1 ignored (1 回目は `proxy_test` の `request_body_on_a_reused_connection` が負荷の下で 1 回 502、単独 5/5 と 2 回目の全体では通過。既知の小物に足す)、clippy 警告 0、check-docs 差分 0、scripts 297 本 OK。release のバイナリは 2,760,504 B で main と同じ (`.text` +9.6 KB)。`--lite` の forward のシステムコールは 5.036 / 5.040 → 5.033 / 5.035 回/要求。移したファイルの doc リンク `[crate::anomaly::check]` (slo.rs)・`[crate::history…]` (profile.rs) は新しいクレートから解決しない (中身を変えない決まりで残した。rustdoc の警告だけ)。§0・§1・§2 の 180 MB は親が 120 に直した。**CI の `Build memory` (120 MB) は push 後に確かめること。**

- [x] **T17.12 (T17.13 に畳んだ) `ubuntu-latest` の Ubuntu 26 移行 (2026-10-19) への備え**

- [x] **T17.13 CI に `shellcheck` を足し、`ubuntu-latest` の移行に備える**
  - 目的: `scripts/*.sh` に `# shellcheck disable=` が 5 か所 = 以前は掛けていたが CI に無い。2026-09-26 に書いた `collect-deployed.sh` と `mx` も見てもらう。`ubuntu-latest` は 2026-10-19 に Ubuntu 26 へ移り、ビルドメモリの関門の数字が動きうる。
  - 変更箇所: `.github/workflows/ci.yml` (check ジョブに `apt-get install -y shellcheck` + `shellcheck scripts/*.sh scripts/mx`)、指摘が出た `scripts/*.sh` の直し (**動きは変えない**)。
  - やること: shellcheck の指摘を全部消す (disable で黙らせるのは理由をコメントに書けるものだけ)。`Build memory` と `build-memory-find` のジョブは **`ubuntu-24.04` に固定** (関門の数字の出どころを動かさない。`check` の残りと `deployed-like-snapshot` は `ubuntu-latest` のまま)。README / TODO の「CI の機械」の書き方は親が直す。
  - 受け入れ基準: 手元で `shellcheck` は入れられない (apt は使わない決まり) ので、**CI で通ること** (親が push して見る)。`bash -n` と、`scripts/collect-deployed.sh` / `scripts/mx` の手元の動きが前後で同じ (T17.15 のテストが通る)。波 15 の `scripts/` の変更が main に入ってから。
  - 結果 (2026-09-26、5bca088): `shellcheck scripts/*.sh scripts/mx` の指摘を 0.9.0 で 52 → 0、0.10.0 で 52 → 0、0.11.0 で 42 → 0 にした (手元は静的バイナリをスクラッチパッドで。apt は使っていない)。直しは動きを変えない形だけ (`cd … || exit 1`、`A && ok || ng` を `if` に、使わない変数を `_` に)。理由を書ける所だけ disable にした (trap から呼ぶ関数、わざと先に割る段数、collect-deployed.sh の 2 か所)。SC2016 は `scripts/.shellcheckrc` に理由を書いて切った。CI の check に `shellcheck` の段を足した (像に無ければ apt-get)。Build memory は check の中の 1 段だったので別ジョブ `build-memory` に出し、`build-memory-find` と一緒に `ubuntu-24.04` に固定した (check の残りと deployed-like-snapshot は ubuntu-latest のまま)。ついでに、Build memory の `cargo clean` で bench が消えて Benchmark 段が「No such file」のまま成功していた不具合も直った。`python3 -m unittest discover -s scripts` 273 本 OK、`test_mx.sh` 15/15、`check-docs.sh` 差分 0。CI の実物は push 後に親が確かめる。

- [x] **T17.14 揺れるテスト 3 本の根治**
  - 目的: `tests/clientacl_test.rs:117` (`.env` 再読込と CONNECT の競合、稀に 403)、`tests/history_depth_test.rs:235` (プロセス全体の RSS の差が上限 3,407,872 B を 5 回に 1 回超える)、`crates/sys/src/sys.rs:1106` 付近の fd を数えるテスト (1 回揺れた)。
    2026-09-26 に CI で落ちた `stale_entry_with_validators_survives_and_can_be_refreshed` (秒の境目) も同じ「時刻・資源に依存」の類だった。stable が動く今、CI の信頼度は前より要る。
  - やること: 1 本ずつ原因を突き止めて直す (`sleep` で誤魔化さない)。`clientacl`: 再読込の完了を `/config` の `reload` の欄で待つ。`history_depth`: RSS の差ではなく **リングの `rings_used` の差**を上限と比べるか、差を測る前に `malloc_trim` 相当の揺れを外す (T17.8 と相談: 同じ波ではないので、T17.8 の受け入れ基準に書いた「触るぶんは差に入らない位置」を守る)。`sys` の fd: 他スレッドの fd を数えない形に。
    ついでに `let now = now_epoch()` のあと TTL 0 で入れて `is_fresh(now)` を見る形が他に無いか `grep` (あれば同じ直し)。
  - 受け入れ基準: 各テストを **20 回**回して 20/20 (`for i in $(seq 20); do scripts/mx cargo test --test clientacl_test …; done`)。直す前に落ちる再現があれば、その回数も報告に。
  - 結果 (2026-09-26、`4a07c7b` / `2259755` / `6d78c7a`): 3 本とも、直す前の揺れは手元で再現しなかった。clientacl は mx 20/20・バイナリ直 100/100・6 並列 150/150、history_depth は 20/20・6 並列 60/60、sys の fd は単体テスト全体 100/100。(1) `clientacl_test` は、ログの行のあとに `/config` の値 (`PROXY_ALLOW_CLIENTS` に `127.0.0.0/8`、`PROXY_ALLOW_LOCAL` が true) と `reload` の欄 (`reloads >= 1`、`error` null) を確かめてから CONNECT を送る。回数ではなく値で見る。Phase 14 の表の「ログのあとに入れ替わる」はコードと合わない (`reload.rs` は差し替えてからログを出す)。403 の原因は特定できていない。(2) `history_depth_test` は RSS の差をやめ、`memory.rings_used.history` の差 (手元で毎回 2,972,160 B = 688 B × 4,320、上限 3,407,872 B) を上限と比べる。RSS は参考に印字するだけ。T17.8 の起動時の触りは差に入らない。(3) `sys.rs` の `listen_socket_reports_address_in_use_without_leaking` は、テストバイナリを `--exact --test-threads=1` で子として起こし直して子の中で数え、幅なしの `after == before` を見る (子で 5 → 5。子が落ちれば親も落ちることを確認)。直した後は 3 本とも 20/20、sys の単体テスト全体 50/50、history_depth の 6 並列 60/60。TTL 0 + `is_fresh(now)` と同じ形は、grep した範囲では見つからなかった。(注記の Sample 664 B は今 688 B、直していない)

- [x] **T17.15 `scripts/mx` のテスト**
  - 目的: 2026-09-26 に入れた `scripts/mx` は手で 4 つ確かめただけ (終了コード、残った子の片付け (正常終了 / TERM)、占有の待ち、2 口の並列)。
  - やること: `scripts/test_mx.sh` (bash。`MX_DIR` を一時ディレクトリに向ける) に上の 4 つ + 「他人のベンチ待ち」(`target/release/rust-http-proxy` という名前の `sleep` を偽物として起動) + `MX_WAIT` で 75。`python3 -m unittest discover -s scripts` から呼べるように `scripts/test_mx.py` で `subprocess` 1 本。
  - 受け入れ基準: 6 つが通り、CI (`python3 -m unittest discover -s scripts`) でも回ること。
  - 結果 (2026-09-26、35dd4a5 + e433ad5): `scripts/test_mx.sh` (bash。`MX_DIR` は mktemp の中) に 6 項目を入れた。(1) 終了コード 7 / 0 / 引数なしで 2、(2) 正常終了のあとと TERM (143) のあとに、裏に残した子が止まる、(3) `MX_SLOTS=1` の占有中は `MX_SLOTS=1` も 2 も待ち、知らせを出す、(4) 2 口で 2 本が同時に走り、3 本目は待つ、(5) `exec -a …/target/release/rust-http-proxy sleep 3` の偽物が消えるまで待つ (uutils の sleep はシンボリックリンクだと argv[0] の名前で断るので `exec -a`)、(6) `MX_WAIT=1` で命令を走らせずに 75。全体で約 15 秒。mx から `cleanup` と `exit 75` を外すと 2 と 6 が NG になることを確かめた。`scripts/test_mx.py` が subprocess 1 本で呼ぶので `python3 -m unittest discover -s scripts` から回る (223 本 OK、skip 5)。本文と違った点: ci.yml は scripts/ の unittest を回していなかったので、check ジョブの check-docs.sh の隣に 1 行足した (CI で通るかは push のあとに見る)。気づいたが直していないこと: mx は TERM の trap を命令の起動後に張るので、その隙間の TERM では子が残る。`others()` の子孫除外は `CHILD` が未設定の時点で呼ばれるので効いていない。

- [x] **T17.16 匿名化した実データの更新と、`collect-deployed.sh` が `.anon.json` を隣に置く**
  - 目的: `scripts/testdata/deployed-2026-09-16.anon.json` は Phase 13 の版で、`recent` / `profile` / `events` / `hosts_series` / T16.0 の 3 列が無い。T14.35 のやり残し (`collect-deployed.sh` が匿名化を呼んでいない)。
  - 変更箇所: `scripts/anonymize-snapshot.py` (`PART_ORDER` に無い部があれば足す。`daily` / `profile_res_60` などの隣のファイルも同じ置換で)、`scripts/collect-deployed.sh` (雪像の隣に `<時刻>-snapshot.anon.json`)、`scripts/testdata/deployed-2026-09-26.anon.json` (新規。古いものは残す)、`scripts/test_snapshot_diff.py` / `test_snapshot_summary.py` の `Anonymized` の期待値。
  - やること: `status/2026-09-26T114602Z-snapshot.json` を匿名化して fixture に。**ホスト名・IP・UA・SNI・接続元がすべて置き換わっていること**を機械で確かめる (`grep` で元の値が 0 件。`status/` の生の値をテストや報告に書かない)。
  - 受け入れ基準: 新しい fixture で phase15 / phase17 の判定表が出るテスト。`python3 -m unittest discover -s scripts` 全通過。fixture は 4 MiB 未満。
  - 結果 (2026-09-26、69ac1e3): `scripts/anonymize-snapshot.py` に `--side IN[=OUT]` を足し、雪像に入らない口 (`-daily.json` `-profile_res_60.json`) を雪像と同じ表で置き換えるようにした。置き換え漏れを 2 つ直した: いまの版の接続元の行にある `agent` (単数) と、`/events` の `new_client:` の説明の終わりの `agent "…"`。どちらも UA が生のまま残っていた (47c9cd5 / b7564bd)。`PART_ORDER` は `snapshot()` の 17 部と同じで、足す部は無かった。`scripts/collect-deployed.sh` は `/profile?res=60` を繋いだあとで、雪像の隣に `<時刻>-snapshot.anon.json` と `<時刻>-{daily,profile_res_60}.anon.json` を置く (`ANON=0` で置かない。881b8b8)。`status/2026-09-26T114602Z` を匿名化して `scripts/testdata/deployed-2026-09-26{,-daily,-profile_res_60}.anon.json` (計 2.55 MiB) にした。2026-09-16 の fixture は残した。生の値 1,048 個を fixture の中で探して 0 件、文書用でない IP は 0 件だった。2026-09-16 → 2026-09-26 の fixture で phase15 (6 行) と phase17 (8 行) の判定表が出て、T16.99 の数字が出る (ffc45ec)。前後は別々に匿名化したので名前の番号が合わず、`discord.com` の行は「判定できず」になる。`python3 -m unittest discover -s scripts` は 297 本通過 (飛ばし 6)、`check-docs.sh` は差分 0。

- [x] **T17.17 §2「デプロイ先の現在地」と README を 2026-09-26 の雪像で更新 (親)**
  - やること: T16.99 の数字 (47.2 時間、版 `2e57626`) で §2 の表と README の「デプロイ先の数字」を書き直す。古い 1 枚 (2026-09-24) は付録 A へ 1 文字も変えずに移す (これまでと同じ)。
  - 受け入れ基準: README と §2 の数字が同じ雪像から出ていること。`./scripts/check-docs.sh` 差分 0。
  - 結果 (2026-09-26、`ff1ff14`、取り込み `d293094`): §2 に「デプロイ先の現在地 (2026-09-26、Phase 16 (T16.0 + `codegen-units = 4`) をデプロイして 47.2 時間)」を書き、2026-09-24 の 1 枚は付録 A の「デプロイ先の現在地の古い版」の先頭へ 1 文字も変えずに移した (`cmp` で同一)。README の「デプロイ先の数字」も同じ雪像 (`status/2026-09-26T114602Z-snapshot.json`、版 `0.1.0+2e57626`) の数字にした。後の列は平常時 37 標本 2,477 本で、確立 p50 8.1 → 7.8 / p95 187.0 → 159.9 ms、`wait` p50 7.9 / p95 167.9 ms。名前解決は 0.05 → 0.06 回/接続 (ミス 1 回 33.2 → 106.8 ms、接続 1 本あたり 1.6 → 6.1 ms)、引き直し 436 回/時・名前ごとの最大 80.0、`dns_warm_max` 15・`warm_evicted` 0、絞り 0 / 412,474 周期、プロセス全体 0.0013 コア、`timeout` 0 件、`dns_slow` 0.61 件/時、RSS 47.0 → 54.2 MB。数字は `snapshot-diff.py --criteria phase17` と `snapshot-summary.py` で突き合わせた。T16.99 と違った所: 日ごとのミスの幅はこの版の 2 日とも 0.001 (T16.99 の 0.00〜0.15 は前の版の日を含む)、datadog の引き直しは 3,771 回 (T16.99 は 3,761)、conn 役の前の値は道具の 738 us を使った (T16.99 は 699)。§0「到達」の参照は親が 2026-09-26 に向け直した。残り: README のこの節の冒頭の「0.015〜0.068 req/s」と末尾の「残っている待ちは 2 つ … 尾 (p90 53.5 / p95 78.0 ms)」は Phase 14 の頃の文のまま (T17.99 で直す)。

- [x] **T17.18 `TODO.md` (7,000 行) の整理: Phase 0〜14 を `docs/HISTORY.md` へ (親)**
  - 目的: 2026-09-26 に冒頭の節の表が Phase 14 で止まっていた = 手で保てていない。
  - やること: §3 (やったこと Phase 0〜7) と §5 の Phase 8〜14 を `docs/HISTORY.md` へ**そのまま**移す (番号は振り直さない。コミットメッセージからは引ける)。この文書には「§3 / Phase 8〜14 は `docs/HISTORY.md`」の 1 行と、§4 (採らなかったもの) は**残す** (同じ道を二度調べないため)。
    `git grep 'TODO.md'` で行を指している所 (`scripts/` / README / crates のコメント) が無いか先に見る。**T17 の波が全部マージされてから** (親の `TODO.md` の編集とぶつからないように、最後)。
  - 受け入れ基準: `./scripts/check-docs.sh` 差分 0、`python3 -m unittest discover -s scripts` 通過、`TODO.md` が 2,000 行以下。
  - 結果 (2026-09-26): §3 (Phase 0〜7)・Phase 8〜14・Phase 15 の済んだタスク (T15.0〜T15.11・T15.13〜T15.16・T15.99) を `docs/HISTORY.md` へそのまま移した (7,323 → 1,596 行。移した 5,735 行と残した行を突き合わせ、空行以外で消えた行 0)。本文の「Phase 0〜14」だけでは約 2,750 行残って 2,000 行に届かないので、Phase 15 の済んだタスクも移した。**残したもの**: §0〜§2、§3 の案内 1 行、§4、§5 の「実装を Opus に渡すときの決まり」、旧 Phase 14 の「既知の小物」の表 (生きている表なので §5 の「既知の小物」へ。HISTORY には移した旨の 1 行)、Phase 15 の見出しと「Phase 15 の渡し方」とまだ開いている T15.12、Phase 16・17、付録 A。コードやスクリプトのコメントは `TODO.md T14.35` のように番号で指していて行番号は無い (`git grep`) ので、番号は HISTORY で引ける。Phase 17 の指示文の「`grep -n "Phase 15 の渡し方"` の 2 つめ」は元から 1 つめが見出しだったので「1 つめ」に直した。`check-docs.sh` 差分 0、`python3 -m unittest discover -s scripts` 通過。

- [x] **T17.19 再デプロイの記録の決まり (親)**
  - 目的: T16 の再デプロイは `TODO.md` に書かれておらず、2026-09-26 に `uptime_secs` から起こした。
  - やること: §0 の「作業の進め方」に「**再デプロイの直前と直後に `scripts/collect-deployed.sh` を 1 枚ずつ** (直前 = 判定の「前」、直後 = 0 時間の雪像)。利用者が再起動したら親が撮り、その Phase の `Tn.99` の前提に版と起動時刻を書く」を足す。README 運用 §2 と同じ内容。
  - 受け入れ基準: §0 に 1 行、T17.99 の前提がその形で書けていること。
  - 結果 (2026-09-26): §0 の「作業の進め方」に 9 を足した (再デプロイの直前と直後に 1 枚ずつ、`Tn.99` の前提に版と起動時刻)。README の「運用」の 1 (止める直前に 1 枚) と 2 (起動直後に 1 枚、版と起動時刻を `Tn.99` へ) に同じことを書き、7 の「いまは `T15.99`」を `T17.99` に直した。T17.99 の前提はその形 (版・起動時刻・「前」・「0 時間」の 4 つ、未記入) に書き換えた。

- [x] **T17.20 小物 (次にそのファイルを触るとき、と書いてあったもの)**
  - やること: (1) `crates/web/src/probe.html:137` の「`src/lib.rs` の `record_client_agent` の手前」→ `crates/server/src/lib.rs` (呼び出しは 1663 行、本体は `crates/metrics-core/src/metrics.rs:647`)。配る HTML なので `scripts/check-dashboard.js` と同じ上限の検査があれば通す。
    (2) keep-alive の HTTP 接続が `/connections` で宛先とバイト数を出さない (Phase 14 の表の T13.4 の項): **接続の最初の要求の宛先だけ**を登録時に書く (1 回)。まだそうなっていなければ。既にそうなっていたら報告だけ。
    (3) Phase 14 の表で済んでいるもの (`Connection: close`、`.dockerignore`、`evicted_idle` の系列、403 の `/errors`、`--only connect` 6 組、README のデプロイ先の表) は親が表から消す。
  - 受け入れ基準: (1) は HTML の文言だけ。(2) は `tests/connections_test.rs` (あれば) に 1 本。要求の経路に増えるのは登録時の 1 回だけ (`--lite` の費用は不変)。
  - 結果 (2026-09-26、e2bef7c): (1) `crates/web/src/probe.html:137` のコメントの道を `src/lib.rs` → `crates/server/src/lib.rs` に直した (文言だけ。`check-dashboard.js` の probe.html 64 KiB 上限と構文は通過)。(2) は T14.2 (5) / T14.48 で既に済んでいた。`ConnSlot::set_first_target` を接続の最初の要求 (`*served == 0`) で 1 回だけ呼び、`host:port` を書く。既存の `tests/recent_test.rs` の `test_integration_connections_shows_the_first_target_of_a_keepalive_http_connection` が通ることを確かめた (`tests/connections_test.rs` は無い)。要求の経路は不変。残り: keep-alive の HTTP 行の `/connections` の `bytes` は生きている間 0 のまま (`set_bytes` はトンネルだけ)。Phase 14 の表の「バイト数」の半分は未着手。
  - 親 (3) (2026-09-26): 既知の小物の表から済んだ 10 行を消した (`Connection: close` (T14.2)・`.dockerignore`・`evicted_idle` の系列・403 の `/errors`・`--only connect` 6 組・README のデプロイ先の表・`client_read` (T17.4)・揺れるテスト 3 本 (T17.14))。`.dockerignore` と最後の要求の判定は main にあることを親が見た。keep-alive の行は「バイト数」だけに縮め、Phase 17 で見つかった 6 件 (揺れるテスト 3 本・`mx` の 2 つ・`warm_promote` の予定・T17.11 の doc リンク) を足した。T17.5 の「採らない」を §4 に 1 行足した。

- [x] **T17.21 `v*` タグを 1 つ切って `release.yml` (`action-gh-release@v3`) を動かす (親。**利用者が決める**)**
  - 目的: 2026-09-26 に上げた `softprops/action-gh-release` v3 は、タグを切るまで試せない。
  - やること: 利用者が「切ってよい」と言ったら `v0.1.0` (版は `Cargo.toml` の 0.1.0) を Phase 17 のデプロイの版に打ち、`release.yml` が 2 つの target のバイナリを付けることを見る。**タグは外に出るものなので、利用者の明示の許可なしに打たない**。
  - 受け入れ基準: GitHub の Release に `rust-http-proxy-x86_64-unknown-linux-gnu` と `-aarch64-…` が付いていること。付かなければ理由を書いて v2 に戻す。
  - 結果 (2026-09-26、run 36250794855): 利用者の決定でタグは本文の `v0.1.0` ではなく **`v0.17.21`** にし、`2bcf57f` (Phase 17 の波 15〜17 と T17.11 を入れ、CI が通った版) に打った。`release.yml` (`softprops/action-gh-release@v3`) は 2 つとも通り、Release に `rust-http-proxy-x86_64-unknown-linux-gnu` (2,005,088 B) と `rust-http-proxy-aarch64-unknown-linux-gnu` (1,830,584 B) が付いた (https://github.com/takano32/rust-http-proxy/releases/tag/v0.17.21)。v3 のままでよい。`Cargo.toml` の版は 0.1.0 のままなので、バイナリの `--version` と `/status` の `version` は `0.1.0+<commit>` と出る (タグと揃えるかは次に決める)。

- [ ] **T17.99 締める (Phase 17 の版を 24 時間走らせたあとに見る・決める・書く)**
  - 前提 (§0 の 9 の形。再デプロイしたら親が埋める): 版 `0.1.0+<commit>` (未)、起動時刻 `YYYY-MM-DDTHH:MM:SSZ` (未)、「前」の雪像 `status/<時刻>-snapshot.json` (未)、「0 時間」の雪像 (未)。
    波 15〜17 を入れた版を再デプロイして 24 時間。「前」は再デプロイの直前に親が撮った雪像、「後」は 24 時間後の 1 枚 (T17.19)。判定は `scripts/snapshot-diff.py <前> <後> --criteria phase17 --daily <後の -daily.json> --profile <後の -profile_res_60.json>` を明示で回す。
  - **Phase 17 の完了の定義 (下書き)**: (a) `/events` の `dns_slow` が **0.1 件/時以下** (T16.99 は 0.62)、`dns_miss_rate` は立たない (実測 0.05 に対し閾 0.20)。(b) T17.5 を採ったなら平常時のミスが 0.06 回/接続から下がり、引き直しが名前ごとに 80 回/時以下のまま。採らなかったなら「変わらない」。
    (c) **CPU の桁** (rustc 1.96 → 1.98 と T17.6〜T17.8 のぶん): conn 役のコア/要求が前の 1.3 倍以下、cgroup の起動からの user / sys が前後で同じ桁、`cpu_throttled` 0 件。(d) T17.8: 起動 1 時間後の RSS と 24 時間後の RSS の差が **5 MB 未満**。
    (e) T17.7: `/status` の `ipv6` が `probe_by: canary` で、`attempts` が増えない。(f) 判定表 `--criteria phase17` の 7 行が**全部自動で**出ること (「判定できず」が 0)。
  - 最後の手順は「次のターンの準備」(§0 の決まり): 次の再デプロイで判定するもの、要る計器、足りない計器は `T18.0`。

## 付録 A. 計測の記録 (時系列)

着手時からの数字の履歴。**現在地は §2**。Phase 5 以降の数字は各タスクの `結果:` にある (ここには重複して書かない)。

### 着手前 (2026-09-07、Python ベンチ)

環境: 8 コア / 6.6 GiB RAM、loopback、`target/release` (当時 opt-level=s, lto)、`PROXY_CACHE_ENABLED=off PROXY_STATS_PERSIST=off PROXY_LOG_LEVEL=warn`。
計測ツールは `scripts/bench.py` (Python 標準ライブラリのみ)。**Python 側が上限になる** ので絶対値は低めで、前後比較専用。

| 項目 | 値 | 備考 |
|---|---|---|
| forward, 8 並列 keep-alive, 1 KiB 応答 | **176 req/s, p50 44 ms** | プロキシ無しの直結は 621 req/s, p50 9.6 ms |
| forward, 64 並列 | 371 req/s, p50 66 ms, p99 1.5 s | |
| CONNECT トンネル 1 本のスループット | 881 MiB/s | 256 MiB を Python から送出 |
| CONNECT 確立/秒, 64 並列 | 398 tunnels/s | 短命トンネル |
| リリースビルド時間 | 25〜28 s | クリーンビルド |
| バイナリサイズ | 857 KB | |
| テスト | 150 単体 + 21 結合、約 3 s | |

**着手時に判明していた最大のボトルネック** (T1.1 で解消): `TCP_NODELAY` を立てていないため、応答ヘッダーと本文を別々に `write` した際に
Nagle + delayed ACK で **1 要求あたり約 40 ms 止まっていた**。両側に `set_nodelay(true)` を入れると 8 並列で 176 → 674 req/s、
p50 44 → 9.6 ms (直結と同等 = Python ベンチの上限)。

### Rust ベンチでの推移 (Phase 0〜1)

同じ環境、`--body-bytes 1024`、8 並列。`direct` はプロキシを通さないオリジン直結 (ベンチ自身の上限)。

| 時点 | forward 8 並列 | forward 64 並列 | tunnel 1 本 | connect 8 並列 | 備考 |
|---|---|---|---|---|---|
| T0.1 | 188 req/s, p50 42.0 ms, p99 54.1 ms | 1,517 req/s, p50 41.9 ms | 1,044 MiB/s | 3,506 /s, p50 1.85 ms | direct **300,805 req/s, p50 0.013 ms** |
| T1.1 `TCP_NODELAY` | **29,107, p50 0.222, p99 1.33** | 22,027, p50 1.97, p99 15.8 | 1,114 | 3,938, p50 1.70 | 155 倍 |
| T1.2 1 回の `sendto` | 30,231, p50 0.216 | | | | HIT 60,813 → **75,563 req/s**, p50 0.070 ms |
| T1.3 `poll` + `splice` | | | **2,800** | **7,375**, p50 0.68 | トンネルのスレッド 3 → 1、待ち受けのみ 5 スレッド |
| T1.4 余計な仕事をしない | 30,699 | | | 8,293 | 起動直後のスレッド 5 → 3 |
| T1.5 中継バッファの使い回し | **32,842** | **25,912** (+15.7%) | | | 1 並列 6,697 → 8,286 |
| T1.6 プール | 32,074 | 26,142 | | | `pool_hit_ratio` 0.9712 |

connect 64 並列は T0.1 → T1.1 で 3,648 → 3,288 tunnels/s (p50 17.3 → 18.1 ms)、forward 64 並列の p99 は 51.3 → 15.8 ms。
direct は T1.1 後も 300,542 req/s (p50 0.014 ms) でベンチは律速しない。

**T1.5 の内訳** (conn 1 本、1 要求あたり、`Instant` を一時的に仕込んで測定。全体 128 us):

| 区間 | 時間 | |
|---|---|---|
| 要求解析 (parse_request_headers + parse_origin) | 4.9 us | **3.8%** |
| キャッシュ判定 | 2.2 us | |
| 要求ヘッダー組み立て | 1.6 us | |
| オリジン接続の取得 (プール) | 5.2 us | |
| オリジン往復 | 61.2 us | 大半は待ち時間 |
| 応答ヘッダー整形 | 5.6 us | |
| 本文の配信 | 24.2 us → 17.9 us | 毎要求 64 KiB を確保・ゼロ埋めしていた |
| 統計・アクセスログ | 3.3 us | |

確保回数は 1 要求あたり 98.7 回 (数えるアロケータで測定)。解析の割合が 5% 未満なので**ヘッダーのスライス化は見送り**。

### T2.3 の判断 (同時 5,000 本のアイドルトンネル)

| | RSS | fd/本 | 新規 CONNECT p50 / p99 |
|---|---|---|---|
| 接続スレッドのスタック 256 KiB のみ | 199 MiB | 7.0 | 1.18 / 9.07 ms |
| + パイプの遅延生成・CONNECT 中の BufReader 解放 | **198 MiB** | **2.0** | 1.59 / 9.13 ms |

受け入れ基準 (RSS 200 MiB 未満、p99 10 ms 未満) を満たすので **epoll リアクターは作らない**。
RSS の大半はスレッドスタックの実使用ぶん (1 本あたり約 36 KiB) で、fd とヒープはもう削ってある。

### T3.2 `--lite` の比較 (同じ機械で連続、8 並列)

| プロファイル | forward | connect | 起動直後のスレッド |
|---|---|---|---|
| 既定 (キャッシュ + 統計あり) | 19,475 req/s, p50 0.295 ms | 7,440 tunnels/s | 6 |
| `--lite` | **33,125 req/s, p50 0.199 ms** | **8,350 tunnels/s** | **3** |

### T4.2 `opt-level` の比較 (`--lite`、8 並列、forward は 3 回の平均)

| | forward | tunnel | connect | バイナリ |
|---|---|---|---|---|
| `"s"` (当時) | 32,419 req/s | 2,681 MiB/s | 8,177/s | 923,040 B |
| `3` | 33,121 req/s (+2.2%) | 2,577 MiB/s | 8,231/s | 1,054,112 B |

### T4.4 システムコールの削減 (`strace -f -c`)

| | システムコール/要求 | CPU/要求 | forward |
|---|---|---|---|
| 前 | 10.06 (setsockopt 4.00, getsockname 1.00) | 89.9 us (user 34.6 / kernel 55.3) | 25,321 req/s |
| 後 | **5.06** (recvfrom 3.00 + sendto 2.00 だけ) | **85.0 us** (user 32.3 / kernel 52.8) | **26,685 req/s** |

3 回測って 3 回とも改善 (**-5.4% CPU / +5.4% スループット**)。残る 5 回は本質的な入出力。
その前段で確保を 90.2 → 76.3 回/要求 に減らしたが速度は誤差の中 (91.8 → 93.3 us、ノイズ ±5%) だった。理由は
プロキシの CPU の 63% がカーネル側で、ユーザー空間の確保・解放 12% = 全体の約 4 us だから (14 回減らしても 0.6 us)。

### Phase 4 完了時点の値

| 項目 | 着手前 | Phase 4 完了時 |
|---|---|---|
| forward, 8 並列 (`--lite`) | 188 req/s, p50 42.0 ms | **32,419 req/s, p50 0.20 ms** (172 倍) |
| forward, 8 並列 (キャッシュ HIT) | — | **75,563 req/s, p50 0.070 ms** |
| forward, 64 並列 | 1,517 req/s, p50 41.9 ms | 25,912 req/s, p50 1.6 ms |
| CONNECT トンネル 1 本 | 1,044 MiB/s | **2,681 MiB/s** |
| CONNECT 確立, 8 並列 | 3,506 tunnels/s | **8,283 tunnels/s** |
| 起動直後のスレッド | 6 | 6 (既定) / **3** (`--lite`) |
| 同時 5,000 トンネル | 未計測 | RSS 198 MiB、新規 CONNECT p99 9.1 ms |
| バイナリ | 857 KB | 923 KB |
| テスト | 150 単体 + 21 結合 | **158 単体 + 28 結合** |

### perf / strace での確認

- **T0.2**: `CARGO_PROFILE_RELEASE_DEBUG=1` だけでは `profile.release` の `strip = true` に消されるので、
  `CARGO_PROFILE_RELEASE_STRIP=none` も要る (README を修正)。付ければ `perf record -g -p <pid>` → `perf report` でシンボルが出る。
- **T1.2**: `strace -f -e trace=write,sendto` で 1 要求あたりのクライアント向け呼び出しを直接確認した
  (Rust の `TcpStream` は `write` ではなく `sendto` を使う)。

  ```
  変更前 (4717a3d): sendto(7, "HTTP/1.1 200 OK...", 90)   ← ヘッダー
                    sendto(7, "xxxxxxxx...",        1000) ← 本文
  変更後:           sendto(7, "HTTP/1.1 200 OK...", 1090) ← 1 回
  ```

- **T1.4**: `PROXY_CACHE_ENABLED=off` のとき、`perf report` に `cache::` / `freshness::` のシンボルは出ない
  (唯一出るのは起動時の `Ballast` の drop が 0.01%)。`strace -c` のファイル系システムコールも 28,644 要求で
  `openat` 18 回・`statx` 2 回・`mkdirat` 2 回 (すべて起動時) で、要求あたり 0。キャッシュ有効時は 58,078 要求で `openat` 283 回・`mkdirat` 257 回。
- 旧 `scripts/bench.py` でも forward 8 並列 p50 44 ms → **11.0 ms** になった (直結が p50 9.6 ms なので Python 側の下限。実際の値は Rust ベンチの 0.20 ms)。

### 2026-09-07 深夜の測り直し (Phase 9 の着手前。この機械の今の状態)

`scripts/cpu-per-request.sh` で 5 秒ずつ (8 並列、`--lite`、本文 1 KiB)。§2 の値より 2 割ほど重く出ているが、
同じ日に同じ道具で測った前後比較にしか使わない (機械の状態が違う。この日は計測中に別の作業も走っていた)。

| 経路 | スループット | p50 | CPU/操作 | 内訳 (user / kernel) |
|---|---|---|---|---|
| forward keep-alive | 30,039 req/s | 0.183 ms | **50.9 us/要求** (3 回で 50.9 / 55.3 / 58.9) | 16.2 / 34.7 |
| forward 1 接続 1 要求 (`--no-keepalive`) | 11,955 req/s | 0.330 ms | **123.3 us/接続** | 29.1 / 94.2 |
| connect (短命トンネル) | 6,068 /s | 0.224 ms | **187.3 us/本** | 26.3 / 161.0 |
| tunnel 1 本 (256 MiB) | 1,301 MiB/s | — | 273 us/MiB | 0 / 273 |
| キャッシュ HIT (メモリ 64 MiB) | 59,461 req/s | 0.091 ms | **30.6 us/要求** | 13.1 / 17.5 |

システムコール (`strace -f -c`、プロキシを strace の下で起動): keep-alive は **5.03 回/要求**
(`recvfrom` 3.00・`sendto` 2.00、`futex` 0.026、`setsockopt` 0.005)。1 接続 1 要求は **14 回/接続** (内訳は T8.2)。
暇なプロキシは 10 秒で `epoll_pwait` 10 回 (監視スレッドの 1 秒周期) だけで、起動後は他に起きない (lite・既定とも)。

### デプロイ先の現在地の古い版

T14.99 (2026-09-18)・T15.99 (2026-09-24)・T17.17 (2026-09-26) で §2 から移したもの。**1 文字も変えていない** (本文からの参照は「付録 A の『デプロイ先の現在地 (2026-09-1X)』」。古い書き方「§2 の…」が残っていてもここを指す)。最新の 1 枚は §2 の「デプロイ先の現在地 (2026-09-26)」。先頭が 2026-09-24 (T17.17 で移した)、次が 2026-09-18 (T15.99 で移した)、その下は日付順。

### デプロイ先の現在地 (2026-09-24、Phase 15 (T15.4 + T15.6) をデプロイして 126.6 時間)

2026-09-19 03:00:28 UTC ごろに版 `0.1.0+9f07de4` (T15.4 の keep-warm 3,600 秒、T15.5 の空回りの修正、T15.6 の connect 既定 10 秒、T15.0 の計器) で
起動し直し、`.env` には特別な行を入れずに走らせた。2026-09-24 09:34:01 UTC (起動から 126.6 時間 = 5.3 日、`uptime_secs` 455,613) に
`scripts/collect-deployed.sh nagoya.sorahost.net:50697` で雪像を 1 枚取った。出どころは `status/2026-09-24T093400Z-snapshot.json`
(同時刻に `2026-09-24T0934Z-{healthz,profile_res_60,slo,daily,config}.json` も取った。1 枚の要約は `2026-09-24T0934Z-collect.md`、判定表は
`2026-09-24T0934Z-phase15.md` = 再デプロイ直前の `2026-09-19T022515Z-snapshot.json` との差分)。**雪像はリポジトリに入れない**。
前後は `$.history["3600"].samples` を起動時刻で切り、**バーストの無い時間帯 (1 標本 300 本未満) どうし**で並べる
(前 = 200 標本 11,802 本 (Phase 12〜14 の版)、後 = 111 標本 5,452 本)。判定の 1 行ずつは T15.99 の「判定 (2026-09-24)」の表。

| 項目 | 前 (Phase 12〜14 の平常時) | 後 (Phase 15 の平常時、126.6 時間) | 出どころ |
|---|---|---|---|
| CONNECT 確立 | avg 25.3 / **p50 8.2** / p95 84.1 ms | avg 21.9 / **p50 8.1** / p95 187.0 ms | `$.history["3600"].samples` の `connect_buckets` を段ごとに足して線形補間 |
| 利用者が待つ `wait` (次の基準線) | (T15.0 より前は無い) | **p50 約 8.2 / p95 約 189 ms**。参考: 直近 1,024 本は p90 111 / p99 113 ms (この 1,024 本はバーストの中) | `$.history["3600"].samples` の `wait` / `$.status.recent_quantiles.wait` |
| 名前解決のミス | **0.40 回/接続**、ミス 1 回 13.3 ms、接続 1 本あたり 5.3 ms | **0.05 回/接続**、ミス 1 回 33.2 ms、接続 1 本あたり **1.6 ms**。日ごとでは 0.02〜0.12 | 同上 (`dns_misses` / `connects` / `dns_ms_sum`)、`/daily` の `dns_per_connect` |
| 主要 3 ホストのミス率 | `discord.com` **0.441** (T15.0 の版の 9 時間) | **`discord.com` 0.01** (1,600 要求 / 10 ミス)、`http-intake.logs.us5.datadoghq.com` 0.00 (4,330 / 17)、`mtalk.google.com` 0.01 (381 / 3) | 前後 2 枚の `$.hosts.hosts` の差分 |
| ミスの種類 (起動から) | `cold` 0.45 : `expired` 0.55 : `warm_stale` 0 : `negative` 0 (T15.0 の版) | `cold` 229 : `expired` 238 : `warm_stale` 6 : `negative` 0 (= 0.48 : 0.50 : 0.01 : 0)。`refresh_late` 0 / `refresh_failures` 0 | `$.status.dns.misses_by_kind` |
| 裏の引き直し | 9,223 回 / 36.6 時間 = 252 回/時 (Phase 14) | 通算 45,499 回 = **359 回/時** (0.1 回/秒、上限 2,560 の 14%)、名前ごとの最大 **78.2 回/時** (`discord.com`。閾 80 = TTL 60 秒の 3/4 おきの物理上の上限)、`warm` 9 件 (上限 32) | `$.status.dns.refreshes` ÷ `uptime_secs`、`$.dns.entries` |
| CPU (コンテナ) | 0.5 コアを 24 時間 100% 使い切り、毎周期絞られていた (Phase 14。T15.5 で直した空回り) | **絞り 0 / 889,600 周期** (起動から)、プロセス全体 **0.0014 コア**・conn 役 0.00004 コア。`/healthz` の `checks.cpu` ok (`throttled_5m` 0) | `$.status.kernel.cgroup_cpu.since_start`、`/profile?res=60`、`/healthz` |
| 確立の尾 | p90 58.8 / p95 83.7 ms (原因は測れていなかった) | 平常時の p90: `wait` 約 120 / `connect` 約 117 ms (差 約 3 ms、平均の差 1.6 ms は名前解決)。尾は**遠い相手の connect** (100〜250 ms の区間に 10.7%)。`run_delay_us` の conn 役は 5.45 時間で合計 72 ms | `$.history["3600"]` の `wait` / `connect`、`/profile?res=60` の `run_delay_us` |
| エラー | 108 件 (前の期間、バースト込み) | **0 件** (`timeout` 0.00 件/時。前 0.04)。connect の最大 3,197 ms で既定の 10 秒に一度も当たらず。個票の窓内 3 件は走査とみられる 2 つの接続元の 400 (`bad_request`) | `$.history["3600"]` の `errors_by_cause`、`$.errors.errors` |
| バースト | 山 218 (Phase 12) | 窓 14 本 (1 標本 300 本以上)、山 **83** / 上限 240、`rejected_overload` 0 / `evicted_idle` 0 | `$.history["3600"]` の `active_max`、`$.status` |
| RSS | 41.8 MB (Phase 14、36.6 時間) | **47.0 MB** (47,005,696 B = 44.8 MiB)。`memory.rings_used.total` 13.25 MB ≤ `rings.total` 14.69 MB | `$.status.memory` |
| canary | 失敗 0 / 2,160 回 (Phase 14) | 失敗 **0 / 7,472 回**。IPv6 側は `null` のまま | `$.status.canary` |
| 状態ファイル | `.rrd` 8,388,608 B・版 3、`.recent` の取りこぼし 0 | `.rrd` 8,388,608 B・版 3・書出し 7,592 回・エラー 0。`.recent` 記録 91,016 件・`dropped` 9,598 (枠が一巡して古い方から落としたぶん。メモリのリングには残る)・エラー 0 | `$.status.state_file` |
| 接続元 | 3 つ | `161.33.196.121` が 100,596 要求 (ほぼ全部、3.0 GiB)、ほかに curl の接続元 1 つ (18 要求) と走査とみられる 2 つ (400 が 3 件) | `$.clients.clients` の差分、`$.errors.errors` |
| ディスク | 3.6% | **3.1%** | `$.status.cache.system.disk_used_percent` |
| 版 | `0.1.0+a262220` | `0.1.0+9f07de4` | `$.status.version` |

**読み方**:

- **T15.4 は効いた**。`discord.com` の 0.441 → 0.01 は「2〜4 本の塊が約 1,800 秒おき」に 3,600 秒の窓が届いたぶん。全体の 0.05 は判定の幅
  (0.06〜0.09) を**良い方に**外れた。幅は再生の窓の 0.090 に「全期間はもう少し悪い」を乗せた推し量りで、同じ窓の予測 (−51%) は 0.045 と実測に合う。
  数え漏れは無い (`/history` のミス 442 + 標本の無い頭尾 = `/status` の 473、hits + misses 100,581 ≈ 要求 100,603)。窓を延ばした害
  (`warm_stale` / `refresh_late`) は出ていない。**ミス 1 回が 13.3 → 33.2 ms に上がったのは、安いミスが消えて高い尻尾が残ったため**で、
  接続 1 本あたりでは 5.3 → 1.6 ms に下がっている。
- **T15.5 は効いた**。Phase 14 の「0.5 コアに張り付き」は `/events` の `cpu_throttled` / `tunnel_spin` 0 件、`spins` の最大 1、絞り 0 周期で消えた。
- **T15.6 の既定 10 秒のまま**でよい (`timeout` 0 件)。
- **p95 が 84 → 187 ms に上がったのはプロキシではなく行き先の構成**。後の期間は遠い相手への CONNECT が増え (`/hosts` の差分で、平均 94 ms のホスト A (遠い相手) が
  32,875 要求 = 窓の要求の約 3 割)、`wait` と `connect` の p90 の差は約 3 ms しかない。**尾は相手までの距離のせいで、コードでは縮まない** (§4 の最終行)。
  p50 は 8.2 → 8.1 ms で 1 RTT のまま (付録 A の 2026-09-18 の読み)。
- **判定の道具が 2 行を取り違えた** (1c の引き直しの率と 3' の閉じ方の形)。どちらもプロキシの不具合ではなく、分子と分母の窓のずれと、256 KiB で
  切れた `/recent` の窓の長さの違いだった → T15.15 / T15.16。
- **物差しの但し書き**: `recent` / `hosts` / `profile` の部は 256 KiB で切れている (`truncated`。追うなら `collect-deployed.sh --full`)。
  `MAX_WARM` 32 の取り合いは 5 秒のリングが 1 時間ぶんしか無く、バーストの瞬間の最大は読めない (60 秒の最大 9、1 時間平均の最大 21 = 張り付いた形跡は無い)。

### デプロイ先の現在地 (2026-09-18、Phase 14 をデプロイして 36.6 時間)

2026-09-16 16:47:05 UTC に版 `0.1.0+9b50254` (Phase 14 の全部) を再デプロイし、7 分後の **16:54:11 UTC (= epoch 1789577651)** に
`0.1.0+a262220` (中身は同じ Phase 14) で起動し直した。2026-09-18 05:32:13 UTC (起動から 36.6 時間) に
`scripts/collect-deployed.sh nagoya.sorahost.net:50697` で雪像を 1 枚取った。出どころは
`~/rust-http-proxy-status/2026-09-18T053213Z-snapshot.json` (1 枚の要約は `2026-09-18T0532Z-collect.md`、
差分の相手は 0 時間の `2026-09-16T164806Z-snapshot.json`)。**雪像はリポジトリに入れない** (利用者の閲覧先ホストと接続元 IP が並ぶ)。
前後は `$.history["3600"].samples` を再起動時刻 1789577651 で切り、**バーストの無い時間帯 (1 標本 300 本未満) どうし**で並べる
(前 = 146 標本 7,523 本、後 = 33 標本 2,034 本)。

| 項目 | 前 (Phase 12〜13 の平常時) | 後 (Phase 14 の平常時、36.6 時間) | 出どころ |
|---|---|---|---|
| CONNECT 確立 | avg 17.6 / **p50 8.2** / p90 58.8 / p95 83.7 ms | avg 17.0 / **p50 8.4** / p90 53.5 / p95 78.0 ms | `$.history["3600"].samples` の `connect_buckets` を段ごとに足して線形補間 |
| (同じ p50 を別の窓で) | — | 生の直近 1,024 本 **8.152 ms** / 個票 614 本 **8.00 ms** / canary **8.0 ms** / `/daily` の 09-17 **8.2 ms** | `$.status.recent_quantiles.connect` / `$.recent.recent` / `$.history["60"].canary` / `/daily?n=365` |
| 名前解決のミス | **0.551 回/接続**、ミス 1 回 11.7 ms、接続 1 本あたり 6.4 ms | **0.144 回/接続**、ミス 1 回 19.3 ms、接続 1 本あたり **2.8 ms** | 同上 (`dns_misses` / `connects` / `dns_ms_sum`) |
| 主要 3 ホストのミス率 | datadog 0.31 / mtalk.google.com 1.00 / discord 0.76 (Phase 13) | **datadog 0.02** (1,459 要求 / 32 ミス)、**mtalk.google.com 0.01** (134 / 2)、**discord.com 0.15** (640 / 94) | 0 時間の雪像 → 後の雪像の `$.hosts.hosts` の差分 |
| 裏の引き直し (T14.1) | 442 回 / 72 時間 (Phase 13) | 9,223 回 / 36.6 時間 = **252 回/時** (通算平均。同日 06:09 UTC の `/status` で測った実勢は **382 回/時**)、`warm` 3〜5 件 (上限 32) | `$.status.dns.refreshes` ÷ `uptime_secs`、`$.status.dns.warm` |
| RSS | 21.1 MB (Phase 13) | **41.8 MB** (十進 = 39.9 MiB)。雪像を配っている最中に読んだ `$.status.memory.rss` は 42.8 MB (= 40.8 MiB。**1 枚の要約が出すのはこちら**) で、うち約 **1.0 MB** は**この測定自身**が増やしたぶん — `$.history["5"]` は 05:31:58Z まで 41,820,160 B で平ら、05:32:03Z に **+950,272 B**、05:32:08Z に +8,192 B、取得は 05:32:13Z | `$.status.memory.rss` と `/history?res=5` の 5 秒刻み |
| CPU (コンテナ) | (見えなかった) | cgroup の割り当て **0.5 コアを 24 時間 100% 使い切り**、毎 CFS 周期 (100 ms) 絞られている。その **99.8% が conn 役**で、`running` のスレッドがどの標本時刻にも 2.000 本 | `$.status.kernel.cgroup_cpu`、`/profile?res=60` の 6.0 時間 |
| `GET /` | 200 | **200**、2,853 B、案内 (endpoints) **32 行** / 全 38 行、ttfb 0.080 秒 | `probe-deployed.sh` (3 回とも 200、ttfb 0.054〜0.078 秒) |
| バースト | 山 218 (Phase 12) / 8 (Phase 13) | 窓 3 本のうち**山と呼べるのは 1 本** (2026-09-16 23:00Z、360 本、**山 171 = 上限 240 の 71%**)。写真 1 枚 (閾 120 を越えた瞬間の 123 本)。`evicted_idle` 0 / `rejected_overload` 0 / `listen_overflows` 0、fd 482 / 1,024、スレッド 45 / 128 | `$.bursts`、`$.history["3600"]` の `active_max` / `threads_max` / `fds_max` |
| エラー | 101 件 (Phase 12 のバースト) / 0 (Phase 13) | **4 件**。すべて 2026-09-16 の **43 分間** (22:49:29〜23:31:54Z、うち 3 件は山と同じ数分) に集中した `161.33.196.121` の CONNECT 502 で、原因は timeout 2 / refused 1 / dns 1。403 は 0 件 | `$.errors.errors` |
| canary (T14.10) | (無い) | 1,440 点 / 24.4 時間 = **1 時間あたり 59 点** (60 点には構造的に届かない)、`connect` p50 **8.0 ms** (利用者の 8.152 ms と差 0.15 ms)、失敗 0 / 2,160 回。IPv6 側は 1,440 点すべて `null` | `$.history["60"].canary`、`$.status.canary` |
| `/profile` の段階の内訳 (T14.3) | (無い) | 6.0 時間で **CONNECT 433 本** (avg: queue 4.93 / client_read 1.12 / dns 1.13 / connect 15.98 / first_relay 20.73 / relay 377.6 / park 71,308 ms)、**forward 97 本** (origin 26.4 / ttfb 50.0 ms、うち 76 本はキャッシュ HIT で <1 ms)。`queue` は **二峰性**で **378 本 (87.3%) が 1 ms 未満**・**37 本 (8.5%) が 25〜75 ms**・100 ms 超は 0 本 (母数はどちらも 433 本) | `/profile?res=60` (362 標本。1,440 標本のうち 256 KiB で切れて 6.0 時間ぶんしか返らない。段ごとの区間 `[<1, 1-2, 2-5, 5-10, 10-25, 25-50, 50-100]` を足すと `queue` は `[378, 1, 2, 5, 10, 16, 21]`・max 75 ms) |
| 接続元 | 2 つ | **3 つ**: `161.33.196.121` (3,299 要求 = 99.2%、283.1 MB、うち CONNECT がほぼ全部)、Wings の NAT (2026-09-15 16:28 以降 無音)、雪像の 86 秒前に現れた接続元 (クラウドの IP。下の「読み方」) | `$.clients.clients` の差分 |
| 状態ファイル | `.rrd` 4 MiB | `.rrd` **8,388,608 B 固定**・版 3 (`converted_from` は前の起動で 2)・書出し 2,197 回・エラー 0。`.recent` 4,194,304 B・書出し 2,295 回・記録 3,321 件・**取りこぼし 0**。`/recent` の `restored` 18・`/events` 4・`/log` 1 | `$.status.state_file` |
| ディスク | 2.5% | **3.6%** (36.7 時間で +23.09 MB。内訳はディスクキャッシュのバラスト 18.64 MB と日次の雪像 2 件 3.83 MB) | `$.status.cache.system` / `$.status.cache.disk` / `/snapshots` |
| 版 | `0.1.0+b2ae41f` | `0.1.0+a262220` | `$.status.version` |

**判定 (完了の定義の数値条件 3 行 + Phase 13 から持ち越した 1 行)**:

| 完了の定義 | 閾値 | 実測 (後の期間) | 判定 |
|---|---|---|---|
| `dns.misses ÷ 接続` が 0.15 未満 | < 0.15 | **0.144** 回/接続 (2,034 接続 / 293 ミス) | **満たした** |
| 主要 3 ホストのミス率が 0.05 未満 | < 0.05 | datadog 0.02・mtalk.google.com 0.01 は満たし、**discord.com 0.15** だけ届かず | **一部** |
| 平常時の CONNECT 確立 p50 が 6 ms 以下 | ≤ 6.0 ms | **8.4 ms** | **届かず (基準の立て方が誤り)** |
| (Phase 13 の定義から持ち越し。Phase 13 では判定できずだった) バーストがあっても `rejected_overload` 0 で `/status` が取れる | = 0 | 36.9 時間ずっと 0 (山 171 のときも 0) | **満たした** |

Phase 14 の完了の定義には数値でない文 (T14.2 の 7 件、T14.3 の `/profile` が既知の答えを再現、T14.4〜T14.8 の「1 枚の JSON に取れる」、
T14.9〜T14.12、T14.99 で README / §2 / §0) も入っている。それらは下の「判定 (2026-09-18)」と手引き (d) の 16 行で見る。

**読み方**:

- **名前解決は片づいた**。0.55 → 0.144 回/接続で、接続 1 本あたりの名前解決は 6.4 → 2.8 ms (-56%)。keep-warm (T14.1) を切った
  再生 (窓 0) では 0.549 回/接続 = Phase 13 の 0.551 に戻るので、この差は keep-warm のものとして測れている。
  **ミス 1 回が 11.7 → 19.3 ms に上がったのは「安いミスが消えて高い尻尾が残った」ためだけではない**: 両方の窓に出てくる
  名前つき 13 ホストだけで見ると同じ名前の値段が **9.34 → 12.52 ms (+34%)** で、9.46 → 14.95 ms の上がりのうち構成の入れ替えが
  53%・**同じ名前の値上がり (= 上流の名前解決そのものが遅くなった) が 47%**。これはこちらのコードの外側にある。
- **discord.com だけ 0.15**。理由は測れている: **2〜4 本の塊が約 1,800 秒おきに来る**ので `PROXY_DNS_WARM_SECS` の 900 秒の窓を
  いつも外れ、塊の 1 本目が必ずミスし、2 本目が TTL 60 秒より遅れて来るともう 1 回ミスする。**再生の窓 (個票 612 本 = 9.08 時間)**
  では引き直し 424 回のうち **248 回 (58%)** が誰も待っていない 900 秒の尻尾に消えていた (出どころは `$.recent.recent` を使った
  行事駆動の再生)。同じ窓の再生を 3,600 秒にするとこの 21 ミスが 1 (再生の頭の準備運動) になる → **T15.4**。
  **通算**の引き直しは別の量で、`$.dns.entries` の `discord.com` は 36.6 時間で **1,553 回** (ミス 93 回)。
  通算にも 58% が当てはまるかは測っていない (同じ割合が続くなら、という推測になる)。
  なお 0.15 は不均質な窓の平均で、直近 9.08 時間の個票では **0.36**、直近 24 時間の `$.hosts_series` からの見積もりで約 **0.23**。
  基準 0.05 からの距離は表の数字より大きい。
- **p50 6 ms には届かなかった。追いかけても届かない** (理由は 3 つとも測れている)。
  (a) **物理**: 確立 − カーネル RTT (`$.recent.recent[].rtt_ms.origin`) の中央値は **−0.21 ms** で、プロキシの取り分は中央値で 0。
  個票 614 本の `rtt_ms.origin` は最小 6.72 ms・p50 8.22 ms で **6 ms 以下が 1 本も無い**。要求で重み付けた最良 RTT の中央値も 6.59 ms。
  (b) **基準の根拠が誤っていた**: 6 ms は T14.1 の「名前解決は接続 1 本あたり 6.3 ms で、p50 8.3 ms の床のうち動かせる唯一の部分」
  から来ているが、これは**平均の引き算を中央値に当てたもの**。名前解決を払う接続は少数派 (0.144 回/接続) なので、ミス率を
  いくら下げても中央値は定義上動かない。実測もそう出た (名前解決 6.4 → 2.8 ms/接続に対し p50 は +0.15 ms)。
  (c) **物差しが壊れている**: 判定が見ている「確立」の時計に名前解決が **1 ms も入っていない**。`PROXY_ALLOW_LOCAL=false` では
  ACL が `open()` の時計より前に名前を引き (`src/lib.rs:1629-1646`)、`detail_of` が `connect_ms = total_ms.saturating_sub(dns_ms)`
  とするので (`crates/tunnel/src/tunnel.rs:193-212`)、ミスした経路では `/recent` の `connect` が 0 に落ちる (ミスした 54 本のうち
  **40 本**。その 40 本のカーネル RTT の中央値は 11.4 ms)。つまり T14.1 の前提はそもそも検証できなかった → **T15.0 (1)**。
- **尾 (p90 53.5 / p95 78.0 ms) は網ではなく箱の中だが、原因はまだ測れていない**。尾 119 本すべてで SYN 再送 0・オリジン再送 0、
  カーネル RTT の分布も尾でない接続と同じ (p50 8.23 対 8.21)、到着の密度も同じ、利用者と無関係な canary にも同じ尾が出る
  (p95 46.1 ms)。同じ時間帯にプロセスの CPU が cgroup の 0.5 コアに張り付いているが、**因果は支持されていない**:
  絞りはトンネル 2 本より 0.7〜2.2 時間前から始まっており、尾はトンネルが開く前の 10 時間 (p90 67.7 ms) の方が後 (p90 48.3 ms) より
  重く、再起動前の別プロセスにも同じ尾がある (p90 58.8 ms)。**尾は再起動も版の入れ替えも生き延びている** → **T15.5** は
  「まず測る」から始める。なお**完了の定義は尾を 1 行も見ていない**し、`/events` の異常の基準線が既に p95 80 ms 前後なので、
  いまの計器では尾は「平常」として扱われる。
- **物差しの但し書き** (次に読む人のために):
  - 「平常時 = 1 標本 300 本未満」の切り方は、同時接続 32 の本当のバースト 1 時間 (ミス率 0.381) を**残し**、同時接続 6 / 8 で
    本数だけ多い 2 時間 (ミス率 0.028 / 0.019) を**捨てる**向きに働く。`$.history["3600"]` を再起動時刻で切ったあと切り方を
    9 通り変えると、ミス率は **0.094〜0.171** に振れる: 本数で切る `connects` < 100 / 200 / **300 (決まり)** / 500 / 1000 が
    **0.140 / 0.156 / 0.144 / 0.171 / 0.147** (< 1000 は切らないのと同じ)、同時接続で切る `active_max` < 20 が **0.094**、
    直近 24 時間が **0.110**、最初の 6 時間を飛ばすと **0.130**、最初の 6 時間だけなら **0.291**。
    つまり **0.144 は「いちばん悪い値」ではない** — 本数の閾を 200 / 500 にすると **0.156 / 0.171** で基準 0.15 を越える。
    切らずに全時間で読む 0.147 の余裕は 2%、決まりどおりの 0.144 で 4%。定常状態 (同時接続で切る側) は 0.09〜0.11。
    **判定がぎりぎりのときは、値 1 つではなくこの幅で読む。**
  - `mtalk.google.com` の 0.01 は**再起動後の最初の約 11 時間の 134 要求**に乗っている (`idle_secs` 91,388 = 25.4 時間、
    直近 24 時間の `$.hosts_series` では要求 0 件)。しかも 132 回のミスを消すのに裏の引き直しを 907 回払っている
    (救ったミス 1 回あたり 6.9 回)。戻ってきたら測り直しが要る。
  - `/recent` は 256 KiB で切れて「**最後に閉じた** 615 本」しか入らない (環には 2,000 本、通算 3,308 本)。`at` (開始時刻) の
    最小で窓を切ると 2 時間ぶん (85 本) が抜けるので、正しい左端は **`min(at + secs)`**。`ms.dns` はミリ秒の整数なので
    個票から数えたミスは常に下限 (同じ窓で `$.history["60"]` は 55、個票は 53)。
  - `/hosts` は 1,000 件で満席・`shown` 639 で切れており、窓の Δ要求 3,328 本のうち **831 本 (25%) は名前を持たない `other`** の
    行に畳まれている。`/profile?res=60` も 1,440 標本のうち 362 しか返らない。それでも雪像の top-level の `dropped` は `[]` の
    ままなので、読む人は欠けに気づけない (`dropped` は「部ごと落とした」の意味) → **T15.0 (11)**。
  - `/events` の anomaly 15 件に名前解決のものは 0 件だが、これは**健全さの証拠にならない**。`DnsSlow`
    (`crates/metrics-watch/src/anomaly.rs:101`) が立つのは 5 分のミス 1 回の平均が 100 ms を超えたときだけで、実測 16.3 ms の
    約 6 倍。ミスの**率**を見る規則は 5 種のどれにも無い。
- **判定の外で起きたこと (2026-09-18、記録として)**: 雪像の 86 秒前に新しい接続元 (クラウドの IP) が現れ、15 分で 92 件の
  forward を投げた (宛先はその接続元自身の A レコード、応答は 308、73 件はプロキシのキャッシュ HIT)。その 23 分後
  (05:55〜05:57Z) に別のホストへの CONNECT が 5 本通っている (4 本はポート 80、1 本は 443 で 5.7 kB)。この接続元が誰なのかは
  プロキシからは IP と Host 名しか見えず決められない (反証役は「利用者自身のドメインが指すホスト」の読みも残している)。
  断りの数が全部 0 なのは、断る設定を 1 つも入れていないから (容量の余裕を示す `rejected_overload` / `evicted_idle` の 0 とは意味が違う)。
  **利用者の決定 (2026-09-18)**: フリーにアクセスできるままにし、接続元・宛先・ポート・接続元ごとの上限の制限は**入れない** (T15.10 を「入れない」で閉じた)。

### デプロイ先の現在地 (2026-09-10、`nagoya.sorahost.net:50697`)

**§2 の表は手元の loopback**。こちらは実際に動いているプロキシ (Pterodactyl コンテナ、cgroup 256 MiB、
`ulimit -n` 1024、既定プロファイル、ログ `info`) を 2026-09-10 05:37 UTC に測ったもの
(稼働 149,206 秒 = 41.4 時間、`.rrd` の通算 8,953 要求)。**同じ集計は
`scripts/status-diff.py <snapshot> --aaaa <表>` で再現できる** (T12.0)。生の JSON はリポジトリに入れない
(個人の閲覧先が入る。手元の `~/rust-http-proxy-status/` に置いた)。表の中身は §5 Phase 12 の
「合議の結果」と同じ。

| デプロイ先の実測 (2026-09-10) | 値 | §2 (loopback) との対応 |
|---|---|---|
| 要求の内訳 (永続化された 8,953 件) | **CONNECT 8,800 (98.3%)**、forward 140 (1.6%。HIT 56 / MISS 84)、エラー 12、ブロック 1 | §2 の主戦場 (forward keep-alive、HIT) は **1.6%** |
| 要求の頻度 (起動から 41.4 時間で 1,854 件) | **0.012 req/s** (80 秒に 1 件) | §2 は 38,619 req/s。**CPU/要求 41 us は 1 日で 2 秒ぶん** |
| CONNECT 確立、AAAA の無いホスト (22 ホスト、2,063 件) | **p50 の中央値 5.10 ms、avg の中央値 7.10 ms** (discord.com 5.2、x.com 5.0、www.mof.go.jp 5.0) | §2 の 140 us/本 は **この 2.7%**。残りは名前解決と RTT |
| CONNECT 確立、AAAA のあるホスト (25 ホスト、**6,022 件 = CONNECT の 74.5%**) | **avg 257 ms** (avg の中央値 257.4、p50 の中央値 260。www.dlsite.com 257.4、registry.npmjs.org 256.7、example.com 257.0、www.google.com 257.6) | **§2 には存在しない経路**。250 ms は `net.rs` の `STAGGER` (`crates/net/src/net.rs:19`) そのもの |
| 41.4 時間で失った待ち時間 | 6,022 × 250 ms = **1,506 秒 (25 分)** | — |
| ホスト別統計の p50 = p95 = max になっているホスト | **50 ホスト中 29** (datadog: avg 257.2、p50 = p95 = max = 280) | 区間 `LATENCY_BOUNDS_MS` (`crates/metrics/src/metrics.rs:45`) が 10 段しか無い |
| RSS / cgroup | **215.9 MB / 268.4 MB (256 MiB)**、うちメモリキャッシュのバラスト 201.3 MB、キャッシュの実体は **1 件 798 B** | §2 の「暇なトンネル 5,000 本で 29.9 MB」は `--lite` (バラスト無し)・6.6 GiB の機械での値 |
| 上限 | `max_conns` 240 (`ulimit -n` 1024 から)、`max_threads` 128、生きているスレッド 1、接続 2 | §2 の「同時 5,000 本」はここでは起きない (240 で 503) |
| DNS キャッシュ (起動から) | 35 件、hit 2,748 / miss 966 (26%)、TTL 60 秒。**hit + miss = 3,714 は要求 1,868 の 1.99 倍** (T12.7) | — |
| オリジンプール (起動から 41.4 時間) | **new 1 / reused 0** | §2 の keep-alive とプールの前提はここでは 1 度も効いていない |
| 接続元 | **1 つ** (`10.255.0.1`、Wings の NAT) | 接続元別の統計はこの環境では 1 行しか出ない |
| `GET /` (ブラウザでプロキシの URL を開く) | **502 Bad Gateway、本文 0 バイト** | 自分宛てなのに `Host` へ転送しに行き、コンテナが自分の公開アドレスに届かないので 502 |

**手元から見た値** (`scripts/probe-deployed.sh nagoya.sorahost.net:50697`、3 回ずつ、2026-09-10):

| 経路 | 状態コード | `time_starttransfer` | `time_appconnect − time_connect` |
|---|---|---|---|
| `GET /status` | 200 | 0.070〜0.214 秒 | — (TLS 無し) |
| `GET /` (自分宛て) | **502** | 0.061〜0.316 秒 | — |
| `http://example.com/` (forward) | 200 | 0.070〜0.087 秒 | — (**キャッシュ HIT。出て行っていない**) |
| `https://www.dlsite.com/` (**AAAA あり**) | 301 | 0.475〜0.496 秒 | **0.320〜0.329 秒** |
| `https://discord.com/` (**AAAA なし**) | 200 | 0.187〜0.207 秒 | **0.072〜0.113 秒** |

**AAAA の有無で app-conn が約 0.23 秒違う** = `STAGGER` の 250 ms。プロキシ側の集計 (257 ms 対 7 ms) と
向きも桁も合う。

### デプロイ先の現在地 (2026-09-12、Phase 12 をデプロイして 58.6 時間)

2026-09-10 11:38 UTC に版 `0.1.0+d75f1f2` (Phase 12 の全部) を再デプロイし、2026-09-12 20:18 UTC に `/status` `/history` `/metrics` を
取った (稼働 210,860 秒。`.rrd` は版 2 で作り直されたので通算 = デプロイ後)。生の JSON は `~/rust-http-proxy-status/2026-09-12T2018Z-*`。
**上の表 (2026-09-10) と同じ行を同じ道具で並べる**:

| 項目 | デプロイ前 (2026-09-10) | デプロイ後 (2026-09-12) | 出どころ |
|---|---|---|---|
| 要求の頻度 | 0.012 req/s (41.4 時間で 1,854 件) | **0.068 req/s** (58.6 時間で 14,410 件。使われ方が変わった) | `/status` |
| 要求の内訳 | CONNECT 98.3%、forward 1.6% | CONNECT **99.0%** (14,188)、forward 44 (HIT 3 / MISS 41)、エラー 101 | 接続元別 |
| CONNECT 確立、AAAA ありのホスト | 25 ホスト・6,022 件 (74.5%)、**avg の中央値 257.4 ms**、p50 260.0 | 29 ホスト・6,842 件 (65.1%)、**avg の中央値 9.1 ms**、p50 7.4 | `status-diff.py --aaaa` |
| CONNECT 確立、AAAA なしのホスト | 22 ホスト・2,063 件、avg 7.1 / p50 5.1 | 21 ホスト・3,663 件、avg 13.8 / p50 12.1 (ホストの顔ぶれが違う。**同じホストは変わらない**: discord.com 7.1 → 7.6、pics.dmm.co.jp 6.1 → 7.2、oauth.secure.pixiv.net 7.1 → 7.6) | 同上 |
| 同じホストの前後 | datadog 257.2 / img.dlsite.jp 257.4 / mtalk.google.com 293.8 | **8.2 / 8.3 / 35.4 ms** (mtalk は RTT 26 ms のホスト) | ホスト別 |
| 接続元から見た応答 (全要求) | avg 200.5 / p50 **327.8** / p95 484.0 ms | avg 28.2 / p50 **8.0** / p95 53.4 ms (max 30,029 = 接続の締め切り) | 接続元別 |
| 履歴の窓つき分位点 (CONNECT 確立、全 58.6 時間 14,267 本) | (無かった) | **avg 27.0 / p50 8.7 / p95 56.3 ms**。平常時の 1 時間は p50 7.5〜9.5 / p95 8〜25 ms | `/history?res=3600` |
| バースト (09-11 17〜23 時、09-12 02 時 UTC。1 時間に 3,435 / 4,966 本) | (見えなかった) | **同時接続の山 195〜218 本 (上限 240)、fd 499〜501 (上限 1,024)、スレッド 79 (上限 128)**、名前解決ミスの平均 38〜110 ms、エラー 8〜32 件/時、確立の最大 30,000 ms (締め切り) | `active_max` / `fds_max` / `threads_max` |
| 名前解決 | hit 2,748 / miss 966 (1 要求 2 回引いていた) | hit 8,339 / miss 5,981 (**0.42 ミス/要求**)、990 件、**ミス 1 回の平均 36.5 ms** (上位 50 ホストでは 9.5〜13 ms。差は失敗する解決 ≈ 2 秒 × 80 件とバースト時の混雑) | `dns` |
| エラーの原因 | (件数だけ。12 件) | **dns 80 / reset 8 / refused 5 / timeout 4 / unreachable 2 / tls 0 / loop 0** (= 99。`negative_hits` 81) | `/metrics` `sorahost_errors_total` |
| IPv6 | (見えなかった) | attempts 53 / wins 0 / losses 52、**`v4_first` = true**。53 回の試行は「IPv4 が 250 ms で勝てなかった遠いホスト」(urlscan.io = 欧州、v4 の接続 250.6 ms) で `STAGGER` 後に起動されたもの | `ipv6` |
| ホスト別 p50 = p95 = max | 50 ホスト中 29 | **0** | ホスト別 |
| RSS / cgroup | 215.9 MB / 268.4 MB、バラスト 201.3 MB | **35.4 MB** / 268.4 MB (使用 32.3 MB)、メモリキャッシュ 13.4 MB・21 件、バラスト 0 | `cache.system` |
| ディスク | 91.7% (バラスト 2.9 GB) | **2.4%**、ディスクキャッシュ 18.6 MB・29 件、バラスト 18.5 MB | 同上 |
| スレッド / fd (平常時) | (見えなかった) | 9 本 / 24 本 (上限 1,024) | `/status` |
| 状態ファイル | 1 MiB | **4 MiB**、flush 3,514 回、書込エラー 0 | `state_file` |
| オリジンプール | new 1 / reused 0 | new 37 / reused 17 | `origin_connections` |
| `GET /` | 502 | **200** (案内) | `probe-deployed.sh` |
| 手元から `time_appconnect − time_connect` | www.dlsite.com **0.320〜0.329 秒** / discord.com 0.072〜0.113 | www.dlsite.com **0.089〜0.111 秒** / discord.com 0.077〜0.111 | 同上 (3 回ずつ) |
| 版 | (出ない) | `0.1.0+d75f1f2` | `/status` |

**読み方**: AAAA ありのホストの 250 ms は消え (257.4 → 9.1 ms)、AAAA なしのホストと同じ桁になった。接続元から見た p50 は
327.8 → 8.0 ms。**残っている待ちは「名前解決のミス (0.42 回/要求 × 約 10 ms、失敗すると約 2 秒)」「遠いホストの RTT
(urlscan.io 250 ms)」「バースト時の同時接続の山 (218 / 240)」**の 3 つで、これが Phase 13 の材料 (T13.0)。

### デプロイ先の現在地 (2026-09-16、Phase 13 をデプロイして 72.7 時間)

2026-09-13 00:22 UTC に版 `0.1.0+b2ae41f` (Phase 13 の全部) を再デプロイし、2026-09-16 01:06 UTC に `/status` `/history` `/metrics` と
Phase 13 の個票 (`/errors` `/connections` `/dns` `/log` `/hosts`) を取った。生の JSON は `~/rust-http-proxy-status/2026-09-16T0106Z-*`。
**この期間はトラフィックが少なく (3,930 要求、0.015 req/s)、バーストが 1 度も無かった** (同時接続の山 8)。
前後の比較は `/history?res=3600` を再起動時刻で分け、**バーストの無い時間帯 (1 時間 300 本未満) どうし**で並べる。

| 項目 | Phase 12 の期間 (09-10〜09-13、平常時 58 時間) | Phase 13 の期間 (09-13〜09-16、72 時間) | 出どころ |
|---|---|---|---|
| CONNECT 確立 | avg 20.9 / **p50 8.1** / p95 90.8 ms | avg 15.3 / **p50 8.3** / p95 80.7 ms | `/history` |
| 名前解決のミス | **0.55 回/接続**、ミス 1 回 12.6 ms、接続 1 本あたり 6.9 ms | **0.55 回/接続**、ミス 1 回 11.5 ms、接続 1 本あたり 6.3 ms | 同上 |
| (参考) Phase 12 の期間をバースト込みで | 0.42 回/接続、ミス 1 回 36.0 ms (失敗する解決 80 件を含む) | — | 同上 |
| 裏の引き直し (T13.1) | — | **442 回** (datadog 412、discord 20、mtalk.google.com **0**) | `/dns` |
| 主要 3 ホストのミス率 (Δ、09-12 20:18 → 09-16 01:06) | — | datadog **0.31** (2,301 要求)、discord **0.76** (489)、mtalk.google.com **1.00** (920 要求 = 920 ミス) | `/hosts` の差分 |
| 名前解決の表 | — | 90 件。最後に使ってから 60 秒未満 **2 件**、10 分未満 1、1 時間未満 3、**1 時間以上 84** | `/dns` |
| エラー | 平常時 0 (バーストで 99) | **0** (`/errors` も空、負のキャッシュの出番なし) | `/errors` `/metrics` |
| 上限 | 山 33 (バーストで 218) | 山 **8**、`evicted_idle` 0、`rejected_overload` 0 (T13.2 は出番なし) | `/status` |
| IPv6 | — | attempts 22 / wins 0、`v4_first` (6 時間で確定、`/log` に warn 1 行) | `/log` |
| RSS / ディスク | 35.4 MB / 2.4% | **21.1 MB** / 2.5% | `/status` |
| 接続元 | 1 つ (NAT) | **2 つ**: `10.255.0.1` (NAT、09-15 16:28 まで) と `161.33.196.121` (463 要求、宛先は mtalk / discord / jma = 従来と同じ顔ぶれ) | `/status` `/connections` |
| 手元から www.dlsite.com の `time_appconnect − time_connect` | 0.089〜0.111 秒 | 0.071〜0.122 秒 | `probe-deployed.sh` |

**読み方**: 名前解決のミス率は **動いていない** (0.55 → 0.55)。T13.1 の先回りは「直近 TTL (60 秒) 内に使われた名前」にしか効かず、
主要ホストのアクセス間隔は datadog 約 2 分、mtalk.google.com 約 5 分、discord 約 10 分で TTL より長い。
ミス 1 回の平均が 36 → 11.5 ms に見えるのは、この期間にバーストと失敗する解決が無かったから (平常時どうしなら 12.6 → 11.5)。
残る待ちは「名前解決 6.3 ms/接続 (確立 15.3 ms の 41%)」と RTT (mtalk.google.com の接続 30 ms)。
