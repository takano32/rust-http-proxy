[package]
name = "proxy-prom"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
proxy-base = { path = "../base" }
proxy-net = { path = "../net" }
proxy-cache = { path = "../cache" }
proxy-stats = { path = "../stats" }
