[package]
name = "proxy-reload"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
proxy-base = { path = "../base" }
proxy-sysinfo = { path = "../sysinfo" }
proxy-net = { path = "../net" }
proxy-config = { path = "../config" }
proxy-blocklist = { path = "../blocklist" }
