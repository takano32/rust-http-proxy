[package]
name = "proxy-tunnel"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
proxy-base = { path = "../base" }
proxy-sys = { path = "../sys" }
proxy-net = { path = "../net" }
proxy-stats = { path = "../stats" }
